2025-10-26 05:08:40

任务：为 controlplane 增加最小化测试脚本并执行验证（apply/status/drain/delete/hotswap）。

执行情况：
- 新增脚本：
  - `video-analyzer/test/scripts/cp_control_smoke.ps1`：应用 YAML 管线→状态→drain→删除，全链路 PASS。
  - `video-analyzer/test/scripts/cp_control_hotswap.ps1`：基于 `det` 节点对 `yolov12m.onnx` 执行 hotswap，PASS（202→status 200）。
- 运行结果：两脚本均 PASS；/metrics 出现相应计数（apply_pipeline 202、status 200、drain 202、pipeline DELETE 202、hotswap 202）。

备注：脚本默认使用 `build-ninja/bin/config/graphs/analyzer_multistage_example.yaml` 与 `build-ninja/bin/model/*.onnx`；如需切换模型，可传参 `-ModelUri`（hotswap 脚本）。

