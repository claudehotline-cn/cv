2025-11-15 10:00

- 任务：修复概要设计文档中的 Mermaid 流程图解析错误。
- 操作：
  - 调整 `docs/design/architecture/整体架构设计.md` 中“订阅与播放流程（前端视角）”的 mermaid 图，将边标签中的 `<br>` 与 `{id}` 替换为更兼容的形式（使用引号包裹标签文本、将 `/api/subscriptions/{id}/events` 改为 `/api/subscriptions/:id/events`，并去掉节点标签中的 `<br>`），避免 Mermaid 解析失败。
- 预期：在支持 Mermaid 的渲染环境中，该流程图可以正常渲染，不再出现 `Parse error`，同时保留原有含义与可读性。*** End Patch***」}``` -->
