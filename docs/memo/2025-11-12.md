2025-11-12 20:54

- 任务：修复前端“模型/添加模型/转换进度”仅显示 5%→100%，缺少中间进度的问题。
- 变更：
  - web-front/src/views/Models.vue
    - 新增“柔性推进”逻辑（定时器），在后端事件稀疏时提供连续进度展示，并限制在阶段上限（running≤85，uploading≤99）。
    - 兼容后端 progress 为小数（0~1）或百分比（0~100）的两种上报格式。
    - 在 uploading 阶段自动将进度提升至 ≥90，并提升柔性上限至 99。
    - 在 done/failed/cancel 时清理定时器，确保状态一致。
    - 初始进度从 3% 调整为 5%，与用户感知更一致。
- 影响范围：仅前端 Models 页面“添加模型”对话框中的转换进度展示，无 API 兼容性破坏。
 - 建议测试：
  1) 上传 .onnx，平台选择 TensorRT（tensorrt_plan）；观察进度条是否从 5% 平滑推进至 85% 左右，再进入上传阶段提升至 ~99%，完成后到 100%。
  2) 模拟后端仅上报 start/done 的场景，确认中间阶段仍有平滑推进显示。
  3) 模拟 progress 上报为 0~1 的小数值，确认进度换算准确。

2025-11-12 21:35

- 任务：新增“模型删除”能力（Models 页删除按钮 + CP/VA 支持）。
- 变更：
  - video-analyzer/proto/analyzer_control.proto：新增 RPC `RepoRemoveModel` 及请求/响应消息。
  - video-analyzer/src/controlplane/api/grpc_server.cpp：实现 `RepoRemoveModel`（最佳努力卸载后删除文件夹；当前支持本地 FS 仓库，S3 未实现返回错误）。
  - controlplane/include/controlplane/grpc_clients.hpp / src/server/grpc_clients.cpp：新增 `va_repo_remove_model` 客户端封装。
  - controlplane/src/server/main.cpp：新增 HTTP 路由 `POST /api/repo/remove { model }`，调用 VA 删除模型，失败返回错误码；成功 200。
  - web-front/src/api/cp.ts：新增 `cp.repoRemove(model)`。
  - web-front/src/views/Models.vue：
    - 列表每行添加“删除”按钮（仅仓库模式启用），带确认。
    - 顶部按 ID 操作区新增 Delete 按钮。
- 注意：
  - 仅支持本地文件系统仓库路径删除；S3 仓库删除暂未实现（需 List+BatchDelete）。
  - 删除前会尝试 `unload`（最佳努力）。
- 建议测试：
  1) 在本地 FS 仓库下创建模型后，点击“删除”，确认列表刷新后不再出现该模型，仓库目录被删除。
  2) 模型处于 loaded 状态时，仍可删除（先尝试卸载）。
  3) S3 仓库场景应返回错误提示（前端提示删除失败）。

2025-11-12 21:50

- 任务：整理上下文与路线图文档。
- 变更：
  - 新增 `docs/context/CONTEXT.md`：汇总问题背景、改动、API、受影响文件、测试建议与后续工作。
 - 新增 `docs/context/ROADMAP.md`（≤800 词）：包含路线图总览（M0/M1/M2）、阶段计划表、依赖矩阵与Top-5 风险清单。
 - 说明：ROADMAP 基于 CONTEXT 制定，聚焦“进度可用性”与“仓库删除”两主线，并预留 S3 删除与子阶段进度细化的下一步。

2025-11-12 22:28

- 任务：读取并将 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md` 内容纳入当前会话上下文。
- 操作：
  - 打开并审阅两份文档，提取关键点（进度柔性推进、删除模型能力、API/路由、受影响文件、测试建议、分阶段路线图与风险清单）。
  - 在会话中记录主要内容以便后续讨论与实现对齐。
- 说明：此次为信息收集与上下文同步，不涉及代码改动与构建；后续实现与测试按文档路线推进。

2025-11-12 22:40

- 任务：设计“基于 MLflow 的模型训练流水线”。
- 产出：`docs/design/mlflow-training-pipeline.md`（草案）。
- 要点：
  - 新增 `model-trainer`（Python）执行训练，CP 以 LRO/SSE 编排；MLflow 做 Tracking/Registry。
  - 生命周期：created→preparing→running→exporting→registering→deploying→done/failed；前端新增“训练任务”。
  - 导出 ONNX 并注册到 MLflow；由 CP 调用 `convert_upload`→`load` 将模型导入仓库并加载 VA。
  - 配置示例（YAML）覆盖数据/模型/训练/导出/注册/部署；默认 FS Artifact，后续支持 MinIO/S3。
  - 路线图：T0/M0（单任务闭环）→T1/M1（对象存储与多任务）→T2/M2（超参搜索、审计与回滚）。
