2025-11-02 14:20

- 任务：整理上下文与路线图
  - 输出：docs/context/CONTEXT.md、docs/context/ROADMAP.md（容器映射、TLS、SSE、WHEP/ICE、最小验证路径）。

- 任务：订阅失败排查（Docker + DevTools）
  - Docker：确认容器（va/cp/web/vsm）健康；抓取 VA 日志出现连续“enqueue frame”→ 已在处理帧，RTSP 拉流成功。
  - DevTools：在 http://127.0.0.1:18080/ 发起订阅并接入 SSE；收到 phase=ready（202 + Location）。

- 任务：纯内网 WHEP 候选
  - 在 docker/config/va/app.yaml 配置 webrtc.ice.public_ip=192.168.50.78，禁用 mDNS，映射 UDP 10000–10100；Answer SDP 中候选已重写为内网 IP。

- 后续：
  - 若前端播放仍阻塞，检查浏览器自动播放策略（muted/playsinline）与 WHEP PATCH trickle；必要时提升 CP 超时与预热小模型。

