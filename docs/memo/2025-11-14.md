2025-11-14 00:00

- 任务：排查 VA 分析后视频在前端播放画面模糊、疑似编码异常的问题，并修正默认编码配置。
- 分析：
  - 通读 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md`，梳理训练流水线、CP/VA/前端职责以及 WHEP 播放链路。
  - 检查 VA 媒体路径：`encoder_h264_ffmpeg.cpp`、`encoder_h264_nvenc.cpp`、`pipeline.cpp`、`transport_webrtc_datachannel.cpp` 与 `media/whep_session.cpp`，确认 WebRTC/WHEP 期望接收 H.264 Annex B 码流。
  - 发现 `Application::buildEncoderConfig` 中，当 profile 未显式配置 `encoder.codec` 时，默认回退为 `jpeg`，会导致：
    - NVENC 包装器 `NvencH264Encoder` 无法将 codec 纠正为 `h264_nvenc`；
    - 下游仍按 H.264 处理（RTP 打包、WHEP 会话），但上游实际输出 JPEG，比特流与 SDP 声明不匹配，容易表现为“编码有问题/画面严重劣化”。
- 变更：
  - `video-analyzer/src/app/application.cpp:1153` 起：将 `buildEncoderConfig` 中 codec 默认回退由 `jpeg` 改为 `h264`，并在注释中说明：
    - 仅在配置显式声明时启用 JPEG；
    - 默认走 H.264，以保证 WebRTC/WHEP 传输路径与 SDP 声明一致，同时触发 NVENC 包装器自动选择 `h264_nvenc`。
  - `video-analyzer/src/media/encoder_h264_ffmpeg.cpp`：
    - 修复 NVENC H.264 SPS/PPS 注入逻辑：原来先判断 `out_packet.keyframe` 再赋值，条件永远为假，导致不会在关键帧前重复注入 SPS/PPS。
    - 现改为先根据 `AV_PKT_FLAG_KEY` 设置 `out_packet.keyframe`，再判断是否需要在关键帧前拼接 `spspps_annexb_`，保证浏览器端 H.264 解码在 IDR 切换/重连场景下更加稳定，减轻“像编码/解码有问题”的视觉表现。
- 验证：
  - 在当前容器中尝试按仓库根目录执行 `cmake -S . -B build`，因缺少 OpenCV 开发包（`OpenCVConfig.cmake`）导致配置失败，无法在本地完成完整构建与运行验证。
  - 预计效果：对于未配置 `encoder.codec` 的 profile，VA 将恢复使用 H.264（或 NVENC H.264）编码，前端 WHEP 播放的画质应从“编码异常/严重失真”恢复为正常 H.264 画质；前端 `WhepPlayer` 本身仅做 `<video>` 播放与简单样式，未发现额外造成画质下降的处理。

2025-11-14 00:20

- 任务：针对“有检测框但画面像噪点/颗粒”的反馈，优化 NVENC 默认码控与参数集注入逻辑，减轻画质噪点。
- 分析：
  - `logs/video-analyzer-release.log` 中可见编码器实际使用 `h264_nvenc`，分辨率 1280x720@30，码率约 3500kbps。
  - 用户反馈在分析面板（有 overlay 检测框）下画面存在明显噪点，更接近主观画质问题而非比特流错误。
  - 现有 NVENC 默认配置为 `rc=cbr` 且 `spatial_aq=0`、`temporal_aq=0`、`aq-strength=0`，在复杂场景下容易出现量化噪点。
  - 同时发现关键帧前 SPS/PPS 注入逻辑顺序有误，可能在切流/重连时放大主观“画面不稳”的感知。
- 变更：
  - `video-analyzer/src/media/encoder_h264_ffmpeg.cpp`：
    - 修正关键帧标记与 SPS/PPS 注入顺序：先依据 `AV_PKT_FLAG_KEY` 设置 `out_packet.keyframe`，再根据 `spspps_ready_` 判定是否在 IDR 前拼接 `spspps_annexb_`，确保每个关键帧都带完整参数集。
    - 调整 NVENC 默认码控策略：
      - 默认 `rc` 从 `cbr` 提升为 `cbr_hq`，在相同码率下优先主观质量；
      - 默认开启空间/时间 AQ：`spatial_aq=1`、`temporal_aq=1`，并将 `aq-strength` 默认提升为 `8`；
      - 保持环境变量 `VA_NVENC_RC`、`VA_NVENC_SPAQ`、`VA_NVENC_TAQ`、`VA_NVENC_AQ_STRENGTH` 作为覆盖入口，便于在极限低延迟/低算力场景下按需下调。
- 预期：
  - 在保持 profile 码率不变（例如 det_720p 3500kbps）的前提下，NVENC 输出 H.264 流在平滑区域的颗粒/噪点应明显减轻。
  - 若仍觉得不清晰，可在 `profiles.yaml` 或部署配置中进一步将 `bitrate_kbps` 提升至 5000–8000 以对齐源级画质。

2025-11-14 12:40

- 任务：在 docker 环境中实际调试 WHEP 播放画质，并提升编码配置。
- 操作与变更：
  - 检查 `docker/compose/docker-compose.yml`，确认 `va` 服务挂载 `docker/config/va` 作为运行时配置，并通过 `docker compose ps` 确认 `va/cp/web` 等容器状态正常。
  - 利用 `docker logs va` 确认 NVENC 实际运行配置为 `rc=cbr_hq sp_aq=1 tm_aq=1`，并观察编码日志（H.264 NVENC、720p@30）。
  - 在 `docker/config/va/profiles.yaml` 中，将 `det_720p` / `seg_720p` 的 `bitrate_kbps` 从 3500 提升至 8000，再到 10000，以减轻噪点和颗粒感。
  - 在 `docker/config/va/app.yaml` 中，将 `overlay_alpha` 从 0.25 调整为 0.15，降低半透明填充对主观“脏感”的影响。
  - 通过 `docker compose -f docker/compose/docker-compose.yml restart va` 重启 VA 容器，使配置生效；用户在前端分析面板中确认画质从“明显模糊/噪点多”提升到“明显好很多”，在 8–10Mbps 下接近源级流清晰度。
- 结论：
  - 当前 docker 环境下推荐的 VA H.264/NVENC 基线为：1280x720@30fps + 8–10Mbps + `rc=cbr_hq` + 开启空间/时间 AQ，overlay 使用较低透明度填充。
  - 画质问题主要归因于重编码码率与默认 AQ 配置，而非比特流损坏或 WHEP 协议错误；后续可在 Grafana 中补充编码相关指标，用于长期监控与调参。

2025-11-14 12:45

- 任务：基于本次对话重新整理上下文与路线图文档。
- 变更：
 - `docs/context/CONTEXT.md`：重写为 2025-11-14 版，补充视频分析与 WHEP 播放链路、VA 编码与画质调优、docker 部署配置（`docker/config/va/*`）以及本次编码参数调整的背景与结论。
  - `docs/context/ROADMAP.md`：在原有训练/部署路线图基础上，增加“VA 编码画质基线”相关内容；在 M1 中明确 H.264/NVENC 画质目标，在 M2 中纳入画质调参和可观测性；更新依赖矩阵与 Top-5 风险，加入 WHEP 播放与编码参数漂移相关风险。
- 说明：
  - 新版 CONTEXT/ROADMAP 将训练流水线、一键部署、WHEP 播放和画质调优统一到一个叙事中，后续调试视频质量或扩展多分辨率/多码率策略时可直接引用。

2025-11-14 13:10

- 任务：将本次 VA 编码画质调优及 CONTEXT/ROADMAP 更新相关改动提交到远程仓库。
- 操作：
  - 在当前环境中跳过构建和自动化测试（OpenCV 依赖未满足，且用户明确要求直接提交）。
  - 使用 `git status` 和 `git diff` 确认修改范围主要集中在 VA 编码逻辑、docker 配置和文档/备忘录。
  - 执行 `git commit` 与 `git push` 将变更推送到远程仓库。
- 说明：本次提交仅包含已在前文记录的修改内容，未新增额外代码路径。
