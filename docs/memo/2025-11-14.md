2025-11-14 00:00

- 任务：排查 VA 分析后视频在前端播放画面模糊、疑似编码异常的问题，并修正默认编码配置。
- 分析：
  - 通读 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md`，梳理训练流水线、CP/VA/前端职责以及 WHEP 播放链路。
  - 检查 VA 媒体路径：`encoder_h264_ffmpeg.cpp`、`encoder_h264_nvenc.cpp`、`pipeline.cpp`、`transport_webrtc_datachannel.cpp` 与 `media/whep_session.cpp`，确认 WebRTC/WHEP 期望接收 H.264 Annex B 码流。
  - 发现 `Application::buildEncoderConfig` 中，当 profile 未显式配置 `encoder.codec` 时，默认回退为 `jpeg`，会导致：
    - NVENC 包装器 `NvencH264Encoder` 无法将 codec 纠正为 `h264_nvenc`；
    - 下游仍按 H.264 处理（RTP 打包、WHEP 会话），但上游实际输出 JPEG，比特流与 SDP 声明不匹配，容易表现为“编码有问题/画面严重劣化”。
- 变更：
  - `video-analyzer/src/app/application.cpp:1153` 起：将 `buildEncoderConfig` 中 codec 默认回退由 `jpeg` 改为 `h264`，并在注释中说明：
    - 仅在配置显式声明时启用 JPEG；
    - 默认走 H.264，以保证 WebRTC/WHEP 传输路径与 SDP 声明一致，同时触发 NVENC 包装器自动选择 `h264_nvenc`。
  - `video-analyzer/src/media/encoder_h264_ffmpeg.cpp`：
    - 修复 NVENC H.264 SPS/PPS 注入逻辑：原来先判断 `out_packet.keyframe` 再赋值，条件永远为假，导致不会在关键帧前重复注入 SPS/PPS。
    - 现改为先根据 `AV_PKT_FLAG_KEY` 设置 `out_packet.keyframe`，再判断是否需要在关键帧前拼接 `spspps_annexb_`，保证浏览器端 H.264 解码在 IDR 切换/重连场景下更加稳定，减轻“像编码/解码有问题”的视觉表现。
- 验证：
  - 在当前容器中尝试按仓库根目录执行 `cmake -S . -B build`，因缺少 OpenCV 开发包（`OpenCVConfig.cmake`）导致配置失败，无法在本地完成完整构建与运行验证。
  - 预计效果：对于未配置 `encoder.codec` 的 profile，VA 将恢复使用 H.264（或 NVENC H.264）编码，前端 WHEP 播放的画质应从“编码异常/严重失真”恢复为正常 H.264 画质；前端 `WhepPlayer` 本身仅做 `<video>` 播放与简单样式，未发现额外造成画质下降的处理。

2025-11-14 00:20

- 任务：针对“有检测框但画面像噪点/颗粒”的反馈，优化 NVENC 默认码控与参数集注入逻辑，减轻画质噪点。
- 分析：
  - `logs/video-analyzer-release.log` 中可见编码器实际使用 `h264_nvenc`，分辨率 1280x720@30，码率约 3500kbps。
  - 用户反馈在分析面板（有 overlay 检测框）下画面存在明显噪点，更接近主观画质问题而非比特流错误。
  - 现有 NVENC 默认配置为 `rc=cbr` 且 `spatial_aq=0`、`temporal_aq=0`、`aq-strength=0`，在复杂场景下容易出现量化噪点。
  - 同时发现关键帧前 SPS/PPS 注入逻辑顺序有误，可能在切流/重连时放大主观“画面不稳”的感知。
- 变更：
  - `video-analyzer/src/media/encoder_h264_ffmpeg.cpp`：
    - 修正关键帧标记与 SPS/PPS 注入顺序：先依据 `AV_PKT_FLAG_KEY` 设置 `out_packet.keyframe`，再根据 `spspps_ready_` 判定是否在 IDR 前拼接 `spspps_annexb_`，确保每个关键帧都带完整参数集。
    - 调整 NVENC 默认码控策略：
      - 默认 `rc` 从 `cbr` 提升为 `cbr_hq`，在相同码率下优先主观质量；
      - 默认开启空间/时间 AQ：`spatial_aq=1`、`temporal_aq=1`，并将 `aq-strength` 默认提升为 `8`；
      - 保持环境变量 `VA_NVENC_RC`、`VA_NVENC_SPAQ`、`VA_NVENC_TAQ`、`VA_NVENC_AQ_STRENGTH` 作为覆盖入口，便于在极限低延迟/低算力场景下按需下调。
- 预期：
  - 在保持 profile 码率不变（例如 det_720p 3500kbps）的前提下，NVENC 输出 H.264 流在平滑区域的颗粒/噪点应明显减轻。
  - 若仍觉得不清晰，可在 `profiles.yaml` 或部署配置中进一步将 `bitrate_kbps` 提升至 5000–8000 以对齐源级画质。

2025-11-14 12:40

- 任务：在 docker 环境中实际调试 WHEP 播放画质，并提升编码配置。
- 操作与变更：
  - 检查 `docker/compose/docker-compose.yml`，确认 `va` 服务挂载 `docker/config/va` 作为运行时配置，并通过 `docker compose ps` 确认 `va/cp/web` 等容器状态正常。
  - 利用 `docker logs va` 确认 NVENC 实际运行配置为 `rc=cbr_hq sp_aq=1 tm_aq=1`，并观察编码日志（H.264 NVENC、720p@30）。
  - 在 `docker/config/va/profiles.yaml` 中，将 `det_720p` / `seg_720p` 的 `bitrate_kbps` 从 3500 提升至 8000，再到 10000，以减轻噪点和颗粒感。
  - 在 `docker/config/va/app.yaml` 中，将 `overlay_alpha` 从 0.25 调整为 0.15，降低半透明填充对主观“脏感”的影响。
  - 通过 `docker compose -f docker/compose/docker-compose.yml restart va` 重启 VA 容器，使配置生效；用户在前端分析面板中确认画质从“明显模糊/噪点多”提升到“明显好很多”，在 8–10Mbps 下接近源级流清晰度。
- 结论：
  - 当前 docker 环境下推荐的 VA H.264/NVENC 基线为：1280x720@30fps + 8–10Mbps + `rc=cbr_hq` + 开启空间/时间 AQ，overlay 使用较低透明度填充。
  - 画质问题主要归因于重编码码率与默认 AQ 配置，而非比特流损坏或 WHEP 协议错误；后续可在 Grafana 中补充编码相关指标，用于长期监控与调参。

2025-11-14 12:45

- 任务：基于本次对话重新整理上下文与路线图文档。
- 变更：
 - `docs/context/CONTEXT.md`：重写为 2025-11-14 版，补充视频分析与 WHEP 播放链路、VA 编码与画质调优、docker 部署配置（`docker/config/va/*`）以及本次编码参数调整的背景与结论。
  - `docs/context/ROADMAP.md`：在原有训练/部署路线图基础上，增加“VA 编码画质基线”相关内容；在 M1 中明确 H.264/NVENC 画质目标，在 M2 中纳入画质调参和可观测性；更新依赖矩阵与 Top-5 风险，加入 WHEP 播放与编码参数漂移相关风险。
- 说明：
  - 新版 CONTEXT/ROADMAP 将训练流水线、一键部署、WHEP 播放和画质调优统一到一个叙事中，后续调试视频质量或扩展多分辨率/多码率策略时可直接引用。

2025-11-14 13:10

- 任务：将本次 VA 编码画质调优及 CONTEXT/ROADMAP 更新相关改动提交到远程仓库。
- 操作：
  - 在当前环境中跳过构建和自动化测试（OpenCV 依赖未满足，且用户明确要求直接提交）。
  - 使用 `git status` 和 `git diff` 确认修改范围主要集中在 VA 编码逻辑、docker 配置和文档/备忘录。
  - 执行 `git commit` 与 `git push` 将变更推送到远程仓库。
- 说明：本次提交仅包含已在前文记录的修改内容，未新增额外代码路径。

2025-11-14 13:30

- 任务：分析 VA GPU 零拷贝分支（NVDEC→CUDA 预处理→IoBinding→GPU NMS）与 CPU 路径在检测框精度上的差异。
- 分析：
  - 对比 `LetterboxPreprocessorCPU` 与 `LetterboxPreprocessorCUDA` 实现，确认两者在缩放比例、pad 计算和 `LetterboxMeta` 回填上是一致的，区别主要在插值方式：
    - CPU 路径：`preproc_letterbox_cpu.cpp` 使用 OpenCV `cv::resize`（双线性插值）+ BGR→NCHW(F32)；
    - GPU 零拷贝路径：`preproc_letterbox_cuda.cpp` 中 NV12→NCHW 的 CUDA kernel `letterbox_nv12_to_nchw_fp32` 使用最近邻采样（nearest），并直接在设备侧完成 Letterbox。
  - YOLO 后处理部分（`postproc_yolo_det.cpp` 中 CPU/GPU 解码 + NMS）在坐标反算逻辑上已经对齐，且共享同一套 `LetterboxMeta` 信息，问题更可能来自前处理阶段的采样差异，而非解码或 NMS。
- 结论：当前 GPU 零拷贝分支因为 NV12→NCHW 预处理使用最近邻插值，相比 CPU 双线性路径会在小目标和边缘区域产生信息损失，表现为“检测框不如 CPU 路径准确”。后续可通过为 CUDA kernel 增加双线性分支，并默认对检测模型使用双线性插值来收敛两条路径的精度差异。

2025-11-14 13:50

- 任务：实现 CUDA letterbox kernel 的双线性采样，并让 GPU 零拷贝分支默认使用双线性插值，对齐 CPU 前处理的行为。
- 变更：
  - `video-analyzer/src/analyzer/cuda/preproc_letterbox_kernels.cu`：
    - 在 `k_letterbox_bgr_to_nchw_fp32` 中引入 `nearest` 分支，当 `nearest==0` 时按目标像素中心计算 `src_x/src_y`（带 0.5f 偏移）并对 4 个邻近像素做双线性插值；`nearest==1` 时保留原先的 round 最近邻逻辑。
    - 在 `k_letterbox_nv12_to_nchw_fp32` 中同样实现双线性路径：对 Y 平面在 full-res 上做双线性插值，对 UV 平面在 subsampled 网格上做双线性插值（先将坐标缩放到 1/2 分辨率，再按 2x2 邻域插值），最后通过 `nv12_to_bgr` 统一转换到 BGR 归一化值，避免 NV12 路径与 CPU BGR 路径的几何偏差。
  - `video-analyzer/src/analyzer/preproc_letterbox_cuda.cpp`：
    - NVDEC NV12 零拷贝主路径（`letterbox_nv12_to_nchw_fp32`）将 `nearest` 参数从 `true` 改为 `false`，默认走双线性采样；BGR staging 路径调用 `letterbox_bgr_to_nchw_fp32` 也改为 `nearest=false`，并更新注释为“默认使用双线性采样”。
  - `video-analyzer/src/analyzer/multistage/node_roi_batch_cuda.cpp`：
    - ROI NV12→NCHW 的 CUDA 预处理同样将 `nearest` 改为 `false`，保证多阶段 ROI batch 在 GPU 路径下与 CPU ROI 预处理的插值行为一致。
- 预期：
  - 在相同 `LetterboxMeta`（scale/pad_x/pad_y）和相同模型输出的前提下，GPU 零拷贝分支经过双线性预处理后，检测框的位置和数量应更接近 CPU 路径，特别是对小目标和边缘区域的召回会提升。
  - 现有使用最近邻的路径仍可通过 `nearest=true` 保留（目前默认所有检测相关路径均使用双线性），后续如需极致低延迟可再按需开放配置开关。

2025-11-14 14:10

- 任务：继续缩小 GPU 零拷贝路径与 CPU 路径在检测框上的差异，对齐 YOLO 解码阶段的 sigmoid 判定逻辑。
- 变更：
  - `video-analyzer/src/analyzer/cuda/yolo_decode_kernels.hpp/.cu`：
    - 为 `yolo_decode_to_yxyx` / `yolo_decode_to_yxyx_fp16` 增加参数 `use_sigmoid`，内核中仅在 `use_sigmoid==1` 时对类别分数应用 sigmoid；否则假定模型输出已是概率，直接使用原始分数，与 CPU 版 `YoloDetectionPostprocessor` 中的 `need_sigmoid` 行为一致。
  - `video-analyzer/src/analyzer/postproc_yolo_det.cpp`（`YoloDetectionPostprocessorCUDA::run`）：
    - 调整 `normalized` 的探测逻辑，改为和 CPU 路径相同的按通道采样方式（在第 0 个检测上采样前若干属性），避免 GPU/CPU 在预缩放判定上出现偏差。
    - 新增 `need_sigmoid` 判定：在 GPU 路径中同样对第 0 个检测的若干类别分数进行采样（支持 F32/F16），若取值完全落在 `[0,1]` 内则视为概率分布、跳过 sigmoid；否则视为 logit，交由 CUDA kernel 在设备侧统一做 sigmoid。
    - 调用 `yolo_decode_to_yxyx` / `_fp16` 时传入 `need_sigmoid?1:0`，并在日志中追加 `need_sigmoid` 字段，便于线上核对模型导出习惯（logit vs prob）。
- 预期：
  - 对于“最后一层已带 Sigmoid”的 YOLO 导出，GPU 路径将不再做二次 sigmoid，从而与 CPU 路径在置信度阈值与候选框集合上进一步对齐，剩余差异主要来自数值精度与插值细节。

2025-11-14 14:20

- 任务：修复 Docker GPU 构建过程中 `postproc_yolo_det.cpp` 缺少头文件导致的编译错误。
- 问题：
  - 在实现 F16→F32 手动转换的 `h2f` lambda 时引入了 `std::memcpy` 与 `uint32_t`，但当前 `postproc_yolo_det.cpp` 只包含 `<algorithm>/<cmath>/<functional>/<numeric>/<vector>`，未显式包含 `<cstring>` 与 `<cstdint>`。
  - Dockerfile.gpu 构建日志中出现错误：`error: 'memcpy' is not a member of 'std'`，导致 `VideoAnalyzer` 目标编译失败。
- 修复：
  - 在 `video-analyzer/src/analyzer/postproc_yolo_det.cpp` 顶部补充 `#include <cstdint>` 与 `#include <cstring>`，满足 `uint32_t` 和 `std::memcpy` 的声明需求。
- 预期：
  - 重新构建 Docker GPU 镜像时，`postproc_yolo_det.cpp` 能够顺利编译通过，不再出现 `std::memcpy` 相关错误。

2025-11-14 14:30

- 任务：进一步精准对齐 NV12 CUDA 双线性预处理，减少 YUV→BGR 转换对小目标检测的影响。
- 分析：
  - 在 `letterbox_nv12_to_nchw_fp32` 的双线性分支中，Y 分量使用 `src_x/src_y` 计算出的 `fx/fy` 和权重 `w00..w11` 做插值是合理的，但 UV 分量在缩放到 1/2 分辨率后的 `(cx, cy)` 上仍然复用了 Y 的 `w00..w11`，而不是基于 `fx_c/fy_c` 重新计算权重，导致色度插值点与亮度插值点不完全对齐，可能造成细节颜色偏差，对小目标检测不利。
- 变更：
  - `video-analyzer/src/analyzer/cuda/preproc_letterbox_kernels.cu`：
    - 在 NV12 双线性路径中，保持 Y 分量插值逻辑不变（使用 `fx/fy` 计算 `w00..w11`），但对 UV 分量增加独立的权重计算：
      - 先在 chroma 坐标 `(cx, cy) = (src_x/2, src_y/2)` 上计算小数部分 `fx_c/fy_c`；
      - 基于 `fx_c/fy_c` 生成 `cw00/cw10/cw01/cw11`，用于 2x2 UV 采样点的双线性插值；
      - 避免继续沿用亮度权重 `w00..w11`，减小 Y/UV 采样位置错位带来的颜色误差。
- 预期：
  - NVDEC→NV12→CUDA letterbox 的颜色还原会更接近 CPU 路径（FFmpeg + OpenCV），特别是对小目标区域的纹理和对比度有帮助，从而缓解“小目标人等在 GPU 零拷贝路径下漏检”的问题。

2025-11-14 14:45

- 任务：编写测试脚本，对比“CPU-like 路径”和“GPU/IoBinding 路径”在检测框数量上的差异。
- 变更：
  - 新增脚本：`video-analyzer/test/scripts/compare_cpu_gpu_boxes_detail.py`：
    - 通过 `/api/engine/set` 配置两种模式：
      - CPU 模式：`use_io_binding=true`、`device_output_views=false`、`stage_device_outputs=true`、`use_cuda_nms=false`。
      - GPU 模式：`use_io_binding=true`、`device_output_views=true`、`stage_device_outputs=false`、`use_cuda_nms=true`。
    - 在 CP 侧通过 `/api/control/set_engine` 切换 VA 推理引擎选项，通过 `/api/subscriptions` 创建订阅（CP 内部通过 gRPC 通知 VA 订阅流），并基于 `/api/logging` 返回的日志文件路径直接 tail VA 的 `ms.nms` 日志中 `boxes=<N>` 行，构造 CPU/GPU 各自的 boxes 时间序列。
    - 对齐两条时间序列（按索引配对），计算每帧的 `diff = gpu - cpu`，同时输出对应的日志行内容（含时间戳），便于在日志中定位差异最大的帧：
      - CPU/GPU 样本数、paired 样本数、`median(cpu)` / `median(gpu)`；
      - `mean_abs_diff`、`max_abs_diff`；
      - 挑选若干差异最大的样本，打印 `idx, cpu, gpu, diff` 以及对应的 `ms.nms` 日志行（cpu_log/gpu_log），用于定位 GPU 模式下盒子明显变少或为 0 的区间。
    - 对两种模式均校验 `frames>0`，若流程未正常跑起来则直接抛出异常。
- 预期：
  - 该脚本可作为“小目标漏检”问题的诊断工具，用于快速量化当前 GPU 路径相对 CPU 路径在 boxes 数量上的差异（尤其是 boxes 明显偏少或为 0 的情况），为进一步分析提供数据支撑。

2025-11-14 21:30

- 任务：整理 docs/design 目录下的设计文档，补充整体架构概要设计，并校正异步订阅与 WebRTC 文档的当前状态说明。
- 操作：
  - 新增 `docs/design/architecture/整体架构设计.md`，按专业概要设计文档结构梳理系统目标、模块划分、关键业务流程、数据设计与外部接口，并给出详细设计文档索引。
  - （历史）曾更新 `docs/design/cp_vsm_protocol/api_endpoints.md` 强调 VA HTTP 仅作为内部/调试接口，推荐外部统一通过 Controlplane `/api/*` 与 `/whep` 访问；当前版本已删除该文档，相关说明统一并入 `video_analyzer_详细设计.md` 与 `docs/design/protocol/控制平面HTTP与gRPC接口说明.md` 中。
  - 在早期迭代中曾在 `docs/design/subscription_lro/async_subscription_pipeline.md` 与 `docs/design/subscription_lro/async_subscription_hardening.md` 顶部增加“状态说明”，标注当前实现已采用 VA LRO Runner + 独立 Controlplane `/api/subscriptions` 架构；当前版本已将两篇文档内容整合进 `docs/design/subscription_lro/lro_subscription_design.md` 的附录 C/D，并删除原文件。
  - 更新 WebRTC 协议文档（现路径为 `docs/design/protocol/webrtc-protocol.md`），补充 WHEP 优先与 `VA_DISABLE_DATACHANNEL` 默认行为说明，使文档与现有 WHEP 媒体输出实现一致。
- 预期：新概要设计文档成为 `docs/design` 的统一入口，团队成员可以从该文档快速定位到具体专题设计；异步订阅与 WebRTC 相关文档与当前代码实现对齐，降低理解偏差与后续维护成本。

2025-11-14 22:00

- 任务：根据专业的软件详细设计文档规范，完善项目的详细设计文档。
- 操作：
  - 新增 `docs/design/architecture/video_analyzer_详细设计.md`，从模块划分、关键类与数据结构、核心流程（配置加载、订阅执行、多阶段 Pipeline 运行、取消与回收）、接口与交互（REST/gRPC）、非功能性设计（性能、可观测性、回退机制）等方面系统描述 `video-analyzer` 子项目的详细设计。
  - 扩充 `docs/design/architecture/controlplane_design.md`，增加模块划分、小节化的“架构视图/关键数据结构/订阅流程”等内容，补充 `Store` 与 `AppConfig` 的结构说明，使该文档既保留原有的架构目标与 gRPC 合同，又具备更完整的详细设计信息。
  - 更新 `docs/design/architecture/整体架构设计.md` 中的“详细设计文档索引”，将新增加的 `video_analyzer_详细设计.md` 纳入“控制平面与订阅”主题下，形成从概要设计 → 子系统详细设计 → 专题设计文档的清晰导航路径。
- 预期：后续在修改 VA 或 Controlplane 行为（尤其是订阅、引擎切换、观测相关逻辑）时，研发可以先从概要设计入手，再逐层深入到对应的详细设计和专题文档，减少理解成本并提升设计一致性。

2025-11-14 22:20

- 任务：补充 LRO、Triton 集成与前端分析面板的专题详细设计。
- 操作：
  - 在 `docs/design/subscription_lro/lro_subscription_design.md` 中新增“类图与时序图”章节，使用 mermaid 描述 Runner/Operation/Step/IStateStore/AdmissionPolicy 等核心类之间的关系，以及基于 LRO 的订阅执行时序。
  - 在早期版本中曾在 `docs/design/engine_multistage/triton_integration_design.md` 中新增类图与时序图，说明 `TritonGrpcModelSession` 与 `IModelSession/ModelSessionFactory/NodeModel` 的关系，以及 VA 调用 Triton gRPC 完成推理的请求—响应流程；当前版本已将该内容合并至 `docs/design/engine_multistage/triton_inprocess_integration.md` 的附录 A。
  - 新增 `docs/design/architecture/web_front_analysis_panel_详细设计.md`（当前版本已合并为 `docs/design/architecture/web_front_详细设计.md` 的第 3.2 节），详细说明 `useAnalysisStore`、`AnalysisPanel.vue`、`cp.ts` 与 `WhepPlayer` 之间的关系，给出“前端 → CP /api/subscriptions → VA 订阅 → CP /whep → WHEP 播放”的完整时序，并覆盖状态设计、取消流程与非功能性要求。
- 预期：LRO、Triton 与分析面板三块的专题详细设计补齐后，团队在排查订阅延迟、推理链路与前端播放问题时，可以直接参考相应文档中的类图和时序图，快速定位责任边界与链路关键点。

2025-11-14 22:40

- 任务：继续按专题完善 GPU 零拷贝与存储访问的详细设计。
- 操作：
  - 新增 `docs/design/engine_multistage/zero_copy_execution_详细设计.md`，系统梳理 VA GPU 零拷贝路径，从 NVDEC 解码、CUDA 预处理、IOBinding/TensorRT/Triton 推理、CUDA YOLO 后处理、GPU 叠加到 NVENC 编码的模块划分、数据流与时序图，并明确 CPU 回退路径与相关配置开关。
  - 新增 `docs/design/storage/storage_详细设计.md`，分别从 VA 与 Controlplane 两侧描述 DbPool 抽象、各 Repo 模块、REST/LRO 与 DB 的交互流程，以及 `/api/_debug/db` 等调试接口，补充数据库访问层的类图与时序说明。
  - 更新 `docs/design/architecture/整体架构设计.md` 的“推理与多阶段图”和“日志、指标与数据库”索引，将上述两份详细设计纳入，形成从架构 → 子系统 → 存储/零拷贝等专题的完整文档链路。
- 预期：以后在做性能调优（GPU 路径）或排查 DB 相关问题时，可以直接使用这些专题详细设计文档作为入口，减少在代码与零散文档之间反复跳转的成本。

2025-11-14 22:55

- 任务：补充观测（日志 + 指标）的集中详细设计。
- 操作：
  - 新增 `docs/design/observability_详细设计.md`，在 `LOGGING.md`、`METRICS.md` 的基础上，从配置、模块与类、时序与分类的角度系统梳理 VA 日志与 Prometheus 指标设计，并补充 Controlplane 的 `_metrics/summary` 与 `_debug/db` 等观测接口说明。
  - 更新 `docs/design/architecture/整体架构设计.md` 的“日志、指标与数据库”索引，将 `observability_详细设计.md` 纳入，形成从概要设计 → 观测专题详细设计 → LOGGING/METRICS/PromQL 示例的结构化文档链路。
- 预期：后续在排查问题或引入新的日志/指标时，可以优先查阅该观测专题详细设计，快速确认配置入口、模块划分与指标语义，避免在多份分散文档之间来回跳转。

2025-11-14 23:10

- 任务：补充 VSM 与训练流水线的详细设计视图。
- 操作：
  - 新增 `docs/design/video_source_manager_详细设计.md`，在原有 VSM 参考设计与 `VSM_REST_SSE与指标配置.md` 基础上，从模块划分（controller/inputs/outputs/rpc/metrics）、SourceControl gRPC 合同、SourceController/Session 数据结构、REST/SSE 行为、指标与 Controlplane/VA 协作等方面系统描述 VSM 子项目的详细设计。
  - 在 `docs/design/cv_训练流水线（training_pipeline）详细设计_v_1.md` 中补充训练流程的时序图，描述从前端发起 `/api/train/start` 到 CP TrainRunner 启动 Trainer、MLflow 记录、DB 落库、事件 SSE 推送以及满足门槛后触发 VA 导入/上线的完整时序。
  - 更新 `docs/design/architecture/整体架构设计.md` 中“媒体与前端集成”索引，加入 `video_source_manager_详细设计.md`，形成 VSM 从概要到详细的文档链路。
- 预期：后续实现或扩展 VSM 与训练流水线能力（例如增加新的输入协议、输出通道或训练任务类型）时，可以直接以这些详细设计为依据，减少设计歧义并为代码评审提供统一参照。

2025-11-14 23:25

- 任务：补充多阶段 Graph 框架的详细设计。
- 操作：
  - 新增 `docs/design/multistage_graph_详细设计.md`，围绕 `analyzer/multistage` 目录中的 `Packet/NodeContext/INode/Graph/NodeRegistry/AnalyzerMultistageAdapter` 等核心类，提供类图与执行时序，说明 YAML → Graph 构建流程、条件边语义、节点生命周期与新节点扩展方式，并与已有的条件边/join/ReID 专题文档形成互补。
 - 更新 `docs/design/architecture/整体架构设计.md` 的“推理与多阶段图”索引，将 `multistage_graph_详细设计.md` 纳入，成为多阶段 Graph 从概要 → 框架详细设计 → 节点级专题文档的导航入口。
- 预期：在新增多阶段节点（例如新的预处理/后处理算子）或调整 Graph 行为时，开发可以直接从该文档了解框架层抽象与运行时行为，减少反复阅读源码的成本。

2025-11-14 23:50

- 任务：整合异步订阅相关设计文档，统一到 LRO 设计说明。
- 操作：
  - 在 `docs/design/subscription_lro/lro_subscription_design.md` 中新增附录 C/D，分别概括早期基于 SubscriptionManager 的异步订阅管线设计与异步订阅补强（排队/限流、取消、WAL、配额与可观测性）要点。
  - 删除 `docs/design/subscription_lro/async_subscription_pipeline.md` 与 `docs/design/subscription_lro/async_subscription_hardening.md` 两个历史文档，在 `docs/design/subscription_lro/README.md` 中将索引统一指向 `lro_subscription_design.md`。
  - 更新 `docs/design/architecture/整体架构设计.md` 与相关 memo 中的引用，避免指向已删除文件，并在 2025-10-21 的异步订阅实施计划记录中改为引用 `lro_subscription_design.md` 的附录 C。
- 预期：订阅/LRO 相关设计形成单一权威入口，读者只需查阅 `lro_subscription_design.md` 即可了解当前实现与历史背景，减少在多份散落文档之间跳转的成本。

2025-11-14 23:59

- 任务：整合 Triton gRPC 与 In‑Process 集成设计文档，以 In‑Process 方案为主。
- 操作：
  - 在 `docs/design/engine_multistage/triton_inprocess_integration.md` 末尾新增附录 A，概括 `provider: triton` 外部 Triton gRPC 集成方案（抽象与类图、调用时序、配置与运行选项、指标与日志、Compose 部署、分阶段推进与风险），并明确其作为 In‑Process 路径的回退与对照方案。
  - 删除 `docs/design/engine_multistage/triton_integration_design.md` 文件，在 `docs/design/engine_multistage/README.md` 与 `docs/design/architecture/整体架构设计.md` 中统一仅索引 `triton_inprocess_integration.md`（说明其已包含 gRPC 附录）。
  - 更新 `docs/design/engine_multistage/zero_copy_execution_详细设计.md`、`docs/plans/triton_wbs.md`、`docs/plans/M1_M2_tasks.md` 以及相关 memo，使所有任务与验收引用指向新的统一文档结构。
- 预期：Triton 相关集成设计形成以 In‑Process 为主的单一入口，gRPC 模式以附录形式保留为历史与回退参考，方便后续在调优零拷贝路径或评审任务拆解时快速找到完整设计背景。

2025-11-15 00:10

- 任务：补充一份控制平面 HTTP API 与 VA/VSM gRPC 接口的集中说明文档。
- 操作：
  - 新增 `docs/design/cp_vsm_protocol/控制平面HTTP与gRPC接口说明.md`，从“对外 HTTP API 总览（订阅/源管理/引擎与管线/训练与模型仓库/观测与调试）”“CP ↔ VA AnalyzerControl gRPC 契约”“CP ↔ VSM SourceControl gRPC 契约”“错误码与返回体约定”等角度，系统梳理 CP 对外 API 与内部 gRPC 接口，并给出 HTTP ↔ gRPC 的典型映射关系。
  - 在 `docs/design/cp_vsm_protocol/README.md` 中将该文档纳入“协议与 API”索引，在 `docs/design/architecture/整体架构设计.md` 的“控制平面与订阅”索引中补充该文档路径，作为控制平面接口层的统一入口。
- 预期：后续前端或第三方调用方在接入控制平面时，可以通过这份接口说明快速了解 HTTP 与 gRPC 合同及错误码语义，而研发在扩展 CP 功能时也可以统一在此维护接口级契约，减少在多份设计/代码之间来回查找的成本。

2025-11-15 00:25

- 任务：为 Web-Front 前端项目补充一份详细设计文档。
- 操作：
  - 新增 `docs/design/architecture/web_front_详细设计.md`，从技术栈与目录结构、路由与布局、Pinia 状态模块（尤其是 `analysis` Store）、CP API 封装（`cp.ts`）、页面级设计（Sources/Pipelines/Observability/Training 等）以及与 Controlplane 的交互模式（HTTP + SSE + WHEP）等方面系统描述 `web-front/` 子项目的详细设计，并将分析面板与视觉规范分别指向已有的专题文档。
  - 在 `docs/design/architecture/整体架构设计.md` 的“媒体与前端集成”索引中纳入 `web_front_详细设计.md`，使 Web-Front 从概要（整体架构）→ 集成设计 → 前端详细设计 → 分析面板专题的文档链路顺畅可追踪。
- 预期：后续在调整前端订阅流程、集成新的 CP API、或扩展 Observability/Training 等页面时，可以以该详细设计为入口，快速理解前端模块边界与数据流，减少直接从代码反推设计的成本。
