# 2025-10-28 21:05 任务日志

- 根据“GPU零拷贝问题修复.md”线路A继续补充：在 YOLO CUDA NMS 后处理路径注入 `ensure_cuda_ready()`，确保工作线程首次进入后处理时已完成 CUDA 运行时初始化，避免 `post.yolo.nms` 节点因未初始化报错。
- 受限于正在运行的 VA，链接阶段因 `bin/VideoAnalyzer.exe` 占用失败（LNK1104）。请先停止 VA 后执行：
  - `tools/kill_running.ps1`
  - `tools/build_va_with_vcvars.cmd`
  - 重启 VA 与 CP，再用 CP 订阅测试。

