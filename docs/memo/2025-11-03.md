2025-11-03 00:20:15 CST

- 任务：阅读并梳理 docs/context/CONTEXT.md 文档要点。
- 摘要：
  - 架构：VA 拉 RTSP，经多阶段图推理并通过 WHEP 输出；CP 提供订阅与 SSE（/api/subscriptions 与 /api/subscriptions/{id}/events）；VSM 管理 RTSP 源；Web 仅通过 CP 交互。
  - 容器配置：模型卷统一映射到 /models，并在图中使用绝对路径；VA 日志到 /logs/video-analyzer-release.log；CP 连接 MySQL（host.docker.internal:13306，root/123456，cv_cp）；CP↔VA 双向 TLS，证书 SAN 需覆盖 localhost/内网 IP。
  - WHEP/ICE：在 app.yaml 中配置 ice.public_ip、端口范围（10000–10100）、禁用 mDNS；容器需映射 UDP 端口，宿主（Windows）防火墙放行；Answer SDP 中应出现 public_ip 的 host 候选。
  - 最小链路：创建订阅 → 订阅 SSE 看到 phase: ready → 前端经 CP 发起 WHEP Offer 并获得 Answer，确认候选可直连。
  - RTSP 拉流：FFmpeg/NVDEC 参数调优（analyzeduration=1s, probesize=5MB），可低延迟；冷启动时可提高 CP 对 VA 的超时或用小模型先验通路。
  - 已知事项：ORT CUDA EP 需匹配 libcudnn，否则回落 CPU；/api/events/recent 暂可 404；订阅超时优先检查 VA 是否已连 RTSP 并解码。
- 后续建议：
  - 校验 docker-compose 端口/卷/证书映射，确认 CP 数据库连通与凭据。
  - 以 camera_01 与 rtsp://host.docker.internal:8554/camera_01 做端到端验证；抓取 VA/CP 日志关键片段。
  - 前端用 DevTools 核对 /api/subscriptions 与 /whep 请求，并确认 Answer SDP 中 host 候选为内网 public_ip。

- 任务：修复前端从“暂停”切回“实时分析”时退化为 CPU 推理的问题，期望保持 GPU 零拷贝推理。
- 变更：
  - video-analyzer/src/composition_root.cpp：
    - 默认在 GPU provider 提示下启用 CUDA 预处理（LetterboxPreprocessorCUDA），除非显式通过 engine.options.use_cuda_preproc 或 VA_USE_CUDA_PREPROC=0 关闭。
    - 在单阶段链路为 ORT Session 默认开启 IoBinding（options.use_io_binding = cfg.use_io_binding || hint_gpu），确保 GPU 张量可直接绑定，维持零拷贝路径。
  - 影响：避免在 UI 从暂停→实时切换后仍走到 CPU 预处理/CPU 入参导致的 CPU 推理；在 GPU Provider（cuda/trt）存在时更稳态地命中 NVDEC→CUDA 预处理→ORT IoBinding 的零拷贝链路。
- 任务：修复容器 ORT CUDA EP 依赖缺失（libcudnn）导致的 GPU 提供者加载失败。
- 变更：
  - docker/va/Dockerfile.gpu：runtime 阶段基础镜像由 `nvidia/cuda:${CUDA_VER}-runtime-ubuntu${UBUNTU_VER}` 改为 `nvidia/cuda:${CUDA_VER}-cudnn-runtime-ubuntu${UBUNTU_VER}`，以包含 `libcudnn.so.*`，消除日志中的 `libcudnn.so.9: cannot open shared object file` 错误。
- 重建与验证：
  - 构建：`docker compose -f docker/compose/docker-compose.yml -f docker/compose/docker-compose.gpu.override.yml build va`
  - 启动：`docker compose -f docker/compose/docker-compose.yml -f docker/compose/docker-compose.gpu.override.yml up -d va`
  - 验证：`docker logs va | grep -i "provider|iobind|dev_bind"` 应显示 `provider=cuda` 且 `io_bind/dev_bind=true`；Prom 指标页 `va_d2d_nv12_frames_total` 递增。
- 验证建议：
  - 启动 VA/VSM，使用 camera_01；观察 VA 日志中的 RuntimeSummary 与 ort.run 绑定日志，确认 provider=cuda 且 io_bind/dev_bind 为 true。
  - 切换 /api/control/pipeline_mode 为 analysis_enabled=false 再切回 true，确认切回后依然保持 NV12 设备帧与 CUDA 预处理路径（可在 metrics 或日志中看到 overlay_nv12_passthrough/zc 计数增长）。
  - 如需禁用 CUDA 预处理，可通过 engine.options.use_cuda_preproc: false 或设置 VA_USE_CUDA_PREPROC=0。
