# 2025-12-03

2025-12-03 00:39:16 agent-mysql-checkpoint
- 实现 agent MySQL checkpoint 后端（agent_checkpoints 表 + MySQLSaver），并接入 get_checkpointer。
- 更新 db/schema.sql 与 docker-compose agent 环境变量说明，支持多副本共享线程状态。

2025-12-03 00:51:40 agent-mysql-checkpoint-align
- 对齐 third-party/langgraph 的 BaseCheckpointSaver 接口，重写 agent MySQLSaver 子类并保留 agent_checkpoints JSON 结构。
- 使用 serde(JsonPlusSerializer) 编码 Checkpoint/Metadata/pending_writes，简化为每线程+命名空间最新一条记录。

2025-12-03 19:45:57 agent-metrics-and-mysql-docs
- 为 agent 控制与工具统计接入可选 Prometheus 指标（/v1/agent/stats 仍保持兼容）。
- 在 FastAPI 中增加 /metrics 端点（依赖 prometheus_client），记录 HTTP 请求计数与耗时。
- 更新 agent-checkpoint-mysql 设计文档，描述当前 MySQLSaver 实现与 docker compose 多实例用法。
2025-12-03 20:55:14 调整 web-front Agent 页面布局：使用 viewport 高度计算消息展示区域高度，保证输入区始终可见并在消息区域内部滚动。
2025-12-03 21:03:43 进一步对齐 third-party/openAgent 布局：移除 Agent 页面消息区域 JS 高度计算，改为纯 flex 布局（messages-container flex:1），确保消息区内部滚动、输入区固定可见。
2025-12-03 进一步调整 App 根布局：html/body/#app 高度设为 100%，禁止整页滚动，.shell 改为随根容器高度伸展，配合 Agent 页面 flex 布局避免输入区被挤没。
2025-12-03 为 Agent 页面接入 Markdown 渲染：引入 markdown-it 并使用 v-html 渲染消息与步骤内容，修复恢复消息以纯 MD 源码形式展示的问题。
2025-12-03 修复 Docker web 镜像构建：docker/web/Dockerfile 中 npm ci 改为有锁文件时 npm ci 否则 npm install，避免缺少 package-lock.json 导致构建失败。
2025-12-03 调整 Agent 消息 Markdown 样式：去掉 bubble/步骤内容的 pre-wrap，并收紧 p/列表的 margin，减少渲染后多余空行感受。
2025-12-03 调整 _build_agent_data_from_state：即使没有 tool_calls，assistant 消息也先插入一条 type=thinking 的步骤，再插入 response，用于前端展示 LLM 思考过程。
2025-12-03 回滚 _build_agent_data_from_state 的人为 thinking 插入逻辑，恢复为仅在存在 tool_calls 时生成 type=thinking，避免伪造 LLM 思考步骤。
