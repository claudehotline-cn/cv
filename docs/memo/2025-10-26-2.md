2025-10-26 04:57:45

任务：继续实现 /api/control（apply_pipelines、hotswap、status、drain），并修正构建时进程占用导致的热替换问题。

执行情况：
- gRPC 封装：`controlplane/include/controlplane/grpc_clients.hpp:1`、`controlplane/src/server/grpc_clients.cpp:1`
  - 新增 `va_apply_pipelines(...)`、`va_hotswap_model(...)`、`va_get_status(...)`、`va_drain(...)`；定义批量 `ApplyItem`。
- 路由接入：`controlplane/src/server/main.cpp:489` 起新增 4 个路由；统一 startswith 判断兼容查询串；补充 `/api/control` 404 回显（便于定位 path）。
- 构建与运行：
  - 先 Stop-Process（controlplane/controlplain）再构建，避免链接无法覆盖；产物时间戳：`controlplane/build/bin/controlplane.exe: 2025-10-26 04:57:33`。
  - 验证：
    - GET `/api/control/status?pipeline_name=p1` → 502（VA 未启动）；
    - POST `/api/control/hotswap` `{}` → 400；
    - POST `/api/control/apply_pipelines` 有效 items → 502；
    - POST `/api/control/drain` `{}` → 400；
    - `/metrics` 出现对应路由计数，符合预期。

后续：
- VA/VSM 启动后补做成功路径（202/200）与 P95 指标采样；
- 继续实现 `/api/control/hotswap/status/drain` 边界校验与错误映射细化；
- 视需要移除 `/api/control` 404 回显或收敛到统一错误格式。

