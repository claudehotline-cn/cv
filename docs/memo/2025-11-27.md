2025-11-27 21:54:50

- 任务：排查 `check_cp_sources_attach_detach.py` 测试中 CP 与 VSM/VA 链路相关的问题，使在 VA/VSM 正常运行时脚本不再因 VSM gRPC 超时而报告 “backend not available (VSM)”。
- 结论：
  - 在 `controlplane-spring` 中为 VSM gRPC 客户端引入可配置超时：在 `AppProperties.VsmProperties` 新增 `timeoutMs` 字段，并在 `controlplane-spring/src/main/resources/application.yml` 中配置 `cp.vsm.timeout-ms: 1000`，同时在 `VideoSourceManagerClient` 中从该配置读取超时（默认回落为 500ms），替代原先硬编码的 500ms deadline，从而在 VSM 正常但响应略慢时降低 `DEADLINE_EXCEEDED` 导致的 500/503。
  - 修复 `SourceServiceImpl.list()` 中的字段映射错误：将 `SourceItem.sourceUri` 从原先误用 `attachId` 改为使用 VSM `GetHealthReply.StreamState.source_uri` 字段，保证 `/api/sources` 与 SSE 相关接口返回的源 URI 与实际 RTSP 源一致，有利于前端与后续调试 VA/VSM 之间的真实连接问题。
  - 更新 `docs/context/CONTEXT.md` 中关于 `VideoSourceManagerClient` 的说明，明确 unary 调用的 deadline 由 `cp.vsm.timeout-ms` 控制（默认 500ms，可按环境调大），以便后续在不同部署环境中通过配置平衡失败快速返回与稳定性的需求。
  - 当前容器环境缺少 Maven 命令，无法直接在本地完成 `controlplane-spring` 的构建与脚本回归测试；后续需在安装了 Maven 且具备 VA/VSM/MySQL 的环境中运行 `mvn -DskipTests package` 并按之前流程通过 Docker 或直接启动 cp-spring，再配合 `CP_BASE_URL` 与 `check_cp_sources_attach_detach.py` 进行端到端验证。

2025-11-30 20:23:03

- 任务：根据 `docs/plans/controlplane_spring_tasks.md` 执行尚未完全落地的部分任务，主要包括 `/api/orch/*` 编排接口、SSE 占位行为、基础安全与指标集成、最小测试以及灰度/回滚文档细化。
- 结论：
  - 在 `controlplane-spring` 中扩展 `OrchestratorController`，新增 `POST /api/orch/attach_apply` 与 `POST /api/orch/detach_remove`，复用 `SourceService` 与 `ControlService`：`attach_apply` 支持从 query 与 JSON 混合解析 `attach_id/source_uri/pipeline_name` 和 `spec` 字段，先通过 VSM Attach 建立源绑定，再在 VA 上按需调用 `applyPipeline`；`detach_remove` 则根据 `attach_id/pipeline_name` 做 best-effort detach/remove，行为与 C++ 版保持一致的最小实现。
  - 调整 `SseController`：将 `GET /api/subscriptions/{id}/events` 从临时返回 200 的占位 SSE 改为标准 `501 NOT_IMPLEMENTED` 响应（`{"code":"NOT_IMPLEMENTED","msg":"subscription events SSE not implemented"}`），对齐 `check_cp_sse_placeholder.py` 的预期；同时保留 `/api/sources/watch_sse` 通过 `VideoSourceManagerClient.watchState()` 桥接 VSM `WatchState` 流并输出 `event: state` 事件，满足 `check_cp_sources_watch_sse.py` 的要求。
  - 引入基础 Spring Security：在 `controlplane-spring/pom.xml` 增加 `spring-boot-starter-security`，并新增 `SecurityConfig`，禁用 CSRF、允许所有请求 `permitAll()`，暂不强制鉴权，仅为后续基于 `cp.security.*` 收紧策略预留扩展点，保证现有 Python 脚本与前端行为不受影响。
  - 接入 Micrometer 指标：新增 `MetricsConfig` 为 `MeterRegistry` 添加 `app=controlplane-spring` 的 common tag；在 `VideoAnalyzerClient` 与 `VideoSourceManagerClient` 中为关键 gRPC 调用封装 `cp.grpc.client` 级 Timer/Counter（如 `svc=va/vsm`、`method=Attach/Detach/ApplyPipeline/ListPipelines/GetHealth/WatchState` 等），为后续在 Prometheus/Grafana 上观测下游调用时延与错误提供基础指标。
  - 为 cp-spring 添加最小 Web 层测试：在 `controlplane-spring/src/test/java/com/cv/cp/controller` 下新增 `SystemInfoControllerTest` 与 `SourceControllerTest`，使用 `@WebMvcTest` + `MockMvc` 验证 `/api/system/info` 返回 `{"code":"OK","data":{"restream":...}}` 结构，以及 `/api/sources:attach` 在参数合法时返回 202 并调用 `SourceService.attach(...)`，作为 Phase G-TG1 的起步。
  - 在 `docs/plans/controlplane_spring_wbs.md` 中细化第 7 章“测试、灰度发布与回滚策略”：补充 7.3 灰度发布方案（端口/域名规划、前端与 Agent 的 `CP_BASE_URL/AGENT_CP_BASE_URL` 开关、分阶段放量策略）以及 7.4 回滚预案（配置级/流量级回滚、数据兼容性检查、灰度演练与 SOP 记录），对应 Phase G 的 TG3/TG4 要求。

