2025-10-26 04:43:05

任务：读取并纳入 docs/context/CONTEXT.md 与 docs/context/ROADMAP.md 到当前对话上下文。

执行情况：
- 已读取两份文档并提取关键结论加入上下文内存，用于后续讨论/实现决策。
- 主要要点：命名与目录标准化（controlplain→controlplane，VA 内嵌目录至 video-analyzer/src/controlplane）、统一 vcpkg 构建（禁止 Anaconda）、CP 已实现接口（/api/system/info、/api/subscriptions 基线、/api/sources 初步、SSE 演示）、待办（/api/subscriptions/:id/events 真正 SSE 桥接 VA Watch、/api/control/* → VA AnalyzerControl、主流程重构与安全治理）。
- 路线图：M0/M1/M2 目标与验收、分阶段表格、依赖矩阵、Top-5 风险清单与缓解策略已成文，可作为执行依据。

备注：Windows 控制台存在中文编码显示异常，已通过字节级读取确认文档为 UTF-8 并完成要点提取，不影响后续工作。

2025-10-26 04:48:52

任务：实现 M0 最小控制接口（/api/control/apply_pipeline 与 DELETE /api/control/pipeline），并完成构建与最小测试。

执行情况：
- 新增 gRPC 客户端封装：`controlplane/include/controlplane/grpc_clients.hpp:1`、`controlplane/src/server/grpc_clients.cpp:1`
  - `va_apply_pipeline(...)`、`va_remove_pipeline(...)` 对应 VA AnalyzerControl 的 Apply/Remove。
- 路由接入：`controlplane/src/server/main.cpp:1`
  - POST `/api/control/apply_pipeline`：支持 JSON（pipeline_name、spec.yaml_path/graph_id/serialized+format）与 Query 回退（部分字段）。
  - DELETE `/api/control/pipeline`：支持 `?pipeline_name=` 或 JSON。返回 202/400/502，并计入 metrics。
- 构建：`tools/build_controlplane_with_vcvars.cmd` 通过；二进制产物 `controlplane/build/bin/controlplane.exe` 生成成功。
- 运行与验证：
  - 先停止历史 `controlplain.exe`（占用 8080），启动新版 controlplane。
  - 验证 `/api/system/info` 200 PASS。
  - 验证 apply 缺参返回 400；apply 含有效参数在 VA 未启动时返回 502；delete 缺参 400、含参 502（预期）。

后续：
- 扩展 /api/control/* 其余接口（apply_pipelines/hotswap/status/drain）。
- 增加最小脚本测试到 `controlplane/test/scripts/`（校验 400/502 与 metrics 增量）。
