2025-11-20 10:00:00

- 任务：为 OCSORT GPU 追踪路径增加显存分配诊断日志，便于在开启 `track.ocsort` GPU 分支时精确定位 30GiB 显存占用的来源。  
- 操作：  
  - 修改 `video-analyzer/src/analyzer/multistage/node_track_ocsort.cpp` 中的 `NodeTrackOcsort::process_gpu`：  
    - 在成功获取 `p.gpu_rois[in_rois_key_]` 后，增加一次节流日志，输出当前帧 GPU 检测框数量：`gpu_path_enter count=<gdet.count>`，用于确认多路场景下每帧参与 GPU 追踪的 ROI 规模。  
    - 在首次调用 `ocsort_alloc_state` 之前，基于当前 `max_tracks_` 与 ReID 特征维度 `feat_dim` 计算 OCSORT GPU 状态的预估显存占用 `approx_bytes`，并通过 `ms.track` 日志输出：  
      - `max_tracks`、`feat_dim`；  
      - `approx_bytes`（转换为 MiB）；  
      - 当前 CUDA 设备的 `cuda_free_before` 与 `cuda_total`（同样以 MiB 输出，使用 `cudaMemGetInfo` 获取），方便在实际环境中对比“计划分配量”与“nvidia-smi 实际变化量”。  
    - 仅在 `gpu_state_ready_ == false` 的第一次状态分配时打印该日志，避免对长期运行的链路造成日志刷屏；同时使用 `VA_LOG_THROTTLED` 保持与现有 `ms.track` 日志行为一致。  
  - 在当前沙箱环境下重新执行 `cmake -S . -B build && cmake --build build -j` 验证语法与依赖，CMake 仍然因缺少 OpenCV 开发包（`OpenCVConfig.cmake/opencv-config.cmake` 未找到）而失败，确认是环境限制，与本次改动无关。  
- 结果：在 GPU 环境中打开 `use_gpu: "1"` 并启用 OCSORT GPU 分支后，`ms.track` 日志会额外输出一次 `gpu_state_alloc_plan ... approx_bytes=...MiB cuda_free_before=...MiB`，用于精确判断：  
  - 单条管线的 OCSORT GPU 状态理论上要占多少显存；  
  - 在启用 GPU 追踪前后，CUDA 设备的可用显存变化是否与 `approx_bytes` 级别一致；  
  - 从而快速区分“显存多消耗在 OCSORT 状态本身”与“切换 GPU 追踪导致其他模块/模型使用了更多显存”两类场景，为进一步缩小 30GiB 显存问题根因提供定量依据。  

2025-11-21 01:31:43

- 任务：OCSORT 追踪与 ReID Triton 路径稳定性加固，并按 IOBinding 分支要求提交代码。  
- 操作：  
  - 将 `NodeTrackOcsort` 明确固定为 CPU 路径执行 Kalman 与多阶段匈牙利匹配，仅保留 CPU 结果到 GPU 的 ROI 视图构造，用于 `overlay.cuda`，彻底剥离 OCSORT GPU 多阶段匈牙利以避免 30GiB 显存和 GPU 利用率 100% 问题。  
  - 清理并改造 `track_ocsort_kernels`：保留 GPU 状态结构与单线程内核实现，使其不再在当前图中启用，同时控制局部内存与显存占用，为后续需要时提供可选 GPU 实验路径。  
  - 调整 `TritonInprocModelSession`：  
    - 引入 `run_impl` 内部实现，统一处理 In-Process 调用逻辑；  
    - 在 GPU→CPU staging 时先通过 `cudaPointerGetAttributes` 检查指针属性并切换到正确 device，再执行 `cudaMemcpy`，避免因 device 不一致导致 `cudaMemcpy D2H failed: invalid argument`；  
    - 去掉出错后自动“GPU 输入→CPU staging 重试”的递归逻辑，防止在已经 illegal access 的上下文里继续触发二次错误；  
    - 保持 YOLO/ReID 主路径仍由 Triton In-Process 执行，输出端继续支持 GPU 输出缓冲复用。  
  - 更新 `docker/config/va/graphs/analyzer_multistage_ocsort.yaml`：  
    - 为 `track.ocsort` 保留 `use_gpu` 配置项但当前实现只在 CPU 中执行 Deep OC-SORT，多阶段匈牙利和 Kalman 明确不进入 GPU；  
    - 调整 ReID 节点注释，避免误导使用者依赖 GPU 输入路径，强调实际由 Triton In-Process 配置决定。  
  - 在本地环境尝试使用 CMake/Ninja 构建 VideoAnalyzer，确认因缺失 OpenCV/ONNXRuntime 开发环境导致构建失败，此问题属于当前开发容器的环境限制，与本次改动逻辑无关。  
  - 使用 `git add -A` 全量纳入本次修改，并在 IOBinding 分支上准备提交。  
- 结果：  
  - 多目标追踪部分现已完全依赖 CPU Deep OC-SORT，多阶段匈牙利与 Kalman 全部在 CPU 上执行，显著降低了 GPU 显存占用和不必要的 GPU 内核开销，同时保持 overlay CUDA 叠加链路不变。  
  - Triton In-Process 封装在面对 ReID/YOLO 路径的 CUDA 拷贝错误时行为更可控，避免重复 D2H 回退导致的 `invalid argument`，为后续在 Triton 模型仓库或 TensorRT 侧进一步排查 ReID 内部 illegal access 留出了清晰边界。  
  - IOBinding 分支上的改动已经对 OCSORT、Triton In-Process 和多阶段图配置进行集中收敛，便于后续在统一分支上继续迭代 GPU 零拷贝和稳定性优化。  
