2025-11-17 任务日志

- 时间：2025-11-17
- 内容：为降低 In-Process Triton + TensorRT backend 在 yolov12x 上动态输出名路径触发崩溃的风险，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中为检测节点 det 增加了 per-node 固定输出名配置 `triton_outputs: "output0"`。结合之前在 NodeModel 与 ModelSessionFactory 中新增的 `triton_outputs` 覆盖与强制要求逻辑，该修改使得 det 节点在 provider=triton 且使用 In-Process 模式时会显式请求 Triton 模型的 `output0` 输出，避免走“未指定输出名 → Triton 自动决定输出集合”的不稳定路径；而其他未配置 `triton_outputs` 的节点在请求 provider=triton 时会被工厂自动回退至 cuda/ORT 路径，从而只让经过验证的 det+yolov12x 组合继续使用 In-Process Triton。 

- 时间：2025-11-17
- 内容：基于相同的 per-node 固定输出名策略，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中调整 ReID 节点 reid：去掉原先的 force_provider: cuda 覆盖，使其继承 engine 级 provider=triton 设置；同时补充 `model_path_triton: "reid_passvitb"` 并新增 `triton_outputs: "feat"`，明确指定 In-Process Triton 仓库模型名与输出张量名。这样，在当前 In-Process Triton 配置下，det（yolov12x）与 reid（reid_passvitb）两个模型节点都会使用各自固定的 Triton 输出名（output0 / feat），避免走动态输出集合路径；若后续某节点未配置 triton_outputs，则仍按工厂逻辑自动回退到 cuda/ORT 路径。 

- 时间：2025-11-17
- 内容：在完善 per-node Triton 配置能力的基础上，为了彻底消除输入名不一致导致的潜在问题，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中为 det 与 reid 节点分别增加了 `triton_input` 覆盖：det 节点显式指定 `triton_input: "images"`（与 yolov12x 在 Triton config.pbtxt 中的输入名一致），reid 节点指定 `triton_input: "input"`（与 PASS-ReID ONNX/Triton 配置中的输入名一致）。配合前面在 NodeModel 和 ModelSessionFactory 中新增的 per-node `triton_input` 注入逻辑，这两处配置会在创建会话前写入 EngineDescriptor::options["triton_input"]，从而让 In-Process Triton 跳过 metadata 推断输入名的流程，直接使用 graph 中声明的 per-node 输入名，避免再次出现 `unexpected inference input 'images' for model 'reid_passvitb'` 一类错误，并减少对 ModelMetadata 的依赖。 

- 时间：2025-11-17
- 内容：根据当天对话中对 In-Process Triton + OCSORT 多阶段图调试的总结，重新生成了 docs/context/CONTEXT.md，将当前实际实现状态、关键配置（per-node triton_input/triton_outputs、analyzer_multistage_ocsort.yaml 中 det/reid 节点配置、provider 回退策略）、gdb 崩溃栈关键信息以及尚未完成的待办事项集中到单一文件，方便后续 LLM/开发者接力调试；同时基于新的 CONTEXT 输出了 docs/context/ROADMAP.md，对应 M0（In-Process Triton 稳定基线）、M1（OCSORT 多阶段图 + MinIO 仓库打通）、M2（性能与可观测性收敛）三个里程碑，明确了每一阶段的关键交付物、技术要点、风险/缓解策略与指标门槛，为后续演进提供统一路线图。 

- 时间：2025-11-17
- 内容：仔细阅读并加载 docs/context/CONTEXT.md 与 docs/context/ROADMAP.md，将其中关于 In-Process Triton 集成现状、NodeModel/ModelSessionFactory 配置约束、多阶段 OCSORT 图配置细节以及 M0-M2 里程碑路线图的内容纳入当前对话上下文，为后续实现与调试提供统一背景信息。 

- 时间：2025-11-17
- 内容：为彻底消除 In-Process Triton 路径在 provider 解析后仍意外尝试本地 ONNX 的可能，在 video-analyzer/src/analyzer/multistage/node_model.cpp 中扩展了 pick_path_for() 的 provider 判断逻辑：除了原先的 "triton" 以外，将 "triton-inproc"、"triton_grpc" 等变体也统一映射为占位路径 "__triton__"，使得所有 Triton 相关 provider 在 NodeModel::open 中都不再依赖 model_path/model_path_ort，避免在 MinIO 仓库场景下出现 `models/yolov12x.onnx` File doesn't exist 的误访问；同时在 docker/config/va/app.yaml 的 engine.options 中显式填入 triton_input=images 与 triton_outputs=output0，作为 det_720p 场景的全局默认，确保在未配置 per-node 覆盖时 In-Process 会话也能稳定地以固定输入/输出名访问 yolov12x Triton 模型。 

- 时间：2025-11-17
- 内容：针对当前 In-Process Triton 路径在 ServerInferAsync 后仍可能触发 Segmentation fault 的问题，优先恢复到历史上经过验证的“自定义响应分配器 + GPU 零拷贝”实现思路：在 video-analyzer/src/analyzer/triton_inproc_session.cpp 的 TritonInprocModelSession::run() 中重新引入自定义 TRITONSERVER_ResponseAllocator，按 output_names 将各输出映射到会话持久化的 GPU 缓冲（out_dev_bufs_）或一次性 CPU 缓冲，并通过 InferenceRequestSetResponseCallback 将 allocator 与 promise 绑定；解析响应时，当 mtype==TRITONSERVER_MEMORY_GPU 且设备号匹配且 use_gpu_output=true 时直接将 base 指针封装为 on_gpu TensorView，避免默认 allocator + 额外 D2D 拷贝的组合，仅在需要 CPU 视图或 allocator 回退到 CPU/GPU host 时才进行 D2H 拷贝。保留了现有的 response error 与 OutputCount 显式检查以及首次推理自动发现输出名（autofill output_names）逻辑。出于让 per-node 配置完全接管 Triton IO 名的考虑，将 docker/config/va/app.yaml 中 triton_input/triton_outputs 恢复为注释，仅在图中显式配置的节点上启用 In-Process Triton 输出名约束。受限于本地环境缺少 OpenCVConfig.cmake，video-analyzer 子项目的 CMake 配置在本机构建阶段未能完成，后续需在具备完整依赖的环境中重新验证编译与运行。 

- 时间：2025-11-17
- 内容：在上述恢复自定义 ResponseAllocator 的基础上，进一步根据当前配置约定（所有使用 In-Process Triton 的节点均在 engine.options 或 graph params 中显式提供 triton_input/triton_outputs）关闭 In-Process 路径的“动态发现”逻辑：在 video-analyzer/src/analyzer/triton_inproc_session.cpp 的 loadModel() 中移除了通过 TRITONSERVER_ServerModelMetadata/MessageSerializeToJson 自动探测 input_name 的代码分支，仅保留对 opt_.input_name 是否为空的校验，若为空则视为配置错误直接返回 false，并提示需显式配置 triton_input；在 run() 中去掉了基于 opt_.output_names 为空时自动收集输出名并写回的 discovered_names 逻辑，改为在 OutputCount 阶段强校验 output_names 非空，若未配置则删除响应并返回错误，要求使用方通过 engine.options 或 per-node triton_outputs 明确指定输出名。这样，In-Process Triton 行为完全由配置驱动，避免了动态 metadata/输出名推断带来的不确定性，更贴合当前 det/reid 节点在图中显式声明 IO 名的使用方式。 

- 时间：2025-11-17
- 内容：为避免 OCSORT 多阶段图在没有 ROI（例如当前帧检测结果为空）时因为 ReID 模型节点缺少输入 tensor 而导致整图失败，在 video-analyzer/src/analyzer/multistage/node_model.cpp 中放宽了 NodeModel::process 的输入检查逻辑：当在 Packet 中找不到 in_key_ 对应的输入（例如 tensor:roi_batch 尚未被 roi.batch.cuda 产出）时，不再返回 false 触发 composition 的 “Graph node failed” 日志，而是将本次调用视为 no-op 直接返回 true，让后续节点继续运行；仅在 session_->run() 返回失败或模型产生零输出时才累计 infer_fail_count_ 并返回 false。这样，像 ReID 这种“基于检测结果的可选分支”在没有 ROI 的帧上会被优雅跳过，不会把整个分析管线打断，同时保持 det 等基础检测模型在真正异常时仍然会通过返回 false 让上层感知。 

- 时间：2025-11-17
- 内容：在前一条调整基础上进一步收窄影响范围，只对“确实以 ROI batch 为输入的模型节点”放宽输入检查逻辑：在 video-analyzer/src/analyzer/multistage/node_model.cpp 的 NodeModel::process 中，仅当 in_key_ 精确为 "tensor:roi_batch" 且在当前 Packet 中不存在该 tensor 时，将本节点视为 no-op 并返回 true；其他类型的模型（如 det 节点使用的 tensor:det_input）仍保持原有严格行为，在缺少输入时返回 false 以便上层感知错误。这样可以确保 OCSORT 图中的 ReID/ROI 分类一类“基于检测结果的可选分支”在没有检测框时被视为“空集合”而非错误，同时不会影响基础检测模型和其他必须有输入的节点的错误检测能力。 

- 时间：2025-11-17
- 内容：根据“ReID 模型在 Triton 配置中 max_batch_size=1，但希望在多目标场景下仍然使用 ReID 特征参与 OCSORT 匹配”的需求，在 video-analyzer/src/analyzer/multistage/node_model.{hpp,cpp} 中为 NodeModel 增加了 ROI 顺序推理（roi_seq_batch）能力：当 graph 中为某个模型节点配置 roi_seq_batch: \"1\" 且其输入为 tensor:roi_batch 时，NodeModel::process 会将 [N,3,H,W] 的 ROI batch 拆分为 N 个单样本 [1,3,H,W] 依次调用 session_->run，每次获得单个行人特征；若会话输出位于 GPU 且 NodeContext 提供 gpu_pool，则在 GPU 上通过 cudaMemcpyAsync 将每个 ROI 的特征拷贝到一块连续缓冲区，聚合为 [N,D] 的 GPU TensorView（tv.on_gpu=true），供后续 GPU OCSORT 路径直接消费；否则退回到原有的 CPU 聚合逻辑，将特征累加到 roi_seq_feat_buf_ 并输出 CPU [N,D] tensor。对应 OCSORT 图中的 ReID 节点，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中为 reid 增加了 roi_seq_batch: \"1\" 和 triton_gpu_output: \"1\"，使其在保持 batch size=1 Triton 配置的前提下，通过顺序推理 + GPU 聚合方式为多 ROI 场景提供零拷贝 GPU 特征。 
