2025-11-17 任务日志

- 时间：2025-11-17
- 内容：为降低 In-Process Triton + TensorRT backend 在 yolov12x 上动态输出名路径触发崩溃的风险，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中为检测节点 det 增加了 per-node 固定输出名配置 `triton_outputs: "output0"`。结合之前在 NodeModel 与 ModelSessionFactory 中新增的 `triton_outputs` 覆盖与强制要求逻辑，该修改使得 det 节点在 provider=triton 且使用 In-Process 模式时会显式请求 Triton 模型的 `output0` 输出，避免走“未指定输出名 → Triton 自动决定输出集合”的不稳定路径；而其他未配置 `triton_outputs` 的节点在请求 provider=triton 时会被工厂自动回退至 cuda/ORT 路径，从而只让经过验证的 det+yolov12x 组合继续使用 In-Process Triton。 

- 时间：2025-11-17
- 内容：基于相同的 per-node 固定输出名策略，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中调整 ReID 节点 reid：去掉原先的 force_provider: cuda 覆盖，使其继承 engine 级 provider=triton 设置；同时补充 `model_path_triton: "reid_passvitb"` 并新增 `triton_outputs: "feat"`，明确指定 In-Process Triton 仓库模型名与输出张量名。这样，在当前 In-Process Triton 配置下，det（yolov12x）与 reid（reid_passvitb）两个模型节点都会使用各自固定的 Triton 输出名（output0 / feat），避免走动态输出集合路径；若后续某节点未配置 triton_outputs，则仍按工厂逻辑自动回退到 cuda/ORT 路径。 

- 时间：2025-11-17
- 内容：在完善 per-node Triton 配置能力的基础上，为了彻底消除输入名不一致导致的潜在问题，在 docker/config/va/graphs/analyzer_multistage_ocsort.yaml 中为 det 与 reid 节点分别增加了 `triton_input` 覆盖：det 节点显式指定 `triton_input: "images"`（与 yolov12x 在 Triton config.pbtxt 中的输入名一致），reid 节点指定 `triton_input: "input"`（与 PASS-ReID ONNX/Triton 配置中的输入名一致）。配合前面在 NodeModel 和 ModelSessionFactory 中新增的 per-node `triton_input` 注入逻辑，这两处配置会在创建会话前写入 EngineDescriptor::options["triton_input"]，从而让 In-Process Triton 跳过 metadata 推断输入名的流程，直接使用 graph 中声明的 per-node 输入名，避免再次出现 `unexpected inference input 'images' for model 'reid_passvitb'` 一类错误，并减少对 ModelMetadata 的依赖。 
