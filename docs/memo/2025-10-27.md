2025-10-27 01:21:56 读取项目文档并纳入上下文

- 任务：阅读 docs/context/CONTEXT.md 与 docs/context/ROADMAP.md，将内容要点纳入当前对话上下文。
- 动作：使用 PowerShell 检索并读取文档；因控制台编码限制导致中文显示乱码，改为以结构化摘要形式记录关键结论、命令与路径。
- 产出：已在对话中提供两份文档的结构化摘要（架构通信关系、TLS/mTLS 与脚本、编排流程与指标/告警、VA REST 迁移策略、前端预览与 WHEP、SSE Soak 稳定性与 CI、运行方式、已知问题与 ROADMAP 各阶段目标/验收标准、依赖与 Top 风险）。
- 后续：如需在仓库中统一编码为 UTF-8 并保留原文全文展示，可在不改动内容的前提下新增 *.utf8.md 影子文件或提交编码修复；如需，请告知我执行。

2025-10-27 01:30:00 前端联调与播放验证（Chrome DevTools MCP）

- 任务：从前端访问分析页，验证视频是否可正常播放，采集最小充分证据。
- 环境：已检测到以下监听端口与进程——CP: 18080(pid=35396)、VA: 8082(pid=46680)、VSM: 7070(pid=45776)、前端: 5173(pid=9944)。
- 动作：
  - 打开 http://127.0.0.1:5173/#/analysis。
  - evaluate 提取 <video> 状态（readyState/paused/dims/srcObject）。
  - network 列表聚焦 /api 与 /whep；console 提取错误；保存截图 logs/webtest/analysis-page.png。
- 结果：
  - 视频元素存在，但未播放：readyState=0, paused=true, videoWidth=0, srcObject=false。
  - 网络：/api/system/info 200；/api/sources 200；/api/models|/api/pipelines|/api/graphs 404；POST /api/subscriptions?stream=camera_01&profile=det_720p 多次 503/504。
  - 控制台：多条 404/503/504 报错；一次 Vue warn（对 currentSource 只读属性写入）。
  - store 状态：currentSourceId="camera_01"，currentPipeline="det_720p"，whepUrl=""，analyzing=false。
  - 截图：logs/webtest/analysis-page.png。
- 结论：分析页当前无法播放。根因为 CP 订阅创建失败（503/504），未获得 WHEP URL（whepUrl 为空），因此前端播放器无流可播。
- 建议：
  1) 先跑 tools/run_cp_smoke.ps1 确认 CP→VA/VSM 编排链路（含 mTLS）通畅；
  2) 检查 controlplane/config/app.yaml 里 gRPC/TLS 证书与 VA/VSM 地址；
  3) 确认 VA pipeline graph/模型已就绪（VA /metrics 有 pipeline 与 frames 进展）；
  4) 若为 dev 模式，临时放宽 CP 的 /api/models|/pipelines|/graphs 404 对订阅流程的影响，或在前端开启 mocks 以避免拦截；
  5) 复测：创建订阅成功后应见 /whep 201 且前端 video readyState≥2。

2025-10-27 02:05:00 后端 TLS 默认化改造（去环境变量）

- 任务：后端默认开启 TLS，改为读取配置文件；补齐证书；消除 CP→VA 50051 TLS 握手错误。
- 改动：
  - VA：新增 control_plane.tls 配置（enabled/root_cert_file/server_cert_file/server_key_file/require_client_cert）；ConfigLoader 解析；gRPC Server 改为读取 app 配置（不再依赖 VA_TLS_*）。
  - VSM：默认启用 TLS（无需环境变量）；仍保留 VSM_TLS_* 以兼容覆盖；证书默认读取 controlplane/config/certs 下文件。
  - 配置：更新 video-analyzer/config/app.yaml 增加 tls 节；新增脚本 tools/gen_dev_certs.ps1 生成自签证书（va_server/vsm_server/cp_client）。
  - 说明：新增 video-source-manager/config/README.md 说明默认行为与未来 YAML 支持。
- 后续：
  - 建议统一以仓库根为工作目录启动三进程，避免相对路径解析偏差；
  - 如需 mTLS 客户端校验，确保 CA/客户端证书匹配；
  - 运行 tools/gen_dev_certs.ps1 可快速生成开发证书。

2025-10-27 02:25:10 重启后端三进程（VA/VSM/CP）

- 动作：停止旧进程；以仓库根为工作目录重启 VideoAnalyzer、VideoSourceManager、controlplane；使用配置文件（默认 TLS）。
- 结果：
  - 监听：50051(VA gRPC)、8082(VA REST/WHEP)、7070(VSM gRPC)、18080(CP HTTP) 全部就绪。
  - 健康：/api/system/info=200；/metrics=200。
