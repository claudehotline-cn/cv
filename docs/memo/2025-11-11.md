2025-11-11 21:51:56

- 阅读 docs/context/CONTEXT.md 与 docs/context/ROADMAP.md。
- 将两份文档的完整内容纳入当前对话上下文，便于后续讨论与实现对齐。
- 未修改代码；仅添加备忘记录。

22:05

- 前端模型页新增“添加模型”能力：
  - web-front/src/views/Models.vue 增加对话框，支持填写模型ID、选择平台（onnxruntime_onnx / tensorrt_plan / 自定义）、编辑 config.pbtxt、可选“创建后立即加载”。
  - web-front/src/api/cp.ts 新增 cp.repoAddModel() 封装。
- 控制平面新增接口：
  - controlplane/src/server/main.cpp 新增 POST /api/repo/add，调用 VA 的 RepoSaveConfig 创建 <repo>/<model>/config.pbtxt，支持可选立即 Load（最佳努力）。
- 说明：当前为最小可用实现，负责创建模型目录与 config.pbtxt；模型权重文件需通过外部方式放入仓库（FS 或 S3），随后可在模型页执行 Load。后续可扩展二进制上传至 VA（gRPC）以便完成一站式导入。

22:35

- 新增上传权重闭环：
  - Proto：video-analyzer/proto/analyzer_control.proto 新增 RepoPutFile RPC 与消息。
  - VA：video-analyzer/src/controlplane/api/grpc_server.cpp 实现 RepoPutFile，支持 FS 与 S3（SigV4 curl）路径，写入 <repo>/<model>/<version>/<filename>。
  - CP gRPC 客户端：controlplane/include/.../grpc_clients.hpp 与 controlplane/src/server/grpc_clients.cpp 新增 va_repo_put_file。
  - CP HTTP：controlplane/src/server/main.cpp 新增 POST /api/repo/upload。
    - 方式一：application/octet-stream + query: model, version=1, filename。
    - 方式二：application/json，字段：model, version?, filename, content_b64。
  - 前端：web-front/src/api/cp.ts 新增 repoUpload()，使用二进制 body + query 方式。
- 前端 UI：Models.vue 的“添加模型”对话框新增版本号与文件选择，提交时先创建 config.pbtxt，再上传文件；可选立即加载。

23:10

- ONNX→TensorRT Plan 自动转换与进度：
  - CP：controlplane/src/server/main.cpp 新增 /api/repo/convert_upload（POST，body=onnx），接受 model/version/filename；创建转换任务，调用 trtexec 生成 plan 文件后通过 va_repo_put_file 上传为 model.plan；返回 202 和 SSE 事件地址。
  - CP：同文件新增转换任务内存队列与 SSE 事件 /api/repo/convert/events?job=...，实时输出 trtexec 日志与阶段（created/running/uploading/done/failed）。
  - 前端：Models.vue 在选择平台为 TensorRT Plan 且上传 .onnx 时，改用 repoConvertUpload 并开启 SSE 实时显示转换日志；完成后可自动执行 Load。
  - API：web-front/src/api/cp.ts 新增 repoConvertUpload()。
- 备注：trtexec 需在服务器可执行环境中可用；日志按行流式推送，前端以对话框展示进度。

23:20

- 修复 SSE 404：
  - 更新 controlplane/src/server/http_server.cpp 的 SSE 检测逻辑，加入前缀匹配 /api/repo/convert/events（带查询串时不再误判为普通请求）。

23:40

- 交互调整：选择文件即触发转换并在对话框内展示进度
  - web-front/src/views/Models.vue：
    - 在“上传权重文件”下方内嵌转换进度区域（phase 标签 + 实时日志）。
    - 选择平台为 TensorRT Plan 且选择 .onnx 时，立即调用 /api/repo/convert_upload 启动转换，并通过 SSE 在本区域显示进度。
    - 文件选择前需先填写“模型 ID”，文件选择控件在未填写时禁用。
    - 提交按钮仅负责创建/保存 config.pbtxt；是否加载由用户勾选并在转换完成后手动触发或提交时触发（若仍在转换中则提示）。
    - 非转换场景：选择文件后立即通过 /api/repo/upload 上传，并在上传控件下方显示“上传中/已上传/上传失败”状态，无需等待提交。

23:58

- 将转换迁移到 VA：
  - Proto：video-analyzer/proto/analyzer_control.proto 新增 RepoConvertUpload/RepoConvertStream + 相关消息。
  - VA：video-analyzer/src/controlplane/api/grpc_server.cpp 实现转换任务队列（trtexec -> 生成 plan -> 写入 FS/S3），并通过 RepoConvertStream 推送 log/state/done 事件。
  - CP：/api/repo/convert_upload 改为转发到 VA（va_repo_convert_upload），/api/repo/convert/events 改为代理 VA 的 RepoConvertStream（SSE 转发）。
  - CP gRPC 客户端：controlplane/include/.../grpc_clients.hpp 与 controlplane/src/server/grpc_clients.cpp 新增 va_repo_convert_upload。
  - 注意：trtexec 需在 VA 运行环境可用（或通过 TRTEXEC 环境变量指定路径）。

00:15

- 修复 VA 编译错误：
  - video-analyzer/src/controlplane/api/grpc_server.cpp 捕获 append lambda 问题，改为直接对 job->logs 加锁写入；并补全 <unordered_map> 头文件。

00:22

- CP convert_upload 参数校验优化：
  - controlplane/src/server/main.cpp：当请求体为空时直接返回 400 INVALID_ARGUMENT，提示需以 application/octet-stream 提交 ONNX 二进制，避免返回 502 误导。
