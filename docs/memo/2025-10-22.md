### 2025-10-22 00:31:21
- 任务：重生成 docs/context/CONTEXT.md 与 ROADMAP.md（含最新限流/TTL/失败原因指标）
- 结果：已更新两文档；ROADMAP ≤800 词，包含总览/分阶段表/依赖矩阵/Top-5 风险
- 备注：后续继续 YAML 配置来源回显与 E2E 落档
### 2025-10-22 00:58:00
- 任务：重建并重启 VA，运行前端 Playwright E2E 校验 subscriptions.source
- 步骤：
  1) 修复链接错误：在 `video-analyzer/src/server/subscription_manager.cpp` 实现 `cleanerLoop` 与 `normalizeReason`；
  2) 重新构建 VA 并启动；
  3) 运行 `web-front` E2E：`npm run e2e`；
- 结果：`/api/system/info` 返回 `subscriptions` 含 `heavy_slots/model_slots/rtsp_slots/max_queue/ttl_seconds` 与 `source` 回显；用例通过（1 passed）。
- 提交：`a850259` 修复链接与完善清理/归一化。
