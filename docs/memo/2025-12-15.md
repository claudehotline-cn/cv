## 2025-12-15 00:14:56

- 任务：按需求改进插图占位符机制：由 `writer_audit` 审核“有图但未输出占位符”的小节，并触发 `section_writer` 重写该节，而不是由代码静默兜底插入占位符。
- 改动：
  - `writer_audit` 新增校验：若 `image_metadata[section_id]` 非空，但该节草稿中不存在 `<!--IMAGE:<section_id>...-->` 占位符，则将该节加入 `sections_to_rewrite` 并判定 `draft_quality_ok=false`。
  - 移除 `section_writer` 的“自动插入默认占位符”兜底逻辑，避免绕过 Writer 的排版决定。
- 备注：同时保留 `fetch_url_with_images` 的 User-Agent/重试配置以提升 Wikipedia 抓取稳定性（图源可用，审计才有意义）。

## 2025-12-15 00:37:12

- 任务：解决“插图位置不匹配、图片下方没有图名”的问题。
- 改动：
  - 占位符增强：Writer 可输出 `<!--IMAGE:<section_id>:<n>|<图名/图注>-->` 来显式指定图名。
  - Illustrator 替换时自动追加图名行：在图片 Markdown 下方插入 `*图：...*`（优先使用占位符中的图名，其次使用 `caption_hint/alt`）。
  - `writer_audit` 增加占位符索引合法性检查：当只给 1 张候选图却写了 `:2` 这类，会触发重写，避免最终无图/错图。
- 验证：容器内 `replace_image_placeholders` 可正确输出图名行并移除占位符。

## 2025-12-15 00:41:23

- 任务：解释并改进“候选图片列表为什么只有 2 张”导致 Writer 选择空间不足的问题。
- 原因：之前 Researcher 按设计将每节 `image_metadata` 限制为最多 2 张，Writer 的 `available_images` 也随之只有 2 张。
- 修复：将每节图片候选上限放宽到 8 张（Writer 仍最多插 2 张，但可用 `:n` 从更多候选里挑最匹配的图），并同步放宽 `writer_audit` 的占位符索引合法范围为 `1..len(candidates)`。

## 2025-12-15 00:45:32

- 任务：按需求将“每节实际可插入图片数量上限”提升到与候选数量一致。
- 改动：
  - `illustrator_agent` 默认 `max_images_per_section` 提升到 8（与候选上限一致），允许 Writer 通过多个占位符插入更多图片。
  - `writer_audit` 增加限制：本节占位符数量不得超过候选图片数，且 `:n` 索引必须在 `1..len(candidates)`。
- 验证：容器内 `replace_image_placeholders` 在 `max_images_per_section=8` 时可插入 8 张图片并生成 8 行图名。

## 2025-12-15 00:55:17

- 任务：解决“图片不居中、图注无编号且字体风格不对”的问题。
- 修复：将插图渲染从“图片 + 斜体图注行”改为“单列表格”格式，达到更通用的居中效果，并用 `图 N：caption` 自动编号且使用普通字体（不强制斜体）。
- 覆盖范围：占位符替换路径与末尾插图回退路径都会生成相同格式的图注。
