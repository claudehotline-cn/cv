2025-10-19 02:55 实施记录

- 任务：修复 WHEP 无画面（动态 PT 对齐、发送门控、注册会话映射、SDP 回退项、前端诊断日志已具备）。
- 后端改动：
  - video-analyzer/src/media/whep_session.hpp：Session 增加 pcConnected/payloadType 字段。
  - video-analyzer/src/media/whep_session.cpp：
    - attachMediaHandlers 使用协商后的 payloadType；
    - createSession：
      - onStateChange 置 pcConnected；
      - 解析 Answer 中 H264 PT，动态重建 RtpPacketizationConfig/H264RtpPacketizer；
      - 注册 bySid_ 与 indexByStream_ 映射；
      - inject_h264_fmtp 同时补 a=sendonly 与缺失 a=msid；
      - sdp_check 日志补充 used_pt/selected_pt。
    - feedFrame：
      - 在 pcConnected 前跳过发送并打日志；
      - sendFrame 异常不再直接标记 closed；保持会话。
- 构建与重启：
  - 停止 VideoAnalyzer，使用 tools/build_with_vcvars.cmd 构建成功；
  - 使用 tools/win/restart_backend.ps1 启动，/api/system/info=200。
- 运行证据：
  - logs/video-analyzer-release.log 出现：[WHEP] sdp_check h264_pt=121 used_pt=96 → selected_pt=121 reconfigured；
  - POST/DELETE /whep 全流程 201/204 正常，Answer/Offer 头部已打印。
- 后续计划（WHEP 验收）：
  - 前端用无扩展/隐身窗口验证：
    - 控制台应出现 [WHEP] offer_sdp_head/answer_sdp_head；
    - ICE/PC connected；
    - stats inbound-rtp/track 持续增长；
    - video loadedmetadata/playing 日志。
 - 若仍无画面：
    - 对比 sdp_check 的 dir/msid/pt；
    - 观察 feedFrame 是否有 "waiting pc connected" 或 sessions=0。

2025-10-19 03:05 修复 setRemoteDescription 解析错误

- 现象：浏览器报错 Failed to parse SessionDescription. a=msid: … Expected a stream ID and optionally a track ID。
- 处置：移除我们对 Answer SDP 人工注入的 a=msid 与 a=sendonly，仅保留 H264 fmtp 修正与动态 PT 对齐。
  - 修改：video-analyzer/src/media/whep_session.cpp 注入逻辑精简为仅插入/替换 a=fmtp 行。
- 重建与重启：已停止进程后成功链接与启动，/api/system/info=200。
- 验证建议：强刷前端（隐身窗口），观察 setRemoteDescription 不再报错，随后应有 inbound-rtp 统计与 video playing 日志。

2025-10-19 03:20 按《WHEP问题.md》优化

- 必修1（PT一致）：解析 Offer 的 H264 PT 并固定用于发送端与RTP打包；不再依据 Answer 变更 PT；sdp_check 增加 msid(ssrc)识别。
- 必修2（先配Sender后Answer）：addTrack(H264/PT=Offer) → setRemote(offer) → setLocal(answer)。
- 必修3（msid绑定）：继续使用 addSSRC(ssrc,cname,msid,trackId) 方式绑定；诊断识别 a=ssrc:… msid:…。
- 必修4（等待open）：仅在 pcConnected 且 track.isOpen() 后发送帧；加入 track.onOpen 日志。
- 前端：
  - H264优先：setCodecPreferences 将 H264 放前；
  - PATCH分行：候选fragment保持逐行；ICE complete 后 PATCH a=end-of-candidates；
  - ontrack/onunmute 与 transceiver currentDirection 日志增强。
