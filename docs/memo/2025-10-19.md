2025-10-19 18:25

任务：前端分析页面的视频播放功能测试（chrome devtools mcp + webrtc-internals）

步骤与环境：
- 启动后端：VideoAnalyzer、VideoSourceManager 已运行；前端 Vite 开发服 5173 运行。
- 测试源：rtsp://127.0.0.1:8554/camera_01（由用户已启动）。
- 访问分析页：http://127.0.0.1:5173/#/analysis。

关键结果：
- REST 就绪：/api/system/info /api/sources /api/models /api/pipelines /api/graphs 均 200。
- 订阅成功：POST /api/subscribe 201。
- WHEP 握手：POST /whep?stream=camera_01:det_720p 201，返回 Location=/whep/sessions/68f4bac33；后续 PATCH 204×2（trickle + end-of-candidates）。
- 控制台：ICE=connected，PC=connected，answer 协商 H.264（packetization-mode=1/0，多 profile-level-id）。
- video 元素：readyState=0，paused=false，videoWidth=0，videoHeight=0，currentTime≈0（未解码输出）。
- webrtc-internals：已截屏保存 D:\\Projects\\ai\\cv\\logs\\webrtc_internals.png（当前页面未展示 inbound-rtp 累计）。
- VA 日志（摘）：存在 [WebRTC] enqueue frame / transport.webrtc feedFrame/queued frame 记录，显示会话存在且有帧入队。

判定：
- 信令流程正确（201+Location、ICE/PC connected、H264 协商）。
- 媒体未到达前端（前端无 inbound-rtp/track 累计、video 仍 HAVE_NOTHING）。

建议排查（最小闭环）：
1) 在 VA 日志中继续关注 transport.webrtc/encoder.ffmpeg 的报错与关键帧（IDR）发送情况；确保会话初期产生 IDR 并携带 SPS/PPS。
2) 核对 app.yaml 编码参数（H.264、gop≈60、bframes=0、tune_zerolatency=true）与图链输出一致，必要时先降分辨率/码率做 A/B。
3) 若多网卡/NAT，保持 host-only ICE；确认 whep_base 与实际监听一致，避免跨网段阻断 SRTP。
4) 复位会话（前端“刷新”或关闭/重开分析开关）后在 webrtc-internals 观察 inbound-rtp（bytes/framesPerSecond）。

结论：当前“分析页播放”测试不通过：信令成功，但无媒体播放。需按建议继续定位。

2025-10-19 18:40

修复与验证：前端 WHEP 播放无媒体问题
- 变更1（WhepPlayer）：为 trickle-ICE sdpfrag 追加 a=ice-ufrag，增强服务端关联 m-line 的健壮性；
  - 发送候选与 end-of-candidates 均附带 ufrag。
- 变更2（WhepPlayer）：加入媒体流量守护（flow guard），在 setRemoteDescription 后 5s 若仍无 inbound-rtp（bytes/framesDecoded 均 0），自动 DELETE 资源并重建会话。
- 变更3（AnalysisStore）：setSource() 立即调用 updateWhepUrl()，确保切换源后播放器即时重建连接。

验证结果：
- 重新加载分析页后，首次会话仍出现 "no inbound"，flow guard 触发重连；第二次会话日志出现：
  - video loadedmetadata 2x2、video playing、rVFC presentedFrames=1。
- WHEP 信令依然 201 + 204；前端进入播放态。

后续建议：
- 持续观察 webrtc-internals inbound-rtp（bytes、framesPerSecond）；若分辨率稳定为 2x2，需检查 VA 编码器输出尺寸与初始 IDR 发送。
- 视需要在 /metrics 增加 webrtc/outbound 指标，便于自动化校验。

2025-10-19 18:51

修复：WHEP trickle/eoc 片段的 mid 使用实际 transceiver.mid，避免候选与媒体线不一致
- candidate 片段与 end-of-candidates 统一按 sdpMid/Transceiver.mid 回填（回退 0）
- setLocalDescription 后探测 mid 缓存，候选和 eoc 都用一致 mid

验证计划：
- 刷新 http://127.0.0.1:5173/#/analysis，选择 Source=camera_01 与 Pipeline=det_720p
- Console 观察 POST /whep 201、setRemote OK、pc/ice=connected、loadedmetadata 分辨率大于 2x2
- chrome://webrtc-internals/ 观察 inbound-rtp bytes/framesDecoded 持续增长


## 2025-10-19 20:21:24
- 构建 VideoAnalyzer: 成功
- 启动 VA(环境): VA_WHEP_KEY_COMPAT=1 VA_DISABLE_DATACHANNEL=1 VA_WHEP_FALLBACK_MS=0 VA_ICE_BIND=127.0.0.1
- 前端刷新/取证: inbound 仍为 null，pc=connected；保存截图
  - 播放器: D:\Projectsi\cv\logsnalysis_player_after_va.png
  - webrtc-internals: D:\Projectsi\cv\logs\webrtc_internals_after_va.png
- 后续: 若仍无入站，继续核对后端首帧发送日志与 PT/fmtp 对齐


## 2025-10-19 20:26:51
- 重启 VA: VA_WHEP_FALLBACK_MS=2000，等待 IDR 首帧
- 前端复测: pc/ice=connected，但 inbound 仍未出现；保存截图
  - 播放器: D:\Projectsi\cv\logsnalysis_player_after_restart.png
- 后续建议: 若日志出现 [WHEP] first frame start 但浏览器无 inbound，考虑强制编码端秒开（小 GOP、zerolatency）或实现 RTCP PLI 触发 IDR

2025-10-19 21:05:12

前端播放修复与取证（WHEP）
- 变更
  - 前端 WhepPlayer 在以下时机主动触发 play()：ontrack、loadedmetadata、pc.state=connected、setRemoteDescription 后（冪等容错）。
    - 文件: D:\Projects\ai\cv\web-front\src\widgets\WhepPlayer\WhepPlayer.vue
      - loadedmetadata 回调内新增 play()
      - setRemoteDescription 成功后尝试 play()
      - pc.connectionState===connected 时尝试 play()
  - VA 端 WHEP 首帧兜底窗口默认从 2000ms 下调为 500ms（可通过 VA_WHEP_FALLBACK_MS 覆盖）。
    - 文件: D:\Projects\ai\cv\video-analyzer\src\media\whep_session.cpp
      - fallback_ms 默认值从 2000 改为 500（保留 env 覆盖）。
- 构建与重启
  - 关闭运行中的 VideoAnalyzer.exe，重建 video-analyzer（Release），并以 VA_DISABLE_DATACHANNEL=1、VA_ICE_BIND=127.0.0.1 启动。
- 取证（chrome devtools mcp + webrtc-internals）
  - /api/* 正常；/whep POST=201，PATCH=204；ICE/PC=connected；ontrack 收到，video 触发 loadedmetadata 与 playing 日志。
  - webrtc-internals 截图: D:\Projects\ai\cv\logs\webrtc_internals_now.png
  - 播放页截图: D:\Projects\ai\cv\logs\analysis_after_fix.png
  - 服务器日志显示 [WHEP][tx] 连续发送，frames/bytes 递增。
- 现状
  - 前端控制台出现 video loadedmetadata 2x2、video playing 日志；偶发重连期间出现 ERR_CONNECTION_REFUSED，自动重试成功。
  - 建议后续：编码端定期 IDR、小 GOP、zerolatency，并实现上游 IDR 触发（RTCP PLI→编码器）。
2025-10-19 21:28:36

WHEP 最小页播放验证（chrome devtools mcp）
- 页面：http://127.0.0.1:5173/whep_minimal.html（点击 Start）
- 结果：video playing，1280x720，readyState=4，currentTime≈0.84，paused=false
- 网络：POST /whep 201，PATCH /whep/sessions/* 204×2（同域 127.0.0.1:8090）
- 控制台：无错误
- 证据：logs/whep_minimal_after_start.png
2025-10-19 22:09:41

修复：VA 8082 WHEP 送帧选路 + 验证
- 变更：whep_session.cpp 重写，feedFrame 同时匹配 fullKey 与 baseKey，并按 sid 去重。
- 构建：tools/build_with_vcvars.cmd 成功，已重启 VideoAnalyzer。
- 验证：
  - 分析页（组件）video playing，1280x720，readyState=4（证据：logs/analysis_panel_after_fix.png）
  - 最小页 + 8082（含 stream 参数）video playing（证据：logs/whep_minimal_on_8082_after_fix.png）
- 备注：保留 STUN 配置，未改动前端 WHEP 基址。
2025-10-19 22:18:54

优化：WHEP 播放流畅度
- 启用发送端 pacing（VA_WHEP_PACE_BPS=6Mbps），平滑 RTP 发送速率。
- 自适应 90kHz 时间戳：按 wall-clock 计算 ts 步长，避免固定 33ms 导致抖动。
- 重启 VA 后验证：inbound-rtp fps≈50、jitter≈0.033；组件页 1280x720 playing。
- 证据：DevTools getStats（结构化）：{fps:50, jitter:0.033, frames:~891}
2025-10-19 22:25:21

变更：去除对环境变量依赖的平滑设置
- pacing：默认启用 6 Mbps（可通过 VA_WHEP_PACE_BPS 调整或设为<=0关闭，但无需设置即可生效）。
- 首帧兜底：默认 200ms（VA_WHEP_FALLBACK_MS 可覆盖）。
- 帧策略：默认不丢弃 B-like 帧，提升流畅度（避免源端存在 B 帧时的卡顿）。
- 代码：video-analyzer/src/media/whep_session.cpp/.hpp。
