# 2025-10-18 01:25:23
- 任务：VSM 稳定性与 /metrics 可用性增强（第2项）
- 变更：REST/SSE 增加 CORS 头与 OPTIONS 预检；SSE 发送超时(SO_SNDTIMEO)；连接计数与拒绝计数接入全局指标
- 路径：video-source-manager/src/app/rest/rest_server.cc, video-source-manager/src/app/source_agent.cc, CMakeLists(已具备 ws2_32)
- 构建与运行：build-vs 配置（USE_GRPC=OFF），可执行监听7071；/api/source/list=200，OPTIONS=204含CORS；/api/source/watch_sse keepalive 正常
- 后续：可补充 SSE 断线重连退避策略与错误体结构化，与 VA 一致
# 2025-10-18 01:28:58
- 任务：REST 错误语义对齐与最小用例（第3项）
- 动作：新增测试 scripts/check_rest_error_semantics.py，覆盖 VA 与 VSM 的 400/404/204/200 路径；生成 evidence JSON
- 结果：通过；证据 docs/memo/assets/2025-10-18/rest_error_semantics.json
- 后续：可扩展 409（资源冲突）与 503（DB 不可用）场景的自动化触发与断言
# 2025-10-18 01:32:01
- 任务：REST 错误语义拓展（409/503）与取证
- 动作：扩展 check_rest_error_semantics.py：无效 YAML 触发 409；Docker 暂停 MySQL 触发 503，再自动拉起；生成证据 JSON
- 结果：通过；409/503 断言满足；证据 docs/memo/assets/2025-10-18/rest_error_semantics.json
# 2025-10-18 01:41:37
- 任务：Overrides/模板系统映射与类型转换（第4项）
- 变更：GraphAdapterYaml 增加  占位展开（优先 overrides.params.key/params.key）；SimpleExecutor 将 engine 覆盖限定在 open_all 期间，避免全局泄漏
- 验证：临时 YAML 含 ，以 overrides.params.* 注入数值 → /api/control/apply_pipeline=200 accepted
- 回归：构建成功并重启后端；系统信息未污染（engine options 未被持久改写）
# 2025-10-18 02:13:43
- 文档：重生成 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md`（≤800 词），对齐当前事实与下一步优先事项。
- 重点：后端 DB-only（无回退）与分页/时间窗/过滤、稳定的 /metrics + retention；前端 Logs/Events/Sessions DB 视图分页与导出；证据路径维持在 `docs/memo/assets/`。
- 下一步：修复并运行 `tools/win/db_bench_index.ps1`，追加多值过滤取证（node/stream_id CSV + 分页），完善 Metrics 阈值高亮与采样方案。
# 2025-10-18 02:20:00
- 控制面：落地 HotSwap 状态增强与 REST 状态扩展
  - 执行器 CollectStatusJson 增补：`overrides_consumed/ignored` 粗粒度分类、`model_targets`（遍历 `model.ort` 节点并输出当前 `model_path`）。
  - Graph 新增 `for_each_node` 以便只读遍历节点元数据。
  - PipelineController 记录 `last_apply_ms/last_apply_error`，`GetStatus` 合并 revision/project/tags 及 apply 统计。
- 构建：使用 `tools/build_with_vcvars.cmd` 成功重建；若占用触发 LNK1104，先停止进程再链接。
- 运行与验证：启动 `VideoAnalyzer.exe`，`/api/system/info=200`；`/api/control/status?name=hs_demo` 返回 NotFound（apply 未成功，但状态通路正常）。

## 2025-10-18 02:50:00
- 控制面：修复单条接口的 JSON 校验并分段 try/catch 标注阶段（parse/validate/apply/serialize）。
- 复验：apply_pipeline 正常（单次）；hotswap 正常（det -> yolov12x_copy.onnx）；status 显示 model_targets 更新。
- 注意：此前异常源于客户端双重 ConvertTo-Json 导致服务端收到 stringValue；已在测试脚本改为单次 ConvertTo-Json。

## 2025-10-18 03:05:00
- 控制面：新增 CP 指标（va_cp_requests_total 与 va_cp_request_duration_seconds 直方），对 apply/apply_batch/hotswap/drain/remove 计数与时延采样。
- 状态面：PipelineController 聚合 drain 快照（timeout_sec/elapsed_ms/ok/reason/blocked_nodes）。
- 取证：/metrics 摘要保存于 docs/memo/assets/2025-10-18/metrics_cp_excerpt.txt。
