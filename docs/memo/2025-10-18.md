2025-10-18  当前任务：WHEP 调试日志增强

执行内容：
- 后端（VideoAnalyzer）
  - rest.cpp：为 /whep 系列接口新增调试日志（POST/PATCH/DELETE，记录 streamKey、offer/frag 长度、选路 gRPC/local、cp_sid/va_sid、返回状态与 Answer 长度）。
  - media/whep_session.cpp：在 createSession/patchSession/deleteSession/feedFrame 增加日志（会话创建开始/完成、ICE 完成、Answer 长度、候选解析、删除会话、按 3s 节流输出 feedFrame 目标会话数与帧大小、sendFrame 异常）。
- 前端（web-front）
  - widgets/WhepPlayer/WhepPlayer.vue：在握手关键节点输出 console.debug（startSession 开始、pc 状态、设置本地/远端 SDP、ICE 完成、POST 状态、Location/Answer 信息），失败时输出 console.warn（重试）与 POST 状态。
  - 新增显式播放与 trickle-ICE：ontrack 中调用 video.play()（失败时提示解除静音/手动播放）；实现 pc.onicecandidate 将候选以 SDP frag PATCH 到 /whep/sessions/:sid（Content-Type: application/trickle-ice-sdpfrag），在 setRemoteDescription 后刷新 pending 候选。

构建与运行：
- 使用 tools/win/restart_backend.ps1 重启后端并健康检查 /api/system/info=200。
- 使用 tools/win/restart_frontend_preview.ps1 触发构建与预览重启（4173）。

后续建议：
- 若仍遇到“分析页打开后 REST 全部 pending”，可临时设置环境变量 `VA_DISABLE_SSE=1` 验证是否与 SSE 并发相关；并观察新增的 rest.whep 与 transport.webrtc 日志定位瓶颈。
- 追加 /metrics 暴露 WHEP 当前会话数与每路会话明细，前端 Metrics 页做可视化。
