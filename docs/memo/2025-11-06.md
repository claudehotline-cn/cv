2025-11-06 01:52:00

- 任务：远程 trt 分支“无法开始推理”排查；通过 docker 命令取证 VA 日志。
- 证据：
  - 容器：`va`（镜像 `cv/va:latest-gpu`）健康运行；`docker ps` 显示 Up/healthy。
  - 运行态：`curl http://localhost:18080/api/system/info` 返回 `provider=tensorrt-native`、`gpu_active=true`、`io_binding=false`。
  - 日志要点：
    - `analyzer.trt` 连续打印 tactic 选择与若干 “Skipping CaskConvolution: No valid tactics ...” 警告，为 TensorRT 构建期正常现象（可回退到非 Cask 路径）。
    - 12:23:15 起出现 `ms.node_model out_count=1 out0..2=1x84x8400(gpu)`，表明推理已开始且输出维度正确。
    - 未见引擎构建失败或推理失败的错误日志。
- 结论：
  - 远程 trt 分支当前可开始推理；先前“无法开始”应为首次构建引擎耗时较长导致的等待阶段。
  - 建议保留 timing cache（原生 TRT：`/app/.trt_native_cache`），后续冷启动时间将下降。
- 后续建议：
  - 如需缩短构建时长，可设置 `VA_TRT_TACTIC_SOURCES=cublaslt,cublas,cudnn` 或在配置中使用较小工作空间/串行构建。
  - 若需切回 ORT-TRT EP，请将 provider 设置为 `tensorrt`（非 `tensorrt-native`）。

- 运维改动：
  - 为 VA 服务增加原生 TRT 缓存挂载：
    - docker/compose/docker-compose.yml:22 新增 `../../.trt_native_cache:/app/.trt_native_cache:rw`
    - docker/compose/docker-compose.gpu.override.yml 对等新增挂载
    - 新建宿主目录 `.trt_native_cache/` 并在 `.gitignore` 中忽略
  - 应用方式（GPU 版）：
    - `NGC_TRT_TAG=25.08-py3 docker compose -f docker/compose/docker-compose.yml -f docker/compose/docker-compose.gpu.override.yml up -d va`
    - 或手动运行时追加：`-v $(pwd)/.trt_native_cache:/app/.trt_native_cache:rw`

- 新增工具：原生 TensorRT 预构建缓存程序（离线）
  - 可执行文件：`/app/bin/trt_cache_builder`（GPU 镜像内）
  - 用途：在不打开分析/不经由前端的情况下，直接解析 ONNX、构建整图引擎并写入 `/app/.trt_native_cache/timing_sm*.blob`。
  - 典型用法（容器内）：
    - `docker exec -e VA_TRT_TACTIC_SOURCES=cublaslt,cublas,cudnn -e VA_TRT_MIN_TIMING=1 -e VA_TRT_AVG_TIMING=1 -e VA_TRT_BUILDER_OPT=1 va /app/bin/trt_cache_builder --model /app/models/yolov12n.onnx --fp16 --workspace-mb 1024`
    - 完成后日志包含 `Saved timing cache to ...`，随后开启分析将直接加载缓存，避免前端超时。

- 事件与处理：
  - 观察到 `RuntimeSummary` 后紧接着 `REST /api/control/pipeline_mode enabled=false` 导致分析关闭，难以进入推理。
  - 已通过 `POST /api/control/pipeline_mode {stream_id, profile, analysis_enabled:true}` 打开分析，随后进入 TensorRT 引擎构建阶段（大量 analyzer.trt tactic 日志）。
  - 为缩短构建时长，已在 compose 注入：`VA_TRT_TACTIC_SOURCES=cublaslt,cublas,cudnn`、`VA_TRT_BUILDER_OPT=1`、`VA_TRT_MIN_TIMING=1`、`VA_TRT_AVG_TIMING=1`。
- 为避免开启分析后在原生 TRT 首次构建期间前端画面冻结：
    - 注：按要求已回退对 `video-analyzer/src/core/pipeline.cpp` 的改动，恢复原行为：`analyzer_->process()` 返回 false 时不做直通，仅返回 false。
