# 2025-10-29 21:10

- 任务：确保 controlplane 构建成功（为后续用 Chrome DevTools 调试前端视频播放、CP 代理 VA 的 WHEP 协商提供后端保障）。
- 操作：
  - 分析链接错误（LNK2019/LNK1120），定位 `controlplane/src/server/main.cpp` 引用 `controlplane::proxy_http_simple`，但目标未编译 `src/server/http_proxy.cpp`。
  - 修改 `controlplane/CMakeLists.txt`，在 `add_executable(controlplane ...)` 中补充 `src/server/http_proxy.cpp`。
  - 使用脚本 `tools/build_controlplane_with_vcvars.cmd` 进行 Windows 全量构建（脚本会清理 `controlplane/build` 并调用 Ninja）。
- 结果：
  - 构建成功，产物：`controlplane/build/bin/controlplane.exe`（约 16.0 MB）。
  - 示例：`controlplane.exe` 无配置参数时输出配置加载错误（符合预期），说明可执行已可运行，待按配置启动。
- 影响：修复了缺失编译单元导致的链接错误，CP 可进入运行阶段。
- 后续：
  - 按既定流程使用 CP 配置启动并联通 VA/VSM，随后进行 WHEP 协商与前端播放测试。

## 2025-10-29 21:25 前端联调取证（进行中）

- 场景：打开 `http://127.0.0.1:5173/analysis`，用 DevTools 观察网络与控制台。
- 现象：
  - `/api/models`、`/api/pipelines`、`/api/graphs` 返回 404（CP 未实现，属可选列表接口）。
  - 关键调用 `POST /api/subscriptions` 被浏览器以 CORS 拦截（缺少 `Access-Control-Allow-Origin`）。
- 处置：
  - 已为前端开发态切换为使用 Vite 代理，避免跨域：
    - 修改 `web-front/src/api/http.ts`，DEV 下 `base` 为空（走 `/api` 代理）。
    - 修改 `web-front/src/api/dataProvider.ts`，DEV 下 `apiBase()` 为空（走 `/api` 代理）。
    - 修改 `web-front/src/api/cp.ts` 的订阅取消与 SSE URL，DEV 下走相对路径。
  - 建议同步重启/刷新前端 dev 服务，确保 HMR 生效。
  - 或者，重启使用本次构建的 CP（内含全局 CORS 与 OPTIONS 预检）。
- 待续：确认其一后继续抓取 `/api/subscriptions`→`/whep` 证据与截图。

## 2025-10-29 21:35 实现 CP 列表接口（models/pipelines/graphs）

- 变更：在 `controlplane/src/server/main.cpp` 新增三个路由，返回最小 JSON：
  - `GET /api/models`    → `{ "code":"OK", "data": [] }`
  - `GET /api/pipelines` → `{ "code":"OK", "data": [] }`
  - `GET /api/graphs`    → `{ "code":"OK", "data": [] }`
- 目的：前端对这三类列表做“可选增强”，返回 200 且结构一致即可，空数组将触发 UI 默认回退（如 pipeline 默认为 `det_720p`）。
- 构建与发布：
  - 按规范先停止后端进程：`tools/kill_running.ps1`。
  - 使用脚本：`tools/build_controlplane_with_vcvars.cmd` 完整构建（含清理与 CMake 生成）。
  - 启动 CP：`tools/start_cp.ps1`（配置目录：`controlplane/config`）。
- 验证：
  - GET `http://127.0.0.1:18080/api/models` → 200 + CORS `*` + `{"code":"OK","data":[]}`
  - GET `http://127.0.0.1:18080/api/pipelines` → 200 + CORS `*` + 空数组
  - GET `http://127.0.0.1:18080/api/graphs` → 200 + CORS `*` + 空数组

## 2025-10-29 21:55 列表接口接入数据库（可选）

- 配置：
  - 在 `controlplane/config/app.yaml` 追加 `db` 段：
    - `driver: mysqlx`
    - `mysqlx_uri: mysqlx://root:123456@127.0.0.1:13360/cv_cp?ssl-mode=disabled`（示例，按实际 MySQL X Plugin 端口调整）
    - `timeout_ms: 1000`
- 代码：
  - 新增 `controlplane/include/controlplane/db.hpp`、`src/server/db.cpp`，基于 MySQL X DevAPI 读取三张表：`models`、`pipelines`、`graphs`。
  - 查询使用 JSON 生成：`JSON_ARRAYAGG(JSON_OBJECT(...))`，避免在 C++ 拼接。
  - CMake：可选链接 mysql-connector-cpp（vcpkg：`mysql-concpp` 或 `unofficial-mysql-connector-cpp`）。未配置/连接失败时回退为空数组，不影响主流程。
- 验证：
  - 默认（未配置 db）：三接口仍返回 `data: []`。
  - 待你提供有效的 `mysqlx_uri` 后，可复测返回真实数据。
2025-10-29 23:39:22  重建 context 与路线图
- 任务：重建 docs/context/CONTEXT.md（修复乱码、补充细节）。
- 结果：新增清晰版上下文，包含 DB 方案、WHEP/前端调试、三接口现状与下一步。
- 证据：docs/context/CONTEXT.md。

2025-10-29 23:39:22  生成 ROADMAP（≤800 词）
- 任务：在 docs/context/ROADMAP.md 生成路线图（总览/表格/依赖/风险Top-5）。
- 结果：按规范输出，聚焦 M0/M1/M2 与关键指标、缓解措施。
- 证据：docs/context/ROADMAP.md。
2025-10-29 23:54:12  修复三接口返回空数组问题
- 根因：controlplane/config/app.yaml 存在重复 db: 键，yaml-cpp 仅保留首个映射，导致 driver: mysqlx 且 mysqlx_uri 为空，从而 DB 访问被跳过，返回 []。
- 变更：
  1) 新增最小诊断：controlplane/src/server/db.cpp 捕获 JDBC/ODBC/mysqlx 异常并缓存；/api/_debug/db 输出错误快照与 DB 配置。
  2) 规范化配置：合并为单一 db:（driver=mysql，host=127.0.0.1，port=13306，user=root，schema=cv_cp）。
- 验证：
  - GET /api/_debug/db => { cfg.driver = "mysql" }
  - GET /api/models => 非空（5 条）
  - GET /api/pipelines => 非空（4 条）
  - GET /api/graphs => 非空（3 条）
- 路径：controlplane/src/server/db.cpp, controlplane/include/controlplane/db.hpp, controlplane/src/server/main.cpp, controlplane/config/app.yaml
