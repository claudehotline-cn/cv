## 2025-12-14 20:56:24

- 任务：根据 `docs/references/article_agent_design.md` 的设计，重构 `article_agent` 的状态与 LangGraph 工作流。
- 关键改动：
  - 按设计文档补齐 `ContentState` 字段，统一以结构化数据在 Graph 内流转（不保存 LLM 原始 JSON 字符串）。
  - 重构 `deep_graph`：init → collector → planner → researcher → research_audit → section_writer → writer_audit → merge_sections → doc_refiner(可选) → illustrator → assembler → summary_for_user。
  - 新增/调整 Pydantic Schema：`OutlineOutput/OutlineSection`、`ResearcherOutput`、`SectionDraftOutput`，并在 Planner/Researcher/Section Writer 使用 structured output。
  - 增加 `research_audit` / `writer_audit` 的纯规则质检与循环补全/扩写机制（带轮次上限）。
  - `Doc Refiner` 增加标题结构锁：校验润色前后所有标题行完全一致，否则回退草稿。
  - `Illustrator` 改为纯规则插图：仅从 `image_metadata` 里选原图插入章节末尾（不使用图像生成）。
  - Docker 部署增强：`docker/compose/docker-compose.yml` 为 `article-agent` 增加 `host.docker.internal:host-gateway` 映射，便于容器访问宿主机 Ollama。
- 构建/验证：
  - `python3 -m compileall -q article_agent/article_agent` 通过。
  - `docker build -f docker/article-agent/Dockerfile -t cv/article-agent:local .` 构建成功；并在容器内设置 `PYTHONPATH=/app/article_agent` 后可正常导入并编译 Graph。

## 2025-12-14 21:52:10

- 任务：修复 Agent Chat UI 点击 `md_url=/articles/...` 下载链接返回 404。
- 排查：
  - `article-agent` 导出的 Markdown 文件在宿主 `logs/articles` 与容器 `/data/articles` 中存在；
  - `agent-chat-ui` 容器虽然挂载了 `/articles`，但当前前端镜像版本未内置 `/articles/**` 的文件下载路由，点击相对路径会 404；
  - 同时 `third-party/agent-chat-ui` 是 gitlink 指向上游仓库，无法在本仓库直接提交其源码修改并推送到远端。
- 修复（不改动上游前端代码）：
  - 在 `docker/agent-chat-ui/Dockerfile` 构建阶段注入 `/articles/[...slug]` 路由（从容器 `/articles` 读取文件并返回，带路径穿越防护），无需修改 `third-party/agent-chat-ui` 的 gitlink。
  - 验证：重建并重启后，`curl http://localhost:18120/articles/article-001/article.md` 返回 200。

## 2025-12-14 22:05:30

- 任务：修复“图文并茂”提示词生成的 Markdown 无图片的问题。
- 原因：Researcher 虽然能看到 sources.images，但不一定会输出 image_metadata；下游 Illustrator 只会从 image_metadata 插图，因此最终无图。
- 修复：
  - 在 Researcher 阶段增加一次基于 LLM 的“只选图”补全流程：当 sources 有图片但 image_metadata 为空时，二次调用 structured output 让模型输出 `image_metadata`（严格要求 path_or_url 必须等于 sources.images 中给出的值）。
  - 提升网页采集阶段的图片采样上限（每个来源最多 30 张），增加可选图范围。

## 2025-12-14 22:20:49

- 任务：说明“图片现在如何获取与整理”，以及设计文档是否包含图片。
- 结论：
  - `docs/references/article_agent_design.md` 仍为纯文本设计说明，不包含图片。
  - 生成文章的图片来自 Collector 抓取的网页 `<img src>`（仅保存 URL，不下载二进制）；由 Researcher 的 LLM 结构化输出 `image_metadata` 进行章节分配，再由 Illustrator 规则插入 Markdown。

## 2025-12-14 22:28:42

- 任务：按你的要求改为“Writer 输出插图占位符，Illustrator 仅按占位符替换插图”。
- 改动：
  - Section Writer：在每节正文合适位置输出占位符行 `<!--IMAGE:<section_id>:<n>-->`（n=1/2）。
  - Doc Refiner：强制保留所有 `<!--IMAGE:...-->` 行（内容与位置都不能变化）。
  - Illustrator：优先用占位符替换为 `image_metadata` 中的图片 Markdown；若正文无占位符则回退到旧的“章节末尾插图”逻辑。
- 验证：
  - `docker build -f docker/article-agent/Dockerfile -t cv/article-agent:local .`
  - 容器内执行 `replace_image_placeholders` 断言通过（占位符被替换且不会残留）。

## 2025-12-14 23:51:37

- 任务：排查“Writer 已有占位符机制但最终仍无图”的原因并修复。
- 根因（证据）：抓取 Wikipedia 链接时 `requests` 默认 User-Agent 被拒，Collector 日志出现 403，导致 sources/images 为空，后续 image_metadata 与插图都为空。
- 修复：
  - `fetch_url_with_images` 增加可配置的 HTTP `User-Agent`/timeout，避免 Wikipedia 403。
  - `section_writer` 传入并暴露本节 `available_images`（来自 Researcher 的 `image_metadata`），要求“有图必须插占位符、无图不得插”，并加兜底：有图但没占位符时自动插入 `<!--IMAGE:<section_id>:1-->`。
- 验证：
  - 容器内 `fetch_url_with_images("https://en.wikipedia.org/wiki/Deep_learning")` 可成功返回图片数量 > 0。
