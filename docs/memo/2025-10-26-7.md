2025-10-26 12:54:44

任务：重生 docs/context/CONTEXT.md 与 ROADMAP.md（统一上下文与路线图），并纳入最新实现；同时推进 gRPC TLS 客户端可选支持。

执行情况：
- 生成：`docs/context/CONTEXT.md`（架构/现状/构建/测试/风险与后续）；`docs/context/ROADMAP.md`（≤800 词，含总览/表格/依赖/Top-5 风险）。
- 安全与配置示例：在 `controlplane/config/app.yaml` 增加 security 注释；CONTEXT/ROADMAP 增补安全基线说明。
- gRPC TLS（客户端）可选支持：`config.hpp`/`config.cpp` 解析 tls；`grpc_clients.cpp` 新增 `init_grpc_tls_from_config` 与按需 SslCredentials；`main.cpp` 启动初始化。
- 构建：CP 通过；冒烟（最小/常规）保持 PASS。

后续：如需 mTLS 端到端验证与证书示例，可新增独立脚本与 CI Job（自托管）。

