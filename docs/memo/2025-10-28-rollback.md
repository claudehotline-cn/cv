# 2025-10-28 21:34 回滚记录

- 回滚范围：撤销线路A相关代码改动（ORT TLS 初始化、预处理/后处理/叠加/Join/编码器中的 ensure_cuda_ready 以及 ORT 回退判断修订）。
- 操作方式：使用 git revert 逐步回滚以下提交：3e35385, 19e47e7, 385ec96, 6cc90b6。
- 结果：代码恢复至“全量提交当前仓库变更(73868ca)”之后的状态；移除了新增的 `video-analyzer/src/core/cuda_tls.hpp` 等改动。
- 后续：如需重新启用零拷贝修复，请先讨论具体路线及测试基线后再分阶段引入。

