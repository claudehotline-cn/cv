2025-10-24 03:01:06

停止 VA 进程并重建二进制（修复链接占用）\n- 拆分 server/rest.cpp 为多模块并更新 CMake；修复命名空间/重复定义
补齐 /api/subscriptions 流程：POST 返回 202+Location；GET 支持 ETag/If-None-Match 304\n- 运行脚本：check_headers_cache.py 通过；check_metrics_exposure.py 通过（阶段直方图暂缺告警）
重启 VA 并验证 /api/system/info 正常\n
- 2025-10-24 19:32:22 M1 进展：接入 WAL（init/mark_restart/scan），/api/system/info 暴露 registry.preheat 与 wal 状态；/metrics 增加 va_wal_failed_restart_total；完成构建与验证。

# 2025-10-24 19:59:46
- 实施内容：新增 WAL 管理接口（/api/admin/wal/summary, /api/admin/wal/tail），注册路由；CMake 显式纳入源文件。
- 构建：tools/build_va_with_vcvars.cmd 成功；停止运行中的 VA 后构建避免 LNK1104。
- 运行：已启动 VideoAnalyzer 并通过 /api/system/info 健康检查。
- 测试：新增 test/scripts/check_admin_wal_endpoints.py 并通过；手动验证两个新接口返回结构化 JSON。
- 下一步：补充订阅分阶段直方图/WAL 标签优化、预热耗时/错误指标、CI 集成 WAL/预热校验脚本。

# 2025-10-24 20:16:56
- 实施内容：/metrics 增加 ModelRegistry 预热指标与订阅分阶段直方图；Rest 启动阶段接入 WAL mark_restart+scan；新增 admin WAL 接口测试与预热/WAL 扫描测试脚本。
- 构建：成功（MSVC+Ninja）；运行：8082 健康检查 OK；/metrics 指标可见。
- 脚本：check_preheat_status.py、check_wal_scan.py 均通过。
- 下一步：完善 WAL 维度标签与前端展示，补充 app.yaml 环境示例与 CI 集成。

# 2025-10-24 20:25:22
- 实施内容：新增 tools/run_ci_smoke.ps1（串跑 admin wal/预热/wal 扫描三项测试）；文档新增 docs/references/wal_preheat_guide.md 与 docs/references/ci_smoke.md。
- 运行：本地验证 All smoke tests passed。
- 下一步：可选接入 check_headers_cache / check_metrics_exposure（安装 requests）并纳入 CI；前端 Admin 展示 wal/registry.preheat。

# 2025-10-24 20:28:21
- CI 集成：新增 .github/workflows/ci-smoke.yml（windows-latest），使用 run_ci_smoke.ps1 -BuildMode ci；新增 build_va_ci.ps1 用于 Actions 环境构建。
- 说明：本地与 CI 统一入口，Actions 将在 IOBinding 分支 push/PR 自动运行 Smoke。

# 2025-10-24 20:50:53
- M1 收尾：预热失败接入真实路径（未知模型计失败），新增 va_feature_enabled{feature=wal} 暴露 WAL 启用状态（低基数）。
- 构建与验证：本地构建通过；/metrics 可见 feature 标签；Smoke 仍可运行。

# 2025-10-24 20:59:45
- 实施内容：订阅入口接入 ACL（scheme/profile）与配额（全局并发/按键并发/按键每分钟），超限返回 429/403 并附 Retry-After；新增 /metrics: va_quota_dropped_total。
- 代码入口：rest_subscriptions.cpp（创建入口），rest_impl.hpp（配额窗口/计数），rest_metrics.cpp（暴露 dropped 指标）。
- 构建运行：通过；/metrics 可见配额 dropped 指标。

# 2025-10-24 21:36:01
- M2 开始：引入灰度配额 observe_only/enforce_percent、白名单与按 key 覆盖；新增 would_drop 指标与开关暴露。
- 压测/soak：新增 tools/run_soak.ps1 与 test/scripts/soak_load.py；添加 nightly-soak GitHub Actions 定时任务与 artifacts 上传。
- Grafana：后续将扩展面板展示 dropped/would-drop 分布与 enforce 百分比（参考 va_dashboard.json）。


## 2025-10-24 22:17:37
- 任务：扩展 Grafana 面板加入配额 dropped/would-drop 与 enforce 面板
- 变更：
  - 更新 `docs/references/grafana/va_dashboard.json`，新增：
    - 折线图：`sum(rate(va_quota_dropped_total[5m]))`、`sum(rate(va_quota_would_drop_total[5m]))`。
    - 统计：`va_quota_enforce_percent{}`（当前执行比例）。
  - 更新 `docs/references/grafana/README.md`，补充配额指标覆盖说明与查询建议。
- 验证：JSON 结构检查通过，布局与既有面板一致（24 栅格）。
- 后续建议：可补充 observe-only/enforce 开关状态面板，以及按规则/密钥维度的拆分视图（注意基数）。


## 2025-10-24 22:34:21
- 任务：增加取消用例的 SSE 轨迹验证（Playwright MCP 最小取证）
- 变更：
  - 新增 `video-analyzer/test/scripts/check_cancel_sse_playwright_mcp.md`，约束工具与最小 JSON 取证返回；给出可直接注入的 evaluate 代码。
  - 本地验证：采用 MCP `browser_navigate` + `browser_evaluate`，创建订阅→连接 SSE（phase）→DELETE 取消→返回 `{ id, firstPhase, delete:{ code, status } }`。
- 运行前置：VA 需运行；若遇 429，临时在 runtime `app.yaml` 中设置 `quotas.observe_only=true` 或提高 `rate_per_min`；本次验证已临时放宽并重启 VA。
- 结果：DELETE 返回 `202/cancelled`；SSE `phase` 事件可取得（值受时序影响）。


## 2025-10-24 22:39:37
- 任务：新增纯脚本取消-SSE最小取证测试，并集成到 smoke
- 变更：
  - 新增 `video-analyzer/test/scripts/check_cancel_sse_trace.py`，不依赖前端/Playwright；仅 requests + 手工解析 SSE。
  - 集成 `tools/run_ci_smoke.ps1`：新增 Test 4 运行上述脚本。
- 判定：默认最小取证（SSE 连接成功 + DELETE 202）；可用 `--require-cancel` 开启严格模式（要求 SSE 或 timeline 出现 cancelled）。
- 本地结果：最小取证 PASS；严格模式可能因 READY 竞态不稳定，需配合降低 start_pipeline_slots。


## 2025-10-24 22:48:31
- 任务：对失败原因映射做静态表校对，降低 unknown 占比
- 变更：
  - 扩充 `video-analyzer/src/core/reason_codes.hpp`：新增 app_not_initialized/profile_not_found/model_not_found/no_model_resolved/pipeline_init_failed/pipeline_init_model_failed/switch_*_failed/update_params_failed/set_engine_failed 等规范化原因码。
  - 强化 `SubscriptionManager::normalizeReason`（`video-analyzer/src/server/subscription_manager.cpp`）：加入上述字符串匹配映射，以及超时/rtsp/onnx/pipeline 等既有规则顺序优化。
- 构建：已重建 `VideoAnalyzer.exe` 成功。
- 预期：`va_subscriptions_failed_by_reason_total` 的 `unknown` 占比显著下降；新增原因码将聚合常见 last_error_ 文本。
- 后续：如观察仍有未知文本，可继续补充关键字；建议留意外部依赖报错格式（FFmpeg/RTSP/ONNX）变动。
