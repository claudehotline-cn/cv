2025-10-24 03:01:06

停止 VA 进程并重建二进制（修复链接占用）\n- 拆分 server/rest.cpp 为多模块并更新 CMake；修复命名空间/重复定义
补齐 /api/subscriptions 流程：POST 返回 202+Location；GET 支持 ETag/If-None-Match 304\n- 运行脚本：check_headers_cache.py 通过；check_metrics_exposure.py 通过（阶段直方图暂缺告警）
重启 VA 并验证 /api/system/info 正常\n
- 2025-10-24 19:32:22 M1 进展：接入 WAL（init/mark_restart/scan），/api/system/info 暴露 registry.preheat 与 wal 状态；/metrics 增加 va_wal_failed_restart_total；完成构建与验证。

# 2025-10-24 19:59:46
- 实施内容：新增 WAL 管理接口（/api/admin/wal/summary, /api/admin/wal/tail），注册路由；CMake 显式纳入源文件。
- 构建：tools/build_va_with_vcvars.cmd 成功；停止运行中的 VA 后构建避免 LNK1104。
- 运行：已启动 VideoAnalyzer 并通过 /api/system/info 健康检查。
- 测试：新增 test/scripts/check_admin_wal_endpoints.py 并通过；手动验证两个新接口返回结构化 JSON。
- 下一步：补充订阅分阶段直方图/WAL 标签优化、预热耗时/错误指标、CI 集成 WAL/预热校验脚本。

# 2025-10-24 20:16:56
- 实施内容：/metrics 增加 ModelRegistry 预热指标与订阅分阶段直方图；Rest 启动阶段接入 WAL mark_restart+scan；新增 admin WAL 接口测试与预热/WAL 扫描测试脚本。
- 构建：成功（MSVC+Ninja）；运行：8082 健康检查 OK；/metrics 指标可见。
- 脚本：check_preheat_status.py、check_wal_scan.py 均通过。
- 下一步：完善 WAL 维度标签与前端展示，补充 app.yaml 环境示例与 CI 集成。

# 2025-10-24 20:25:22
- 实施内容：新增 tools/run_ci_smoke.ps1（串跑 admin wal/预热/wal 扫描三项测试）；文档新增 docs/references/wal_preheat_guide.md 与 docs/references/ci_smoke.md。
- 运行：本地验证 All smoke tests passed。
- 下一步：可选接入 check_headers_cache / check_metrics_exposure（安装 requests）并纳入 CI；前端 Admin 展示 wal/registry.preheat。

# 2025-10-24 20:28:21
- CI 集成：新增 .github/workflows/ci-smoke.yml（windows-latest），使用 run_ci_smoke.ps1 -BuildMode ci；新增 build_va_ci.ps1 用于 Actions 环境构建。
- 说明：本地与 CI 统一入口，Actions 将在 IOBinding 分支 push/PR 自动运行 Smoke。

# 2025-10-24 20:50:53
- M1 收尾：预热失败接入真实路径（未知模型计失败），新增 va_feature_enabled{feature=wal} 暴露 WAL 启用状态（低基数）。
- 构建与验证：本地构建通过；/metrics 可见 feature 标签；Smoke 仍可运行。

# 2025-10-24 20:59:45
- 实施内容：订阅入口接入 ACL（scheme/profile）与配额（全局并发/按键并发/按键每分钟），超限返回 429/403 并附 Retry-After；新增 /metrics: va_quota_dropped_total。
- 代码入口：rest_subscriptions.cpp（创建入口），rest_impl.hpp（配额窗口/计数），rest_metrics.cpp（暴露 dropped 指标）。
- 构建运行：通过；/metrics 可见配额 dropped 指标。

# 2025-10-24 21:36:01
- M2 开始：引入灰度配额 observe_only/enforce_percent、白名单与按 key 覆盖；新增 would_drop 指标与开关暴露。
- 压测/soak：新增 tools/run_soak.ps1 与 test/scripts/soak_load.py；添加 nightly-soak GitHub Actions 定时任务与 artifacts 上传。
- Grafana：后续将扩展面板展示 dropped/would-drop 分布与 enforce 百分比（参考 va_dashboard.json）。


## 2025-10-24 22:17:37
- 任务：扩展 Grafana 面板加入配额 dropped/would-drop 与 enforce 面板
- 变更：
  - 更新 `docs/references/grafana/va_dashboard.json`，新增：
    - 折线图：`sum(rate(va_quota_dropped_total[5m]))`、`sum(rate(va_quota_would_drop_total[5m]))`。
    - 统计：`va_quota_enforce_percent{}`（当前执行比例）。
  - 更新 `docs/references/grafana/README.md`，补充配额指标覆盖说明与查询建议。
- 验证：JSON 结构检查通过，布局与既有面板一致（24 栅格）。
- 后续建议：可补充 observe-only/enforce 开关状态面板，以及按规则/密钥维度的拆分视图（注意基数）。


## 2025-10-24 22:34:21
- 任务：增加取消用例的 SSE 轨迹验证（Playwright MCP 最小取证）
- 变更：
  - 新增 `video-analyzer/test/scripts/check_cancel_sse_playwright_mcp.md`，约束工具与最小 JSON 取证返回；给出可直接注入的 evaluate 代码。
  - 本地验证：采用 MCP `browser_navigate` + `browser_evaluate`，创建订阅→连接 SSE（phase）→DELETE 取消→返回 `{ id, firstPhase, delete:{ code, status } }`。
- 运行前置：VA 需运行；若遇 429，临时在 runtime `app.yaml` 中设置 `quotas.observe_only=true` 或提高 `rate_per_min`；本次验证已临时放宽并重启 VA。
- 结果：DELETE 返回 `202/cancelled`；SSE `phase` 事件可取得（值受时序影响）。


## 2025-10-24 22:39:37
- 任务：新增纯脚本取消-SSE最小取证测试，并集成到 smoke
- 变更：
  - 新增 `video-analyzer/test/scripts/check_cancel_sse_trace.py`，不依赖前端/Playwright；仅 requests + 手工解析 SSE。
  - 集成 `tools/run_ci_smoke.ps1`：新增 Test 4 运行上述脚本。
- 判定：默认最小取证（SSE 连接成功 + DELETE 202）；可用 `--require-cancel` 开启严格模式（要求 SSE 或 timeline 出现 cancelled）。
- 本地结果：最小取证 PASS；严格模式可能因 READY 竞态不稳定，需配合降低 start_pipeline_slots。


## 2025-10-24 22:48:31
- 任务：对失败原因映射做静态表校对，降低 unknown 占比
- 变更：
  - 扩充 `video-analyzer/src/core/reason_codes.hpp`：新增 app_not_initialized/profile_not_found/model_not_found/no_model_resolved/pipeline_init_failed/pipeline_init_model_failed/switch_*_failed/update_params_failed/set_engine_failed 等规范化原因码。
  - 强化 `SubscriptionManager::normalizeReason`（`video-analyzer/src/server/subscription_manager.cpp`）：加入上述字符串匹配映射，以及超时/rtsp/onnx/pipeline 等既有规则顺序优化。
- 构建：已重建 `VideoAnalyzer.exe` 成功。
- 预期：`va_subscriptions_failed_by_reason_total` 的 `unknown` 占比显著下降；新增原因码将聚合常见 last_error_ 文本。
- 后续：如观察仍有未知文本，可继续补充关键字；建议留意外部依赖报错格式（FFmpeg/RTSP/ONNX）变动。


## 2025-10-24 22:53:27
- 任务：新增失败原因统计脚本，衡量 unknown 占比并提取原始关键词 Top-10
- 变更：
  - 新增 `video-analyzer/test/scripts/report_failed_unknown.py`：从 `/metrics` 读取 `va_subscriptions_failed_by_reason_total` 计算 unknown 占比；尝试 `/api/logs` 与 `build-ninja/bin/va.err/out` 提取原始关键词（best-effort）。
  - 集成 `tools/run_ci_smoke.ps1`：追加“Report”步骤，生成 `build-ninja/bin/logs/report_failed_unknown.json`。
- 输出：结构化 JSON（总失败数、unknown、占比、Top-10 原因、Top-10 原始关键词、数据源命中）。
- 说明：若 metrics 或日志不可用，脚本仍输出结构化结果并标注命中源。


## 2025-10-24 22:57:47
- 任务：M0/M1/M2 状态对齐与三周推进计划
- 摘要：基于 `docs/references/未完成工作.md:1` 汇总当前“已完成/在途/待办”并给出三周推进表。
- 参考实现/证据：
  - M0 REST 语义：`video-analyzer/src/server/rest_routes.cpp:170`、`video-analyzer/src/server/rest_subscriptions.cpp:180`、`web-front/src/stores/analysis.ts:240`。
  - 取消与 reason：`video-analyzer/src/server/subscription_manager.cpp:98`、`video-analyzer/src/core/reason_codes.hpp:1`。
  - WAL/预热：`tools/run_ci_smoke.ps1:1`。
  - 指标与面板：`docs/references/grafana/va_dashboard.json:71`。

- M0｜接口与稳定性
  - 已完成：
    - POST 202+Location、GET ETag/304、429+Retry-After 暴露头；SSE cancel 最小取证脚本（`video-analyzer/test/scripts/check_cancel_sse_trace.py:1`）。
    - reason 标准化表扩充与归一（unknown 占比预期下降）。
  - 在途：
    - 取消点 RAII/finally 复核（opening_rtsp/load_model/start_pipeline）。
    - 分阶段并发配置自检与 `/api/system/info` 回显一致性。
  - 待办：
    - 前端微条与“开始即取消”UX 回归；E2E 指标核对脚本补全。

- M1｜缓存/预热与恢复
  - 已完成：最小 WAL/预热路径与状态暴露，smoke 覆盖。
  - 在途：Registry LRU+TTL 与失败回收；预热并发/限流与指标。
  - 待办：Redis 集成与降级日志；failed(restart) 告警联动。

- M2｜配额与可观测
  - 已完成：observe_only/enforce_percent；Grafana dropped/would-drop 与 enforce 面板。
  - 在途：并发/长稳压测脚手架与报警阈值初版。
  - 待办：ACL 文案与按 key/GPU 上限策略收敛。

- 三周推进表：
  - 第1周（收敛 M0）：完成取消 RAII 复核与脚本用例；系统信息回显校对；前端微条与“开始即取消”回归；补阶段/失败原因核对脚本。
  - 第2周（M1 主线）：完成 Registry LRU+TTL 与失败回收；预热并发/限流与指标；WAL 重启扫描稳定性回归。
  - 第3周（M2 起步）：压测/soak 与阈值告警；ACL/配额文案与灰度策略；补 Grafana 告警面板。

- 验收口径：
  - M0：timeline 8 阶段、304/ETag 与 202/Location/CORS 通过，SSE 取消最小/严格取证可稳定复现。
  - M1：failed(restart) 可见、预热指标可控、WAL 扫描多次重启正确。
  - M2：N=50/100 并发与 24h soak 无泄漏，失败率与 P95 达标；配额/ACL 行为与提示一致。


## 2025-10-24 23:01:50
- 任务：执行推进表 M0 第一步（取消 RAII 与失败时间线补齐）
- 变更：
  - 统一取消 reason：使用 `kCancelled`（替换裸字符串）。
  - 补齐 `!got_model` 分支的 `ts_failed` 赋值，确保 timeline 完整。
- 构建：已重建 `VideoAnalyzer.exe`，二进制存在。
- 后续：继续 M0 自检 `/api/system/info` 回显一致性；追加阶段/原因指标核对脚本。


## 2025-10-24 23:47:48
- 任务：按推进表继续 M0 收尾（2/3 与 3/3）
- 变更：
  - 新增 `check_system_info_subs.py`：校验 `/api/system/info` 中 subscriptions 与 source 回显一致性。
  - 新增 `check_metrics_hist_and_reasons.py`：驱动失败样例，校验总时长直方图、分阶段直方图、失败原因聚合存在。
  - 集成 smoke：在 Test 5/6 中启动 VA 并执行上述脚本。
- 本地结果：两脚本均 PASS。


## 2025-10-25 00:10:35
- 任务：按要求联动启动 VSM 后重测
- 操作：
  - 启动 VSM：以前台方式启动并重定向日志（`vsm_run.log`），监听 `HTTP :7071`，gRPC 输出为 `127.0.0.1:7070`（netstat 未显示 7070，但 7071 在 LISTEN）。
  - 启动 VA：健康检查通过。
  - 复测：
    - `check_cancel_sse_trace.py`（最小取证）：PASS（DELETE 202，SSE 连接成功）。
    - `soak_load.py`（短时并发）在本机 3–5s 窗口内仍出现连接异常（`err` 计数，非 4xx/5xx），需后续在 M2 压测阶段配合更长窗口与日志定位。
- 产出：
  - `vsm_run.log`：VSM 启动日志与端口信息。
  - SSE 取消用例通过，证明 VSM + VA 并行运行下最小路径工作正常。
## 2025-10-25 01:47:35

- 任务：重建 docs/context/CONTEXT.md 与 ROADMAP.md
- 动作：读取旧文档，按对话重写，清理乱码版本，写入规范 Markdown
- 结果：完成；纳入 M0/M1/M2、WAL/预热、配额灰度、测试与脚本、依赖矩阵、风险清单
- 后续：调优 soak 阈值与重试；补充 app.yaml 与 Grafana 面板
## 2025-10-25 01:53:33

- 任务：RTSP e2e soak 复测（停止进一步测试）
- 参数：DurationMin=0.5, HttpTimeout=15, Concurrency=4
- 结果：RESULT=FAIL -> soak；ok=373 err=44；其余用例通过
- 证据：video-analyzer/build-ninja/bin/logs/soak_summary.json
- 决策：不再继续测试
- 后续建议（暂缓）：增加 SoakErrTolerancePct 容错阈值与 metrics 重试；或延长时窗/降并发
## 2025-10-25 02:06:57

- 任务：推进 M2（配额/ACL 灰度）最小闭环
- 变更：/api/system/info 暴露 quotas；app.yaml 增加 observe_only/enforce_percent/exempt_keys/key_overrides 示例；新增测试 check_quota_status.py、check_quota_metrics.py
- 构建：通过（tools/build_va_with_vcvars.cmd）
- 快速验证：check_quota_status 通过；metrics 中配额项在 registry 模式下不稳定（待跟进统一导出）
- 后续：可将 metrics registry 分支补齐 quotas would_drop/feature/enforce 行；或暴露 /api/metrics/config 控制开关
## 2025-10-25 02:15:13

- 任务：补齐 metrics registry 分支的配额指标并注册路由
- 变更：rest_metrics.cpp 增加 would_drop/feature/enforce；rest_routes.cpp 已有 /api/metrics 与 /api/metrics/set；验证 /metrics 输出
- 结果：check_quota_metrics.py 通过（registry 模式）；示例配置已更新
## 2025-10-25 02:18:55

- 任务：新增 ACL 白名单与 key 覆盖测试脚本
- 新增：check_acl_profile_scheme.py、check_exempt_bypass_acl.py、check_key_overrides.py
- 结果：ACL 方案/配置测试通过；白名单与覆盖用例因当前配置为空而跳过（pass=true, skipped）
## 2025-10-25 02:22:20

- 任务：将 ACL 白名单与 key 覆盖测试集成到 smoke/CI
- 变更：tools/run_ci_smoke.ps1 增加 Test 7/8，分别执行 quotas 快照/指标 与 ACL 白名单/覆盖用例
- 结果：本地运行 All smoke tests passed
## 2025-10-25 02:31:43

- 任务：M2 其它项推进（阈值告警、文案/提示、更多灰度场景）
- 改动：
  1) 每 key 灰度：key_overrides 支持 observe_only/enforce_percent；系统信息回显
  2) 友好提示：403/429 增加 X-Quota-Reason/X-Quota-Advice；保留 Retry-After
  3) 指标告警：新增 docs/references/alerts/va_alerts.yaml 与 README.md
- 构建：通过；快速校验：system.info/ACL 脚本通过
## 2025-10-25 02:36:40

- 任务：新增 per-key 灰度测试并集成 smoke
- 新增：check_key_observe_only.py、check_key_enforce_percent.py；Test 9 集成
- 结果：当前配置无 observe_only/enforce_percent 覆盖，测试按预期 skipped
## 2025-10-25 02:46:01

- 任务：提供 per-key 灰度演示配置与一键切换脚本
- 新增：video-analyzer/config/app.m2-gray.sample.yaml；tools/switch_app_config_m2_gray.ps1（apply/restore）
- 演示：已应用/验证 observe_only 与 enforce_percent（20 次请求，202/429 混合）
## 2025-10-25 02:51:27

- 任务：前端 Admin 展示与告警联动（M2）
- 改动：web-front/src/views/Admin.vue 增加 Quotas/ACL 卡片与 Alerts 区；支持 VITE_GRAFANA_BASE
- 静态资源：web-front/public/alerts/va_alerts.yaml（前端可下载 Prometheus 规则）
- 使用：VITE_API_BASE 指向 VA；可设置 VITE_GRAFANA_BASE 打开大盘；/admin 页面查看
## 2025-10-25 03:07:27

- 任务：按设计文档落地两个最小闭环
  1) 取消/RAII 收尾：runSubscriptionTask 增加 CompletionGuard（确保遗漏路径也记录完成）
  2) 模型缓存观测：ModelRegistry 增加 cache_entries/new/touch/evict 指标，/metrics 导出 va_model_cache_*
- 构建：通过；/metrics 已可见 va_model_cache_entries/new/touch/evict
## 2025-10-25 03:15:01

- 任务：/system/info 增补 ModelRegistry 缓存回显与校验脚本
- 改动：rest_system.cpp 添加 registry.cache{ enabled, capacity, idle_ttl_seconds, entries }；model_registry.hpp/cpp 补 getters
- 脚本：video-analyzer/test/scripts/check_model_cache_info.py（校验字段存在与类型）
