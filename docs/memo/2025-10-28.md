# 2025-10-28 工作日志


## 2025-10-28 00:40:48
- 统一 TLS/mTLS：三后端改为配置驱动，证书路径相对→绝对，SNI=localhost。
- VSM：新增 app.yaml 字段 va.{addr,tls.*}，源码读取并用于 gRPC mTLS；去除 env fallback。
- VA：NodeModel 读取 allow_cpu_fallback/use_io_binding，默认禁回退；确保零拷贝路径配置对齐。
- 文档：重生 docs/context/CONTEXT.md 与 ≤800 词的 ROADMAP.md。
- 下一步：重建并重启三后端，验证握手与前端播放；对 NMS/模型输出做一致性检查。

