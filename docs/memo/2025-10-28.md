# 2025-10-28 工作日志


## 2025-10-28 00:40:48
- 统一 TLS/mTLS：三后端改为配置驱动，证书路径相对→绝对，SNI=localhost。
- VSM：新增 app.yaml 字段 va.{addr,tls.*}，源码读取并用于 gRPC mTLS；去除 env fallback。
- VA：NodeModel 读取 allow_cpu_fallback/use_io_binding，默认禁回退；确保零拷贝路径配置对齐。
- 文档：重生 docs/context/CONTEXT.md 与 ≤800 词的 ROADMAP.md。
- 下一步：重建并重启三后端，验证握手与前端播放；对 NMS/模型输出做一致性检查。

# 2025-10-28 22:13:10
- 修改：按GPU零拷贝问题修复2.md实施C+B方案：
  - 新增 utils/cuda_ctx_guard.hpp（ensure_cuda_ready 绑定 Primary Context）
  - 新增 exec/stream_pool.hpp（TLS cudaStream 池），将预处理/后处理/叠加内核统一改为TLS流
  - 新增 exec/hooked_executor.hpp + exec/gpu_executor.hpp，并在 LRO 'loading_model' 步骤直接使用 HookedExecutor 同步执行，保证GPU线程初始化
  - 在 Pipeline::run 与 ORT CUDA provider 初始化路径调用 ensure_cuda_ready
- 构建：tools/build_va_with_vcvars.cmd 成功，产物 VideoAnalyzer.exe 已更新
- 运行：已后台启动 VA，可进一步联调 VSM/前端与脚本测试
- 回滚：仅头文件与小范围调用点，撤销补丁可回退

# 2025-10-28 22:35:54
- 测试：使用 CP 进行订阅（attach+apply）验证
  - CP 基础：controlplane/config 默认监听 127.0.0.1:18080，已启动
  - VSM：启动 VideoSourceManager，建立与 VA 的 gRPC 连接 (7070)
  - 步骤：
    1) POST /api/control/apply_pipeline 指定 YAML 成功 (202)
    2) POST /api/orch/attach_apply (attach_id=a_b494e6, pipeline_name=p_orch_21e1c0) 成功 (202)
    3) GET /api/sources 返回 Ready，fps≈25.2 (>0)
    4) POST /api/control/drain + DELETE /api/control/pipeline + POST /api/sources:detach 全部 202
- 结论：CP 订阅链路（VSM attach + VA 应用 pipeline）工作正常，满足帧数>0的最低要求
- 备注：VA gRPC(50051) 未启用情况下，使用 control.apply + orch.attach_apply 组合可完成订阅验证；必要时可再开启 VA gRPC 配合 /api/subscriptions 测试
# 2025-10-28 23:41:40
- 复测：CP + VA + VSM 全链路订阅（推流已就绪）
  - apply_pipeline: OK (202), pipeline=p_live_cam
  - orch.attach_apply: attach_id=camera_01, source_id=camera_01 返回 202
  - /api/sources: attach_id=camera_01 phase=Ready fps≈24.98（>0）
  - /api/control/status: phase=Ready（编排侧就绪）
- 结论：VA 现已处理数据帧，管线与订阅均正常
# 2025-10-29 00:03:36
- 调试增强：增加定位日志
  - postproc_yolo_det：输入形状/阈值、失败路径、CUDA NMS失败提示
  - node_nms：输入形状、prefer_cuda、GPU失败回退告警
  - graph：节点失败时打印当前可用tensor键与形状（最多3项）
  - ort_session：IoBinding运行后输出前3个张量形状与provider/io_bind标志
- 已重建 VA 并可用于复测

