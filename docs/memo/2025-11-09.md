2025-11-09 02:37

任务：Dockerfile.ort 参数内置（ORT_VERSION/CUDARCHS）
- 动作：移除构建命令中 --build-arg 依赖，Dockerfile 内以 ENV 固定；更新头部用法说明。
- 结果：docker/va/Dockerfile.ort 可直接 docker build -f ... -t ... .

任务：解耦 CUDA SHM 输入/输出禁用逻辑
- 动作：将输入侧/输出侧失败计数与禁用状态拆分（in_shm_disabled_/out_shm_disabled_），避免联动关闭。
- 结果：当输入 SHM 失败时，输出 SHM 仍可独立注册与绑定。

任务：输出 SHM 绑定按需字节数
- 动作：SetSharedMemory 绑定 min(need_bytes, capacity)，避免因容量偏大触发 invalid args。
- 结果：降低 SHM 绑定失败概率，便于 on_gpu 输出稳定。

任务：Context/Route 文档更新
- 动作：重写 docs/context/CONTEXT.md 与 docs/context/ROADMAP.md，纳入元数据自适配、SHM 策略、指标与阶段计划。
- 结果：形成后续执行的路线图与排障要点。

任务：推送远程（全量提交）
- 动作：准备 push 命令（当前分支 IOBinding，ahead 2），但受运行环境网络限制未直接执行。
- 指令：git push -u origin IOBinding（或 git push -u origin HEAD）
- 结果：待本机/有网络环境执行后生效。
