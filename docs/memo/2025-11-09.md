2025-11-09 13:30

任务：修复 Triton in-process 推理崩溃并增强防护

变更摘要：
- 禁用 in-process 模式下 `ExitOnError`（避免服务端遇错直接 `exit()`，导致宿主进程退出）。
  - 文件：video-analyzer/src/analyzer/triton_inproc_server_host.cpp
- 在 in-process 推理路径补充完整错误检查与资源释放，避免 NULL/错误状态引发崩溃：
  - 检查 `InferenceRequestNew`/`AddInput`/`AppendInputData`/`AddRequestedOutput` 等返回值，并在失败时删除 request/allocator。
  - 明确检查 `InferenceResponseError` 并记录日志。
  - 统一在成功与失败路径释放 `response`、`allocator`、`request`。
  - 当 `triton_no_batch`（assume_no_batch）为真时，移除 batch 维并调用 `SetBatchSize(1)`，与 Triton 的 batching 语义对齐。
  - 输出分配器：支持 GPU 输出持久化缓存，CPU 回退路径安全释放；修复从 GPU 输出到 CPU 视图时的 D2H 拷贝（避免将设备指针当作主存指针误用）。
  - 输入为 GPU 指针时：校验设备一致性并记录警告。
  - 文件：video-analyzer/src/analyzer/triton_inproc_session.cpp

测试建议：
- 运行 VA 并启用 provider=triton（inprocess=true），并设置 `triton_no_batch=true`（对于配置了 batching 的 TRT 模型）。
- 观察日志中 in-process 路径的警告/错误输出，验证不会出现进程直接退出；使用 `video-analyzer/test/scripts/check_gpu_inference.py` 做最小校验。

后续：
- 如需进一步定位模型维度/类型不匹配，建议在配置中显式指定 `triton_input` 与 `triton_outputs` 并打印 send_shape 与返回 shape。
2025-11-09 13:37:05
- 修复: 移除不兼容的 Triton C API 调用 `TRITONSERVER_InferenceRequestSetBatchSize`，避免编译错误。
- 位置: `video-analyzer/src/analyzer/triton_inproc_session.cpp`
- 原因: 所用 Triton 版本不提供该函数；批大小由输入张量形状推断，无需显式设置。
- 变更: 删除 BatchSize 设置逻辑与未使用变量，保留去批维逻辑（assume_no_batch 时移除前导 1）。
- 后续: 请在含 Triton 依赖的环境中重新构建并运行相关测试脚本验证推理路径。

- 修复: 避免推理请求对象双重释放，使用 TRITONSERVER_InferenceRequestSetReleaseCallback 交由服务端回收，并移除 Infer 后手动 Delete。
- 修复: Triton InferenceRequest 释放回调签名调整为3参版本 `(TRITONSERVER_InferenceRequest*, unsigned int, void*)` 以兼容 Triton 2.60.0 头文件。
- 优化: In-Process 显式模型控制支持
  - loadModel: 在 `model_control=explicit` 时调用 `TRITONSERVER_ServerLoadModel` 主动加载模型；
  - 析构: 尝试 `TRITONSERVER_ServerUnloadModel` 卸载会话使用的模型，缩短退出等待；
  - 通过 `weak_ptr` 绑定已有 Host，避免析构阶段意外新建 Server。
- 变更: 移除会话析构时的 `TRITONSERVER_ServerUnloadModel`，避免分析过程中意外卸载模型。保留 `explicit` 加载逻辑不变。
- 修复: Triton In-Process Server 生命周期
  - 问题: 推理一帧后立刻打印 server 关闭等待日志（30s 倒计时），原因是 Host 仅以临时 `shared_ptr` 存活，`weak_ptr` 单例未持有强引用，`run()` 返回即析构并触发 `ServerDelete`。
  - 变更: 在会话中持有 `shared_ptr<TritonInprocServerHost>`（`host_`），在 `loadModel` 赋值并在 `run()` 缺失时补充获取，保证会话生命周期内 Server 常驻。
  - 文件: triton_inproc_session.hpp / triton_inproc_session.cpp。
- 设计: 添加 MinIO(S3) 托管 Triton 模型仓库方案文档 `docs/design/minio_s3_model_repository.md`，包含 Compose 集成、环境变量、目录结构与运维流程。
