2025-12-04 00:00

- 任务：在 agent 服务中新增 Excel 分析与图表生成功能，基于 LangGraph 工作流和 LLM 生成 insight。
  - 在 `agent/cv_agent/excel` 下新增 schema/df_store/loader/analysis/chart_spec/echarts_builder/graph 等模块，支持自动选择 Sheet、DataFrame 缓存与聚合分析、ChartSpec 中间层以及 ECharts option 构建。
  - 在 `agent/cv_agent/server/api.py` 中增加 `/v1/agent/excel/chart` 接口，接收 `session_id/file_id/sheet_name/query/chart_type_hint`，返回 `used_sheet_name/charts/insight`。
  - 通过 `agent/tests/test_excel_agent.py` 增加测试，使用 DummySettings + Dummy LLM + LangGraph `get_excel_graph().ainvoke` 验证必须经过 Agent 与 LLM 路径生成 insight。

2025-12-04 01:54:16

- 任务：在 agent 服务中实现基于 MySQL 的数据库 → ECharts 图表 Agent，所有“选表/选维度/选指标/选图表类型”由 LLM 决策。
  - 在 `agent/cv_agent/db/schema.py` 中完善 `DbAnalysisRequest/DbChartPlan/DbAnalysisPlan/DbAgentResponse`，统一数据库分析请求与图表计划/响应结构，复用 Excel 侧的 `ExcelChartResult` 以保持前端一致。
  - 在 `agent/cv_agent/db/loader.py` 中基于 `pymysql` 实现 schema 预览与安全聚合查询：从 `information_schema` 中枚举表与列，并用参数化 SQL 构建 `SELECT ... GROUP BY` 查询，日志中记录 SQL 语句。
  - 新增 `agent/cv_agent/db/graph.py`，使用 LangGraph 将数据库分析流程拆为 `load_schema` → `build_plan`(LLM 决策) → `analyze_db`(执行只读聚合 SQL) → `build_chart_spec` → `build_chart` → `build_insight`(LLM 总结)，中间使用 `DbAnalysisPlan/DbChartPlan` 和 Excel 侧 `AnalyzedTable/ChartSpec/EChartsBuilder` 复用整套图表流水线。
  - `build_plan` 节点通过 LLM（OpenAI/Ollama 按配置选择）读取 `DbSchemaPreview`（表名、列名、样本行）和用户 query，严格要求返回 `{\"charts\": [...]}` JSON，字段包括 `table/group_by/metrics/agg/chart_type/title/description`，并使用 Pydantic 校验，不做本地兜底，失败直接返回错误。
  - `analyze_db` 节点构造基于 schema 的白名单（表名→列名列表），对 LLM 生成的每个 `DbChartPlan` 校验 `table/group_by/metrics` 均存在于 schema 中，然后调用 `execute_chart_query`，再用 Excel 侧 `_infer_column_types` + `AnalyzedTable` 封装结果，支持多图与多维视角。
  - `build_chart_spec` 节点构造一个轻量的“伪 Excel 请求”对象，仅包含 `query`，以复用 `build_chart_spec_from_analysis` 生成 ChartSpec；最终通过 `build_chart_results_from_spec` 变为 ECharts option，并由 `DbAgentResponse.used_db_name/charts/insight` 对外返回。
  - 在 `agent/cv_agent/config.py` 中新增 MySQL 连接配置：`db_host/db_port/db_user/db_password/db_default_name`，环境变量前缀为 `AGENT_DB_*`，默认指向 docker-compose 中的 `mysql:3306/cv_cp`。
  - 在 `agent/cv_agent/server/api.py` 中增加 `/v1/agent/db/chart` 接口，使用 `DbAnalysisRequest` 入参与 `DbAgentResponse` 出参，内部通过 `invoke_db_chart_agent` 调用 LangGraph，关键路径增加详细日志（start/plan/analyze/build/insight 以及 used_db_name/duration_ms）。
