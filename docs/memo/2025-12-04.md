2025-12-04 00:00

- 任务：在 agent 服务中新增 Excel 分析与图表生成功能，基于 LangGraph 工作流和 LLM 生成 insight。
  - 在 `agent/cv_agent/excel` 下新增 schema/df_store/loader/analysis/chart_spec/echarts_builder/graph 等模块，支持自动选择 Sheet、DataFrame 缓存与聚合分析、ChartSpec 中间层以及 ECharts option 构建。
  - 在 `agent/cv_agent/server/api.py` 中增加 `/v1/agent/excel/chart` 接口，接收 `session_id/file_id/sheet_name/query/chart_type_hint`，返回 `used_sheet_name/charts/insight`。
  - 通过 `agent/tests/test_excel_agent.py` 增加测试，使用 DummySettings + Dummy LLM + LangGraph `get_excel_graph().ainvoke` 验证必须经过 Agent 与 LLM 路径生成 insight。

