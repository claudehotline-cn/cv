2025-11-08 10:25

- 任务：修复 Triton CUDA 共享内存注册失败导致日志噪声，并提供服务器侧设备 ID 覆盖能力，保障在 CUDA_VISIBLE_DEVICES 映射不一致时可用。
- 变更：
  - video-analyzer/src/analyzer/triton_session.hpp
    - Options 增加 `shm_server_device_id`（默认 -1，表示使用本地 device_id），`shm_fail_disable_threshold`（默认 3）。
    - 运行期新增 `shm_disabled_` 状态，达到阈值后禁用 CUDA SHM。
  - video-analyzer/src/analyzer/triton_session.cpp
    - 注册输入/输出 CUDA SHM 时支持 `shm_server_device_id` 优先；
    - 连续失败（≥ 阈值）后自动禁用 CUDA SHM，并回退到 Host 路径，减少重复错误日志；
    - 日志提示运维检查 CUDA_VISIBLE_DEVICES 或在配置中设置 `triton_shm_server_device_id`。
  - video-analyzer/src/analyzer/model_session_factory.cpp
    - 支持从引擎选项读取 `triton_shm_server_device_id`、`triton_shm_fail_threshold`。
- 配置与文档：
  - docker/config/va/app.yaml 与 app.triton.yaml 新增 `triton_shm_server_device_id: "0"`、`triton_shm_fail_threshold: "3"` 作为默认示例；
  - docs/examples/triton_setup.md 增加“用 Docker 快速对齐 GPU 序号”章节与命令示例。
 - docker/compose/docker-compose.yml：为 `va` 服务补充 `ipc: host`，与 GPU 覆盖保持一致，确保 CUDA IPC 可用。
  - docker/va/Dockerfile.gpu：统一使用 `TRITON_TAG`（默认 25.10-py3）作为 client 与 runtime 基镜像；去掉不可用的 `-sdk` 变体依赖；确保 ARG 在首个 FROM 前声明。
  - docker/compose/docker-compose.gpu.override.yml：默认 `TRITON_TAG/NGC_TRT_TAG=25.10-py3`，可按需覆盖为你私库可用的 CUDA13 版本（如 25.09）。

2025-11-08 13:05

- 任务：在 docs/design 下撰写《Triton In‑Process 集成设计》文档，明确动机、架构、实现与落地计划。
- 变更：
 - 新增 docs/design/subscription_pipeline/triton_inprocess_integration.md（包含 mermaid 架构/时序图、Phase1/2 计划、接口与配置、风险与回退等）。
 - 影响：为后续在 VA 进程内集成 Triton 提供统一方案，消除 CUDA IPC/SHM 依赖与跨进程不确定性。

2025-11-08 13:20

- 任务：将 SOLID 原则要求落实到设计文档。
- 变更：
  - docs/design/subscription_pipeline/triton_inprocess_integration.md 新增第 14 节《SOLID 原则落实》：
    - 明确 SRP/OCP/LSP/ISP/DIP 在 In‑Process 方案中的落地方式；
    - 给出依赖边界、测试策略与实施检查清单。
- 影响：为后续实现提供清晰的结构与边界约束，降低耦合、提升可测试性与可演进性。

2025-11-08 13:40

- 任务：落实现代码骨架（不改现有接口），新增 In‑Process Triton 会话与 Server 托管器，并接入工厂与 CMake 开关。
- 变更：
  - 新增源码：
    - video-analyzer/src/analyzer/triton_inproc_server_host.{hpp,cpp}
    - video-analyzer/src/analyzer/triton_inproc_session.{hpp,cpp}
  - 工厂接入：video-analyzer/src/analyzer/model_session_factory.cpp 增加 `triton_inproc` 可选开关（默认 false）。
  - 配置加载：video-analyzer/src/ConfigLoader.cpp 透传 `triton_inproc`/`triton_repo`/端口与严格配置等键。
 - 构建：video-analyzer/CMakeLists.txt 新增 `USE_TRITON_INPROCESS` 选项，按 `TRITON_SERVER_ROOT`（默认 /opt/tritonserver）探测头/库并仅对 inproc 源添加头路径；存在则链接 `libtritonserver.so`。
- 影响：
  - 保持所有对外接口/调用方式不变；仅当配置 `triton_inproc:true` 且启用 `USE_TRITON_INPROCESS` 时走新路径；否则保持 gRPC 路径不变。
  - Phase 1 输出先走 Host（功能优先）；Phase 2 再提供 GPU 输出缓冲池，实现 GPU→GPU 零拷贝。

2025-11-08 13:55

- 任务：对 Docker Compose 构建进行适配，支持 In‑Process Triton。
- 变更：
 - docker/va/Dockerfile.gpu：新增构建参 `VA_USE_TRT_INPROC`，CMake 传入 `-DUSE_TRITON_INPROCESS=${VA_USE_TRT_INPROC}`。
  - docker/va/entrypoint.sh：新增 `VA_TRITON_EXTERNAL`（默认 1），为 0 时跳过启动外部 tritonserver 并不等待其就绪。
 - docker/compose/docker-compose.gpu.override.yml：设置 `build.args.VA_USE_TRT_INPROC=ON`，并在 `environment` 中设置 `VA_TRITON_EXTERNAL=0`。
  - docs/examples/triton_setup.md：补充 In‑Process 相关说明（构建参数与环境变量）。
- 影响：通过 Compose 即可开启 In‑Process 模式（不改接口），并避免外部 tritonserver 与 In‑Proc 并存导致的资源浪费或潜在冲突。

2025-11-08 14:05

- 任务：修复 Docker 构建失败（tritonclient 阶段路径不一致）。
- 变更：
  - docker/va/Dockerfile.gpu：
    - 新增 `TRITON_CLIENT_TAG`（默认 25.10-py3-sdk），将 `tritonclient` 阶段基于 SDK 镜像构建；
    - 移除对 `/workspace/install` 的直接 COPY，改为拷贝 `/opt` 到中间路径 `/opt/_tc` 并在 RUN 步骤自适配布局，拷贝到 `/opt/tritonclient`；
  - docker/compose/docker-compose.gpu.override.yml：新增 build arg `TRITON_CLIENT_TAG`，默认 25.10-py3-sdk。
- 影响：修复 `COPY --from=tritonclient /workspace/install/*` 在非 SDK 镜像缺失导致的构建失败，增强对不同 NGC 布局的兼容性。

2025-11-08 14:12

- 任务：修复 Docker 构建 `VA_USE_TRT_INPROC: unbound variable`。
- 变更：
  - docker/va/Dockerfile.gpu：在 build 阶段重新声明 `ARG VA_USE_TRT_INPROC`，并在 RUN 命令中使用默认取值 `
    ${VA_USE_TRT_INPROC:-OFF}` 以兼容未显式传参的情况（避免 `set -u` 导致失败）。
- 影响：在未传入该 build arg 时，默认按 OFF 构建；在 compose 覆盖中已明确传 ON 时，In‑Process 功能按预期启用。

2025-11-08 14:25

- 任务：修复 In‑Process 代码与 API 兼容问题（编译错误）。
- 变更：
  - triton_inproc_server_host.cpp：
    - TRITONSERVER_ServerOptionsNew/ServerNew 改为按 Error* 判定；
    - 去除 Http/Grpc 端口设置（部分发行版头未导出原型，默认关闭端口即可）；
    - 保持严格/退出错误/模型控制模式设置；
  - triton_inproc_server_host.hpp：
    - TRITONSERVER_Server 前置声明移至全局命名空间；成员返回类型加 :: 以避免命名空间冲突；
  - triton_inproc_session.cpp：
    - RequestNew/ServerInferAsync 改为 Error* 判定；
    - 回调签名改为 (alloc=nullptr, alloc_userp=nullptr, complete_fn, complete_userp)；
    - Output 读取改用 InferenceResponseOutput(base/size/memtype/...)；
    - on_gpu 输入走 cudaMemcpy D2H。
- 影响：解决编译期符号与原型不匹配问题；确保在 Triton 版本差异下可编译与运行。

2025-11-08 16:58

- 任务：支持“预构建 ORT 基镜像”以避免每次编译 ORT 造成的长耗时。
- 变更：
  - 新增 docker/va/Dockerfile.ort：构建仅包含 `/opt/onnxruntime` 的基础镜像（CUDA13 + TRT 25.08 可配），用于被 VA 镜像复用。
  - docker/va/Dockerfile.gpu：
    - 新增 `ORT_PREBUILT_IMAGE` 阶段 `ortprebuilt`，默认回退到 `TRITON_TAG` 基镜像并创建空目录；
    - 在 build 阶段优先 `COPY --from=ortprebuilt /opt/onnxruntime /opt/onnxruntime`；
    - ORT 构建步骤改为“若已存在 libonnxruntime.so 且 tag 匹配则跳过，否则才构建”。
  - docs/examples/triton_setup.md：新增“预构建 ORT 基镜像”章节，给出构建与复用命令。
- 影响：在提供 `--build-arg ORT_PREBUILT_IMAGE=<ref>` 时，VA 构建将直接复用预编译的 ORT；若未提供则保持现有行为（首次构建耗时，后续命中层缓存则较快）。
- 影响：
  - 若 Triton 服务端与本进程 GPU 序号不一致，将不再频繁报 “failed to open CUDA IPC handle: invalid resource handle”；
  - 可通过配置 `triton_shm_server_device_id` 显式对齐服务器侧设备序号；
  - 达到失败阈值后自动降级为 Host 路径，保证稳定性。
- 建议：
  - 运行 VA 与 Triton 时确保 CUDA_VISIBLE_DEVICES 映射一致；
  - 或在 engine.options 中设置：`triton_shm_cuda=true`、`triton_shm_server_device_id=0`（示例），必要时调整阈值：`triton_shm_fail_threshold=3`。
