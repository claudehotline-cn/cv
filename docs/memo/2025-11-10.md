2025-11-10 22:02:49

任务：使 graph 中的 `model_path_triton` 可生效，并在示例中设置默认值

- 代码：
  - `video-analyzer/src/analyzer/multistage/node_model.hpp/cpp`
    - 新增成员 `model_path_triton_` 并从 YAML 读取 `model_path_triton`；
    - 在 `open()` 中，当存在该字段时，向当前 EngineDescriptor 注入 `options["triton_model"] = model_path_triton_`，从而覆盖 Triton In-Process 的模型名；
    - 仍保持 Triton 会话使用占位路径 `"__triton__"` 的行为不变。
  - `docker/config/va/graphs/analyzer_multistage_example.yaml`
    - 为 `model_path_triton` 赋值 `"yolov12x"`。

- 说明：
  - 这样在使用 provider=triton（In-Process）时，可直接在 graph 节点通过 `model_path_triton` 指定仓库模型目录名，而无需调用 `set_engine` 传 `triton_model`；
  - 仍需在 app.yaml / 引擎选项中配置 `triton_repo` 指向仓库根路径；
  - 版本选择不在本次覆盖范围，默认 latest；如需版本控制，可扩展字段（例如 `model_triton_version`）。

2025-11-10 22:18:00

任务：允许订阅直接使用 Triton 仓库模型名作为 model_id（不切换 provider）

- 变更：`controlplane/src/server/main.cpp` `/api/subscriptions` POST
  - 若 `model_id` 命中 RepoList，则合并调用 SetEngine(options.triton_model=model_id)；不设置 provider；随后清空 model_id 进行订阅。
- 推送：IOBinding 分支 ec7fd38。

任务：新增最小 E2E 脚本

- 新增：`controlplane/test/scripts/test_subscribe_triton_repo_model.py`
  - 步骤：`/api/repo/list` → `/api/subscriptions`（model_id=仓库模型）→ 轮询订阅状态。
 - 运行：`CP_BASE=http://127.0.0.1:8080 python test_subscribe_triton_repo_model.py`

2025-11-10 22:35:00

任务：M1 别名与版本治理（最小可用）

- 新增 CP HTTP：
  - GET  /api/models/aliases → [{ alias, model_id, version? }]
  - POST /api/models/aliases { alias, model_id, version? }（幂等覆盖）
  - DELETE /api/models/aliases/{alias}
- 存储：内存映射 + 文件持久化 logs/model_aliases.json（可用环境变量 CP_ALIASES_FILE 覆盖）；后续可替换为 MySQL 持久化。
- 提交：c405173
