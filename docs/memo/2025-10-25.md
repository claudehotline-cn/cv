2025-10-25 10:00

任务：根据 lro_compiled_library_design.md 与 lro代码示例.md 评估后续工作，并输出清单与优先级。

结论要点：
- LRO 库需保持完全通用：不包含任何 VA 业务名（phase、reason 等）。Runner 仅暴露通用 states 映射与事件回调；VA 侧自行映射显示与指标。
- CMake 采用“编译库”方案：add_library(lro_runtime …)，支持可选 gRPC 适配与示例 REST；提供 find_package/安装规则（后续）。
- VA 接线：/api/subscriptions 全部切换到 Runner；/metrics 与 /system/info 从 Runner 快照取数；最终移除 SubscriptionManager。
- 观测：提供 queue_length、in_progress、states 映射、失败原因聚合（可选），Retry-After 估算通过 AdmissionPolicy 暴露；VA 侧导出 Prometheus 与 JSON。
- 测试：最小 API（POST/GET/DELETE/SSE）、失败路径（无效 scheme/模型加载失败/订阅失败）、取消中断、WAL 一致性等。

后续计划（摘要）：
1) 库侧补全：runner.cpp 通用 phase/states 聚合；admission 与 retry_estimator SPI；notifier 默认实现；MemoryStore 覆盖测试。
2) 适配与示例：示例 REST 与可选 gRPC 适配编译通过，添加 README 与运行说明。
3) VA 接线与替换：完成 REST/metrics/system_info 切换后，移除 SubscriptionManager 并清理引用。
4) 指标与语义：补齐 registry 与纯文本 /metrics 的一致性，增加 in_progress 与失败原因聚合输出。
5) 测试与 CI：将最小 API 与失败路径脚本纳入 smoke；增加 WAL 重启扫描用例。

本次变更与验证：
- 指标：在 metrics registry 分支新增 `va_subscriptions_in_progress`（与纯文本分支对齐）。
- 脚本：`tools/run_ci_smoke.ps1` 集成 `check_min_api_once.py`（Test 1b）。
- 构建：`video-analyzer/CMakeLists.txt` 复用已有 LRO 子目录链接逻辑，清理重复条目；成功链接 `lro_runtime`。
- 文档：重写 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md`（≤800 词）。
- 构建与测试：停止 VA → 构建通过 → 启动 VA → 最小 API 脚本通过，返回 ready 且 GET/DELETE 成功。

追加执行：
- 在 metrics registry 分支镜像 WAL 事件计数（va_wal_events_total），与纯文本分支一致（tail 200 行，低维度）。
- LRO 安装/导出：lro/CMakeLists.txt 增加 install/export 与 lroConfig.cmake.in，支持 find_package 引入。
- 再次构建验证通过，/metrics 可见新增指标（在注册表或纯文本分支之一展示，取决于配置）。

新增：
- lro 单元测试目标：lro/tests/test_min.cpp 与 CMake，目标名 lro_unit_tests（EXCLUDE_FROM_ALL）。
- 构建与运行：在 VS 开发者环境下单独构建并运行，输出 OK。
- 文档：新增 docs/design/lro_integration_guide.md（VA 接入/迁移、验证与回滚指南）。

适配与示例：
- REST 示例：新增 example_rest（无框架伪 Router），文件：lro/examples/rest_server/main.cpp，lro/adapters/rest_simplehttpserver/routes.cpp；CMake 生成可执行。
- gRPC 示例：新增 lro/adapters/grpc/lro.proto 与 server.cpp，example_grpc（条件启用，使用 vcpkg gRPC/Protobuf）；CMake 自动生成 pb/grpc 源并链接。

文档：
- 更新 lro/README.md：补充 find_package 用法、示例构建运行、连续 watch 与 Retry-After 例子。
- 新增 docs/references/lro_usage_examples.md：集中展示 Runner.watch/Admission.retry_estimator 用法与示例构建命令。
