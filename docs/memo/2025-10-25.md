2025-10-25  
任务：保证 video-analyzer (VA) 项目在 Windows 上成功构建（不使用 Anaconda 库，改用 D:\Projects\vcpkg）。

- 重命名与构建验证（control_plane_embedded → controlplane）
  - 将 VA 内嵌控制面目录从 `src/control_plane_embedded` 重命名为 `src/controlplane`。
  - 修正 CMake 源列表与所有 `#include` 引用。
  - 重新构建通过：`video-analyzer/build-ninja/bin/VideoAnalyzer.exe` 更新成功。

- 目录重命名（controlplain → controlplane）
  - 将仓库根下的 `controlplain/` 目录重命名为 `controlplane/`。
  - 更新根 `CMakeLists.txt`：选项变量改为 `BUILD_CONTROLPLANE`，`add_subdirectory(controlplane)`。
  - 更新相关脚本：
    - tools/run_cp_smoke.ps1（路径与输出文案）
    - tools/start_cp.ps1（可执行与配置路径）
    - tools/build_controlplain_with_vcvars.cmd（S/B 路径）
  - 现有可执行仍为 `controlplain.exe`（项目名暂不改动），路径为 `controlplane/build/bin/controlplain.exe`。

- 2025-10-25 补充：清理 VSM/CLI 构建中的 gRPC 开关
  - 脚本变更：
    - tools/build_vsm.bat：移除 USE_GRPC 形参与传参，改为固定使用 vcpkg 工具链并声明“gRPC REQUIRED”。
    - tools/build_vsm_with_vcvars.cmd：移除 -DUSE_GRPC=ON。
    - tools/build_cli.bat：移除 -DUSE_GRPC=ON。
  - CMake 变更：
    - tools/va_vsm_cli/CMakeLists.txt：删除对 USE_GRPC 的编译开关判断，仅在检测到 Protobuf/gRPC 时构建，缺失则跳过。
  - 验证：
    - 成功生成 D:\Projects\ai\cv\video-source-manager\build\bin\VideoSourceManager.exe。
    - 成功生成 va_vsm_cli（在 gRPC/Protobuf 可用时）。

- 步骤与变更：
  - 关闭遗留 VA/VSM 进程，清理旧的 build-ninja 目录。
  - 修复 Ninja 规则错误（清理后重新配置）。
  - 修改工具链与构建脚本，强制使用 vcpkg：
    - tools/configure_va_nv.bat：
      - 强制设置 VCPKG_ROOT=D:\Projects\vcpkg，VCPKG_INSTALLED_DIR，指定 CMAKE_TOOLCHAIN_FILE。
      - 清除可能干扰的 CONDA/CMAKE 前缀变量；添加 CMAKE_IGNORE_PREFIX_PATH 以屏蔽 H:\anaconda3。
      - 指定 gRPC/protobuf/OpenSSL/ZLIB 的 vcpkg 路径；开启 VA_USE_LOCAL_YAML=ON（用内置 yaml-cpp）。
    - tools/build_va_with_vcvars.cmd：无生成文件时先调用 configure_va_nv.bat，再用 Ninja 编译。
  - 修复头文件缺失：video-analyzer/CMakeLists.txt 为 WHEP 工具目标补充 vcpkg 头文件包含；将 protobuf 的 find_package 切换为 Protobuf（CONFIG）。
  - 复制运行时配置到 bin/config（app.yaml、models.yaml、profiles.yaml、graphs/*）。

- 构建结果：
  - 成功生成：D:\Projects\ai\cv\video-analyzer\build-ninja\bin\VideoAnalyzer.exe
  - 产物：WhepClientProbe.exe、WhepStandaloneServer.exe 同时生成。
  - 链接与工具：使用 vcpkg 中的 OpenCV 4.11、OpenSSL 3.5、Zlib 1.3.1、Protobuf 29.5、gRPC（插件与 protoc 路径均来自 vcpkg）。

- 冒烟验证：
  - 启动 VA（传入 bin/config），6 秒内监听端口：
    - HTTP metrics：9090
    - gRPC：50051
  - 随后终止进程，端口监听验证通过（已排除 Anaconda 依赖）。

- 后续建议：
  - 如需一键运行，可用 tools/run_va_preheat.cmd 或补充一个统一的 stage+run 脚本。
  - 若需自动化测试（脚本在 video-analyzer/test/scripts），请先确保本地 MySQL/Redis/RTSP 测试环境已就绪。
