2025-10-25 00:00:00

- 任务：扩展 Grafana 面板（模型缓存 entries/new/touch/evict）
  - 变更：更新 docs/references/grafana/va_dashboard.json，新增 “Model cache entries”（stat）与 “Model cache churn per 5m”（graph，new/touch/evict 三线）。
  - 指标：va_model_cache_entries、va_model_cache_new_total、va_model_cache_touch_total、va_model_cache_evict_total。
  - 验证：导入面板 JSON 后，检查曲线是否有值；可用 rate()/increase() 对比一致性。

- 任务：新增告警（阶段 P95/P99 与队列长度）
  - 变更：docs/references/alerts/va_alerts.yaml 与 web-front/public/alerts/va_alerts.yaml 增加规则组 va-phases-and-queue。
  - 指标：
    - P95/P99：histogram_quantile() 基于 va_subscription_phase_seconds_bucket（按 phase 聚合）。
    - 队列：va_subscriptions_queue_length（avg_over_time）。
  - 阈值：p95 > 5s（5m，warning）、p99 > 8s（10m，critical）；队列平均 >10（5m，warning）、>30（10m，critical）。
  - 使用：Prometheus Alertmanager 导入 YAML；前端 Admin 可通过 /alerts/va_alerts.yaml 下载。

- 后续建议：若业务波动较大，阈值按环境调优；必要时将 per-phase 阈值细分（opening/loading/starting）。

- 任务：CodecRegistry（可选）指标接线（零侵入）
  - 新增：src/core/codec_registry.{hpp,cpp}，仅记录计数（不做缓存/LRU）。
  - 注入点：
    - FfmpegRtspSource::openImpl 成功后 noteDecoderBuild(dec->name|"ffmpeg_sw")。
    - NvdecRtspSource::openImpl 成功后 noteDecoderBuild("nvdec")。
    - FfmpegH264Encoder::open 选型后 noteEncoderBuild(encoder_name|"ffmpeg")。
    - NvencH264Encoder::open 成功后 noteEncoderBuild("h264_nvenc")。
  - 指标：va_codec_decoder_build_total{impl}, va_codec_encoder_build_total{impl}；命中(hit)先保留为0占位。
  - 暴露：/metrics 已输出（registry 分支），构建通过，功能零侵入。
  - 命中语义：当前无缓存/复用路径，定义为“同一 impl 重复使用”的计数（第二次及以后 build 视作 hit 一次），后续若加入 LRU 再调整。

- 任务：调度公平性与 use_existing 可观测性 + SSE 指标
  - 公平：pending 队列改为 deque，worker 选择窗口内第一个与上次不同 key 的任务（窗口=8），避免同源饿死；计数 rr_rotations_total。
  - 合并：在 enqueue 处统计 merge_non_terminal/merge_ready 命中与 miss；/metrics 暴露 va_subscriptions_merge_total{type}。
  - SSE：新增 va_sse_connections{channel}（subscriptions/sources/logs/events）与 va_sse_reconnects_total；在各 SSE 入口增量/减量与 Last-Event-ID 记数。

- 任务：前端 Admin 展示模型缓存（registry.cache）
  - 变更：web-front/src/views/Admin.vue 新增 Model Cache 卡片，展示 enabled/capacity/idle_ttl_seconds/entries；loadInfo 解析 data.registry.cache。

- 任务：Grafana 面板补齐（Codec）
  - 变更：docs/references/grafana/va_dashboard.json 新增三块：decoder builds(per impl)、encoder builds(per impl)、hits（dec/enc）rate 图。

- 任务：失败路径用例稳健化（RTSP 打不开）
  - 变更：video-analyzer/test/scripts/check_fail_rtsp_open.py 扩大超时（≥60s），并在 403 且 X-Quota-Reason=acl_* 时快速通过（环境 ACL 拒绝情况下稳定）。

- 任务：WAL 旋转/TTL 跨重启证据强化（最小脚本）
  - 新增：test/scripts/check_wal_rotation_ttl.py（重启 VA，比较 va_wal_failed_restart_total，拉取 /api/admin/wal/tail；wal 关闭时跳过）。

- 任务：CI smoke 集成
  - 更新：tools/run_ci_smoke.ps1 插入测试项：
    - Test 3b：check_wal_rotation_ttl.py
    - Test 11：check_merge_metrics.py
    - Test 12：check_sse_metrics.py

- 任务：输出 LRO 设计方案文档
  - 新增：docs/design/lro_subscription_design.md（LRO 库与订阅改造完整方案：目标/结构/API/Admission/WAL/可观测/VA集成/删除SubscriptionManager/迁移与验收）。

- 任务：创建独立 LRO 仓库骨架
  - 目录：`lro/`，CMake 包（目标 `lro::lro`），首期 header-first。
  - 头文件：include/lro/{runner.h,state_store.h,admission.h,metrics.h,notifier.h,reason.h,adapters/rest_simple.h}。
  - README：lro/README.md（子目录集成与 API 说明）。

- 任务：/api/preflight 增加拒绝原因建议
  - 在 quotas would_drop 基础上输出 quotas_advice[{reason,suggest}]；示例：acl_scheme/acl_profile。
  - 验证：POST /api/preflight（rtspx://…）可见 quotas_advice。

- 任务：动态背压 Retry-After 与指标
  - 变更：rest_subscriptions.cpp 按队列长度与阶段槽位估算 Retry-After（1..60s），覆盖 global/key 并发、key rate、queue_full 路径。
  - 指标：rest_metrics.cpp 暴露 va_subscriptions_slots{type=open_rtsp|load_model|start_pipeline} 与 va_backpressure_retry_after_seconds（估算值）。
  - 验证：/metrics 可见新增指标；压满队列/限流时 429 响应含 Retry-After（动态）。

2025-10-25 15:20
- 任务：撰写《LRO 运行时（编译库方案）设计》并保存至 `docs/design/lro_compiled_library_design.md`。
- 动作：按示例结构定义 include/src/ 目录与构建目标 lro_runtime；明确与 VA 的集成与迁移路线、风险与回滚。
- 结果：文档已生成，等待后续按文档实施代码改造与构建验证。

2025-10-25 15:36
- 任务：实现 LRO 编译库骨架（严格对齐参考示例）。
- 动作：新增头文件 `include/lro/{status.h,operation.h,state_store.h,executors.h,notifier.h,runner.h}`；新增实现 `src/lro/{executors.cpp,state_store_mem.cpp,runner.cpp}`；更新 `lro/CMakeLists.txt` 生成目标 `lro_runtime`，并保留旧的头文件别名目标以兼容 VA 现状。
- 结果：库代码与 CMake 已就绪。下一步计划：在 VA 中链接 `lro_runtime` 并逐步替换旧接口（/subscriptions, /metrics, /system/info），再统一构建与测试。

2025-10-25 15:58
- 任务：在 VA 中链接 `lro_runtime` 并修复编译。
- 动作：
  - `video-analyzer/CMakeLists.txt` 引入 `add_subdirectory(../lro)` 并 `target_link_libraries(VideoAnalyzer PRIVATE lro_runtime)`；保留旧的 `../lro/include` 头文件路径兼容。
  - 调整 `rest_metrics.cpp`（plain text 分支）仅使用队列/槽位/在途与 states 统计，移除对旧 LRO 直方图与 completed 计数的依赖。
  - 调整 `rest_system.cpp` 预检 `global_concurrent` 使用 Runner 快照；去除对 `subscriptions` 的引用。
  - 调整 `rest_subscriptions.cpp` 的 LRO 路径：移除旧 `request/subscriptions` 引用；修正 ACL/配额使用 `uri/profile_id/requester_key`；GET 路径时间线改读 `op.timeline.*`；DELETE 路径用 Runner 快照写入会话完成；删除遗留 SSE 分支。
  - 停止后重建并启动 VA，健康检查 `/api/system/info` 成功。
- 结果：构建通过，VA 运行正常，LRO 编译库已接入最小路径。

2025-10-25 16:20
- 任务：统一 LRO 头文件后缀为 .h，并删除 .hpp。
- 动作：新增 `include/lro/{admission.h,metrics.h,reason.h,adapters/rest_simple.h}`；删除对应 `.hpp` 文件；更新 `lro/README.md` 与设计文档引用为 `.h`；全局确认 VA 无 `.hpp` 引用。
- 结果：工程可编译运行，接口与引用一致；后续可逐步增强 Runner 指标以恢复 /metrics 的 completed/histograms。

2025-10-25 16:35
- 任务：Runner 接入实际 Steps 并连通 Application。
- 动作：在 `rest_impl_core.cpp` 为 Runner 增加 Steps（prepare、start_pipeline）；在 `rest_subscriptions.cpp` 的 DELETE 路径补充对 Application 的取消（best-effort）。
- 结果：最小 API 测试通过：创建 202+Location，GET 返回 phase=ready，SSE 首条事件写入文件，DELETE 202；文件示例见 `%TEMP%/sse_<id>.txt`。

2025-10-25 16:45:59 提交：全量提交并推送至 origin/IOBinding
- 操作：git add -A && git commit && git push
- 结果：成功（分支 IOBinding）
- 影响：LRO 编译库接入与订阅迁移、/metrics 与 /system/info 更新、文档与脚本同步
- 后续：继续改造/测试前，若需构建请先停止 VA 进程

2025-10-25 17:06:02 任务：清理/构建并执行最小 API 测试
- 构建：通过（修复 rest_metrics.cpp 作用域与 <map> 依赖）
- 运行：启动 VA（8082）
- 测试：POST 202+Location，SSE 捕获成功；GET 间歇性超时；DELETE 因进程退出未验证
- 证据：video-analyzer/build-ninja/bin/logs/min_api_sse_camera_01:det_720p-1.txt
- 备注：进程在订阅流程中可能异常退出，需后续稳定性排查
2025-10-25 18:17:56 任务：LRO Admission 估算 + 清理 + 构建与最小测试
- 代码：新增 estimateRetryAfterSeconds；VA 改用 Admission 估算；删除 SubscriptionManager 源
- 构建：通过（vcvars+ninja）
- 测试：创建/查询/删除最小流转存在超时（可能与 RTSP 源或配额速率相关）
- 证据：logs/min_api_sse_*.txt
- 后续：按计划继续完善 Runner 观测/WAL 适配与脚本重试
2025-10-25 18:43:42 任务：VSM+VA 联动最小 API 测试
- 启动：VSM(7071)/VA(8082) 就绪
- VSM：/api/source/add camera_01 → 200；/api/source/list 存在 1 条（Ready）
- VA：POST /api/subscriptions → 202 + Location=/api/subscriptions/camera_01:det_720p-1（使用 X-API-Key=vip-observe, use_existing=1）
- SSE：已采集 phase=ready（证据：video-analyzer/build-ninja/bin/logs/min_api_sse_camera_01:det_720p-1.txt）
- GET/DELETE：期间 VA 进程有退出现象，GET/DELETE 一次失败（连接拒绝/超时）
- 建议：对 VA 进程退出做稳定性排查；测试脚本加入自动重启与退避
2025-10-25 20:24:31 任务：按计划推进（WAL 管理端点与脚本）
- 代码：保留 admin wal 端点（summary/tail），无新增接口变更
- 测试：新增脚本 check_admin_wal_tail.py 与 check_wal_scan.py；本地运行通过（wal 关闭时返回 SKIP）
- 备注：/metrics 已包含 va_wal_failed_restart_total；后续可扩展 wal 事件维度指标
2025-10-25 20:47:17 任务：WAL 指标与证据快照
- /metrics 保存：video-analyzer/build-ninja/bin/logs/metrics_snapshot.txt
- WAL 指标摘要：video-analyzer/build-ninja/bin/logs/wal_metrics_excerpt.txt
- system/info 保存：video-analyzer/build-ninja/bin/logs/system_info_snapshot.json
