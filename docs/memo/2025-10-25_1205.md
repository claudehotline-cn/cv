2025-10-25 12:05:00

- 修复构建：移除重复定义备份文件，解决链接报错 LNK1169/LNK2005
  - 删除：video-analyzer/src/server/_rest_subscriptions_backup.cpp（与 rest_subscriptions.cpp 重复）。
  - 重新配置并使用 Ninja 构建（tools/build_va_with_vcvars.cmd），构建通过。
- 快速验证：运行 smoke 用例集（tools/run_ci_smoke.ps1 -BuildMode local）
  - 结果：All smoke tests passed；WAL/Admin、Metrics/Headers、ETag/304、SSE、ACL/配额、失败路径均通过；日志与报告已生成到 video-analyzer/build-ninja/bin/logs。
- 提交与推送：已提交至分支 IOBinding 并推送到 origin。
- 下一步计划：
  - 将 REST 订阅路径切换到 LRO Runner（保持 202+Location、ETag/304、SSE 语义不变）。
  - 为 Runner 增补最小内存执行/步骤与 watch；随后删除 SubscriptionManager 并清理 CMake。
  - 对接 Runner metrics 快照，保持 WAL/预热/配额指标一致。

