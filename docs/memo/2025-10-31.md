

2025-10-31 01:19:59 修复检测框问题：
- 移除冗余日志：video-analyzer/src/analyzer/multistage/node_model.cpp: 输入张量日志
- YOLO 后处理：支持归一化坐标（环境开关 + 启发式），CPU/CUDA 路径一致
- ORT IoBinding：修复 stage_device_outputs 时的 FP16→F32 转换，避免半精度误读
- CUDA 后处理：FP16 路径保持（主机转换，后续可切换 device half_to_float）
- 已完成 Windows 构建：tools/build_va_with_vcvars.cmd 成功
- 后续：按需接入设备侧 half_to_float 快路径，长期改为每管线独立非阻塞流



2025-10-31 01:28:27 CUDA 后处理切换为设备侧 half_to_float + 纯设备解码：
- 修改 postproc_yolo_det.cpp：FP16 分支改为 half_to_float(d_in→d_tmp, stream) + yolo_decode_to_yxyx(d_tmp, ...)；移除主机 D2H/OH2D
- 修复 __half 作用域问题：将 cuda_fp16.h 移到命名空间外包含，避免符号为 cudaops::__half 导致链接失败
- 重编译 CUDA 内核库，并成功链接 VideoAnalyzer.exe
- 二进制：video-analyzer/build-ninja/bin/VideoAnalyzer.exe 已更新



2025-10-31 01:37:32 YOLO 解码内核支持归一化坐标：
- 修改 yolo_decode_kernels.[cu|hpp]：新增 pre_sx/pre_sy 入参，内核在解码前乘以预缩放因子
- 修改 postproc_yolo_det.cpp：设备路径移除归一化CPU回退，按 VA_YOLO_OUT_NORMALIZED 计算 pre_sx/pre_sy 并传入内核（FP16 先设备 half_to_float，再调用 float 解码核）
- 重建成功；无 Link 错误；二进制已更新

