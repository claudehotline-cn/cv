2025-10-26 05:10:04

任务：将 cp 控制流测试纳入 CI（tools/run_cp_smoke.ps1），并补充 metrics 校验脚本。

执行情况：
- 新增脚本：
  - `video-analyzer/test/scripts/get_cp_metrics.ps1`：抓取 /metrics，输出 cp_request_total 的 JSON。
  - `video-analyzer/test/scripts/check_cp_metrics_increment.ps1`：对比前后 JSON，按键（route|method|code）断言增量≥1。
- 集成 CI：更新 `tools/run_cp_smoke.ps1`：
  - 启动 VSM/VA（若未运行）。
  - 采集 metrics 前快照 → 运行 `cp_control_smoke.ps1` 与 `cp_control_hotswap.ps1` → 采集后快照并断言计数增量（含 hotswap 可选）。
  - 本地执行 `tools/run_cp_smoke.ps1` → PASS。

备注：保持 vcpkg 工具链，未使用 Anaconda；hotswap 默认使用 `build-ninja/bin/model/yolov12m.onnx`。

