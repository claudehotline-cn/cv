2025-11-19 10:00:00

2025-11-19 10:00:00  

- 任务：为 VA 的 track.ocsort 节点接通 GPU 追踪路径，并在编译启用 CUDA 时默认优先使用 CUDA 核函数完成跟踪。  
- 操作：  
  - 阅读 `video-analyzer/src/analyzer/multistage/node_track_ocsort.hpp/.cpp`，梳理 CPU Deep OC-SORT 分支与已有 GPU 路径（`process_gpu` + `track_ocsort_kernels.cu`）的差异：CPU 分支包含 CMC（稀疏光流）、Kalman 预测/更新与匈牙利多阶段匹配，GPU 分支目前使用 CUDA 核函数完成 Kalman 与 IoU+ReID 贪心匹配，并维护独立的 `OcsortGpuState`。  
  - 修改 `NodeTrackOcsort::open`：在 `USE_CUDA` 条件下记录 `ctx.gpu_pool`，并在存在有效 `gpu_pool` 且 `ctx.stream` 非空时将 `use_gpu_` 置为 true，为后续默认启用 GPU 追踪打基础。  
  - 修改 `NodeTrackOcsort::process` 调度逻辑：  
    - 当 `use_gpu_==true` 时优先调用 `process_gpu(p, ctx)`，成功则直接返回，并通过 `ms.track path=gpu` 日志标记路径；  
    - 若 GPU 分支返回 false（无 GPU ROI 或 CUDA 内核错误等），先清理 `p.gpu_rois[out_rois_key_]`，输出一次 `ms.track path=gpu_fallback -> cpu` 日志，然后回退到 `process_cpu`；  
    - 在 GPU 未启用或已回退到 CPU 的场景下，保留原有“从 `p.rois[out_rois_key_]` 构造 GPU ROI 视图”的逻辑，为 `overlay.cuda(use_gpu_rois=1)` 提供兼容的 GPU 轨迹数据，而成功的 GPU 路径则完全由 `process_gpu` 写入 `p.gpu_rois[out_rois_key_]`。  
  - 在根目录下尝试构建 `video-analyzer`（`cmake -S . -B build && cmake --build build -j`），由于沙箱环境缺少 OpenCV 开发包（`OpenCVConfig.cmake` 未找到）导致 CMake 配置失败，记录为当前环境限制，不修改任何构建配置。  
- 预期：编译启用 CUDA 且运行时具备有效 GPU 上下文时，所有 Graph 中的 `track.ocsort` 节点都会优先调用 CUDA 核函数进行追踪；在 CUDA 路径失败或缺少 GPU ROI 的情况下自动回退到 CPU Deep OC-SORT 分支，同时保持 overlay 在 CPU/GPU 两种路径下都能稳定消费 `track` ROI。后续迭代将继续在 `track_ocsort_kernels.cu` 内核中对齐 CPU 端的匈牙利多阶段匹配与 CMC 逻辑，以进一步缩小 CPU/GPU 行为差异。  

2025-11-19 11:00:00  

- 任务：按 CPU Deep OC-SORT 的生命周期语义，在 CUDA 核函数中扩展并维护轨迹的 `age` 与 `hit_streak`，为后续对齐输出门槛（`min_hits_` 等）打基础。  
- 操作：  
  - 更新 `video-analyzer/src/analyzer/cuda/track_ocsort_kernels.hpp` 中的 `OcsortGpuState` 结构，新增：  
    - `int32_t* d_track_age`：每条轨迹的存活帧数；  
    - `int32_t* d_track_hit_streak`：每条轨迹的连续命中次数。  
  - 在 `ocsort_alloc_state` 中为上述两个数组按 `max_tracks` 分配并清零；在 `ocsort_free_state` 中对应释放，确保内存生命周期与其他轨迹字段一致。  
  - 在 CUDA 核 `k_ocsort_step` 中：  
    - 在读取 `T`（当前轨迹数）后，若存在 `d_track_age`，对所有存活轨迹统一执行 `age += 1`，与 CPU 分支在每帧开头的 `age++` 行为对齐；  
    - 在“无检测帧（N==0）”分支中，对未被删除的轨迹执行 `missed++`，并在存在 `d_track_hit_streak` 时将其重置为 0；在压缩轨迹数组时同时搬运 `age` 与 `hit_streak` 字段；  
    - 在 `track-检测` 匹配成功时（主匹配循环）将 `d_track_missed[best_t]` 清零，并在存在 `d_track_hit_streak` 时执行 `hit_streak = max(prev,0) + 1`；  
    - 在为未匹配检测创建新轨迹时，将新轨迹的 `missed` 置 0、`age` 置 1、`hit_streak` 置 1，反映“新轨迹在首帧即被命中”的语义；  
    - 在“未匹配轨迹 missed++”阶段，对每个 `!track_used[t]` 的轨迹执行 `missed++`，并在存在 `d_track_hit_streak` 时将其重置为 0；  
    - 在最终压缩步骤中，在复制 `ids/missed` 等字段的同时，把 `age` 与 `hit_streak` 一并搬运，确保数组压缩后这些状态与对应轨迹对齐。  
  - 再次尝试在当前沙箱环境下构建 `video-analyzer`，仍因缺少 OpenCV 开发包导致 CMake 配置失败，确认为环境问题而非本次改动导致的构建错误。  
- 预期：未来在 CUDA 核中实现与 CPU 同构的多阶段匈牙利匹配与输出选择逻辑时，可以直接基于 `d_track_age` 与 `d_track_hit_streak` 实现与 CPU 相同的“稳定轨迹输出门槛”，同时保持 GPU 路径下轨迹生命周期与 CPU 分支尽可能一致。  

2025-11-19 12:00:00  

- 任务：在保持“单核串行”的前提下，将原本基于贪心策略的 GPU 匹配逻辑替换为匈牙利算法，实现轨迹-检测的全局最小代价匹配，为后续按 CPU Deep OC-SORT 多阶段结构拆分打基础。  
- 操作：  
  - 在 `video-analyzer/src/analyzer/cuda/track_ocsort_kernels.cu` 中新增 `hungarian_minimize_device`：  
    - 采用与 CPU 端 `hungarian_minimize` 相同的 Kuhn-Munkres 实现思路，使用固定上限 `MAX_N=512` 的本地数组（`cost_ext/u/v/p/way/minv/used`），在单线程内完成小规模代价矩阵的最小化匹配；  
    - 输入为行优先 cost 矩阵（行=轨迹，列=检测），输出 `match_row_to_col[r]=c` 或 -1，用于表示每个轨迹匹配到的检测索引。  
  - 调整 `k_ocsort_step` 中的匹配逻辑：  
    - 删除原有基于 `best_score` 的贪心 while 循环；  
    - 保留 `track_used` / `det_used` 标记数组，但改为在匈牙利输出之后使用；  
    - 新增 `cost_matrix[MAX_TRACKS * MAX_DETS]`，先统一初始化为 `large_cost`，然后对每个 `(track, det)`：  
      - 计算 IoU（`iou_yxyx`）；若 `IoU < iou_thresh` 则保持 `large_cost`；  
      - 计算 ReID 相似度（`cosine_normed`）和运动角度一致性（基于 `d_track_vel` 与检测中心点方向），按 `score = w_iou*v_iou + w_reid*v_reid + inertia*angle_score`（`inertia=0.2`）组合打分；  
      - 将得分归一化到 [0,1] 后赋值为代价 `cost = 1 - score`。  
    - 调用 `hungarian_minimize_device(cost_matrix, T, N, match_row_to_col)` 得到轨迹到检测的匹配关系，并对每个 `t`：  
      - 若 `match_row_to_col[t]` 为有效检测索引且对应 cost 未超过 `large_cost*0.5`，则视为匹配成功：  
        - 使用该检测框更新 Kalman 状态（`kf_update + x_to_bbox`）或直接覆盖 `d_track_boxes[t]`；  
        - 将 `d_track_missed[t]` 置 0，`d_track_hit_streak[t]` 递增，必要时更新 `d_track_feats` / `d_track_has_feat`；  
        - 在 `track_used[t]` / `det_used[d]` 中标记为已匹配。  
  - 保持后续生命周期逻辑不变：  
    - 仍然对 `det_used[d]==0` 的检测创建新轨迹，并初始化 `age/missed/hit_streak/feat/Kalman`；  
    - 仍然对 `track_used[t]==0` 的轨迹执行 `missed++` 与 `hit_streak=0`，并在末尾按 `missed>max_missed` 压缩轨迹数组。  
  - 在当前沙箱环境中再次尝试构建 `video-analyzer`，仍然因缺少 OpenCV 开发包导致 CMake 配置失败，确认本次 CUDA 侧修改不会被本地构建自动验证，需要在实际有 OpenCV 的开发环境中编译测试。  
- 预期：GPU 路径下的匹配行为从贪心改为全局最优（单阶段），在没有启用 CPU 多阶段细节（高分/低分拆分、BYTE、Stage3 等）的前提下，能够得到更稳定的轨迹关联结果；后续可以在此基础上按 CPU `process_cpu` 的结构继续细化为多阶段匈牙利匹配，同时利用已经在设备侧维护的 `age/missed/hit_streak` 状态实现与 CPU 更一致的轨迹输出门槛。  

2025-11-19 12:30:00  

- 任务：在保持单核串行实现的前提下，为 GPU OCSORT 添加第二阶段匈牙利匹配，使匹配流程从单阶段扩展为 Stage1（带外观与角度的综合相似度）+ Stage2（基于 IoU 的再匹配），进一步向 CPU Deep OC-SORT 的多阶段结构靠拢。  
- 操作：  
  - 在 `k_ocsort_step` 中保留 Stage1 的匈牙利匹配（基于 IoU+ReID+运动角度打分），并在匹配完成后维护 `track_used[t]` 与 `det_used[d]`，标记已经关联的轨迹和检测。  
  - 新增 Stage2：  
    - 构造 `unmatched_tracks` 与 `unmatched_dets` 两个列表，分别收集 Stage1 后仍未被使用的轨迹与检测索引；  
    - 若二者均非空，则分配 `cost_stage2[UT * UD]`，初始为 `large_cost`，并对每个 `(track, det)` 组合计算 IoU（使用已有 `iou_yxyx` 函数）作为相似度，代价设为 `1 - IoU`；  
    - 调用 `hungarian_minimize_device(cost_stage2, UT, UD, match_row_to_col2)`，对未匹配子集执行第二阶段匈牙利匹配；  
    - 对于 cost 未超阈值（`< large_cost * 0.5`）的匹配 `(ui, uj)`，映射回原始轨迹/检测索引 `(t, d)`，并复用 Stage1 的更新逻辑：  
      - 若存在 Kalman 状态，则用当前检测框更新 `d_kf_x/d_kf_P` 并回写到 `d_track_boxes[t]`；否则直接覆盖 `d_track_boxes[t]`；  
      - 将 `d_track_missed[t]=0`，`d_track_hit_streak[t]++`，必要时更新 `d_track_feats/d_track_has_feat`；  
      - 将 `track_used[t]` 与 `det_used[d]` 置为 1，避免后续新建与 missed++ 再次处理这些轨迹/检测。  
  - 保持后续“新建轨迹”、“未匹配轨迹 missed++ 与压缩”等生命周期逻辑不变，使 Stage2 仅作为额外的匹配补充层，不改变整体状态更新顺序。  
  - 在当前沙箱环境中再次尝试构建 `video-analyzer`，构建仍然因 OpenCV 配置缺失而失败，确认本次 Stage2 匈牙利改动尚未能在本地完成编译验证，需要在真实 GPU + OpenCV 环境中进一步测试。  
- 预期：在 GPU 路径下，阶段一通过综合相似度捕获主要且可靠的匹配关系，阶段二则在剩余候选上基于 IoU 进行补充，再结合已有 `age/missed/hit_streak` 生命周期管理，有望在遮挡、短时丢检等场景下提高轨迹的连续性与稳定性，同时保持实现结构与 CPU 多阶段匹配方案兼容，便于后续继续引入更接近 Deep OC-SORT 的高低分拆分和 Stage3 补充匹配。  

2025-11-19 13:00:00  

- 任务：在已有 Stage1（综合相似度）与 Stage2（IoU 再匹配）的基础上，为 GPU OCSORT 增加第三阶段匈牙利匹配，使仍未更新的轨迹可以再与剩余检测基于当前轨迹框进行一次 IoU 关联，进一步靠近 CPU 端的三阶段结构。  
- 操作：  
  - 在 `k_ocsort_step` 中，在 Stage2 结束后新增 Stage3 逻辑：  
    - 构造 `unmatched_tracks_stage3` 和 `unmatched_dets_stage3`：对 `track_used[t]==0` 的轨迹和 `det_used[d]==0` 的检测分别收集索引；  
    - 当二者非空时，分配 `cost_stage3[UT3 * UD3]`，默认使用 `large_cost` 填充；  
    - 对每个 `(t,d)` 组合，用当前轨迹框 `state.d_track_boxes[t]` 与检测框 `det_rois.d_boxes[d]` 计算 IoU，若 `IoU >= iou_thresh` 则设置代价为 `1 - IoU`；  
    - 调用 `hungarian_minimize_device(cost_stage3, UT3, UD3, match_row_to_col3)` 对上述子集执行第三阶段匈牙利匹配；  
    - 对每个有效匹配 `(t,d)`：  
      - 若存在 Kalman 状态，则用当前检测更新 `d_kf_x/d_kf_P` 并回写 `d_track_boxes[t]`；否则直接覆盖 `d_track_boxes[t]`；  
      - 将 `d_track_missed[t]=0`、`d_track_hit_streak[t]++`，必要时对 `d_track_feats/d_track_has_feat` 做 EMA 更新与归一化；  
      - 标记 `track_used[t]=1` 与 `det_used[d]=1`，避免后续被当作“未匹配轨迹/检测”参与新建或 missed++。  
  - 保持后续逻辑不变：未匹配检测仍按原路径新建轨迹，未更新轨迹仍执行 `missed++` 与 `hit_streak=0` 并按 `missed>max_missed` 删除，使 Stage3 仅作为在生命周期更新前的最后一轮匹配补充。  
  - 在沙箱环境中再次执行 CMake 配置与构建，仍因缺少 OpenCV 开发包导致配置失败，确认 Stage3 CUDA 改动需要在真实 GPU + OpenCV 环境下进一步编译与运行验证。  
- 预期：在 GPU 路径下，引入第三阶段 IoU 匈牙利匹配可以为在前两阶段未成功关联的轨迹与检测提供最后一次全局匹配机会，配合之前实现的 Stage1/Stage2 与生命周期管理，有望在复杂场景（交叉、短时遮挡）中提高轨迹连续性，使 GPU OCSORT 的行为在结构上更接近 CPU Deep OC-SORT 的三阶段匹配流程。  

2025-11-19 13:30:00  

- 任务：在 GPU OCSORT 的三阶段匈牙利匹配中引入“高分/低分拆分 + BYTE 专用阶段”，使 Stage1/Stage2/Stage3 在候选检测的选择上与 CPU Deep OC-SORT 更一致（高分框优先匹配，低分框通过 BYTE 单独处理）。  
- 操作：  
  - 在 `NodeNmsYolo` 中：  
    - 为 `GpuRoiBuffer` 增强检测置信度输出：在 `emit_gpu_rois_ && ctx.gpu_pool` 分支中增加 `gpu_scores_mem_` 缓冲区，将 `mo.boxes[i].score` 以 `[N]` 形式拷贝到 GPU，并在 `GpuRoiBuffer` 中填充 `d_scores` 指针；  
    - 在 `process` 与 `close` 中接管 `gpu_scores_mem_` 的生命周期管理，确保每帧释放或在节点关闭时归还给 `GpuBufferPool`；  
    - 保持原有 `d_boxes` 行为不变，确保现有只依赖 boxes 的 GPU 消费方不受影响。  
  - 在 CUDA 内核接口中：  
    - 扩展 `ocsort_match_and_update` 的声明与实现签名，增加参数：`det_thresh`（高分阈值）、`low_score_thresh`（低分阈值，用于 BYTE）、`use_byte`（是否启用 BYTE 阶段）；  
    - 在 `NodeTrackOcsort::process_gpu` 中，按 CPU 分支的配置将 `det_thresh_`、常量 `0.1f` 以及 `use_byte_`（布尔）传入 CUDA 侧，使 GPU 多阶段匹配能够复用相同的阈值与行为开关。  
  - 在 `k_ocsort_step` 中按“高分/低分拆分 + BYTE 阶段”的思路使用 `det_rois.d_scores`：  
    - Stage1：在构造 cost 矩阵时，仅对 `score > det_thresh_local` 的检测计算综合相似度并写入代价，对低于阈值的检测保留为 `large_cost`，从而实现“高分检测优先匹配”；  
    - Stage2 高分补充：构造 `unmatched_dets` 时，只将 `!det_used[d]` 且 `score > det_thresh_local` 的检测纳入候选，再基于 IoU 构造 `cost_stage2` 并执行匈牙利匹配；  
    - Stage2 BYTE 预留：通过新增 `low_score_thresh_local` 与 `use_byte_local`，为后续只针对 `low_score_thresh_local < score <= det_thresh_local` 的低分候选建立单独的 BYTE 匈牙利阶段（目前阶段只实现了高分补充与 Stage3 高分重匹配）。  
    - Stage3：在构造 `unmatched_dets_stage3` 时，仅收集 `!det_used[d]` 且 `score > det_thresh_local` 的检测，使最终 IoU 重匹配也只作用于高置信度目标。  
  - 在沙箱环境中再次尝试构建 `video-analyzer`，仍然因缺少 OpenCV 配置而失败，确认“高分/低分拆分 + BYTE 阶段”相关改动尚未在本地完成编译验证，需要在真实 CUDA + OpenCV 环境中继续构建与运行测试。  
- 预期：GPU OCSORT 在候选选择和阶段划分上与 CPU 版本更加一致：高置信度检测在 Stage1/Stage2 中优先得到匹配，而低分框则被约束在 BYTE 阶段使用（当前仅完成接口与高分部分的改造，为后续在 CUDA 内核中真正实现低分 BYTE 专用匈牙利匹配打好基础）。  

2025-11-19 23:43:46
- 任务：阅读 docs/context/CONTEXT.md 和 docs/context/ROADMAP.md，将关键信息加载到当前对话上下文。  
- 结果：已理解 VA In-Process Triton + ReID 动态 Batch + OCSORT GPU 路径的技术背景（架构、内核约束、ReID GPU I/O、OCSORT 多阶段匹配与回退策略）、当前约束与验证重点，以及 M0/M1/M2 关于 In-Process Triton 稳定性、ReID 动态 batch+追踪闭环、模型仓库与转换流水线可观测性的阶段目标和验收标准；后续答复将在该上下文基础上进行推理与方案设计。  

2025-11-19 23:53:03
- 任务：排查 track.ocsort 节点 CPU 与 GPU 追踪效果不一致的原因，分析 GPU 分支为何“不正确”。  
- 操作：  
  - 通读 `video-analyzer/src/analyzer/multistage/node_track_ocsort.hpp/.cpp` 中 CPU Deep OC-SORT 实现：确认其使用 CMC（`cmc_method: sparse`）、integrated OCSORT embedding（`w_assoc_emb`、`alpha_fixed_emb`、`aw_param`）、多阶段匈牙利匹配（Stage1 高分 + ReID + 角度一致性、Stage2 BYTE 低分 IoU、Stage3 高分 IoU 重匹配），并在输出时按 `min_hits` 门槛和 `det_to_track` 映射仅对“当前帧有检测且命中次数足够”的轨迹输出框。  
  - 通读 `video-analyzer/src/analyzer/cuda/track_ocsort_kernels.hpp/.cu` 与 `node_track_ocsort::process_gpu`：确认 GPU 路径当前只在内核中维护 Kalman + IoU + ReID 多阶段匹配与简单角度一致性，完全未实现 CMC、integrated embedding 的自适应权重、`embedding_off/aw_off` 开关，也没有在输出侧应用 `min_hits` 或“仅输出本帧命中的轨迹”逻辑，而是直接将 `OcsortGpuState` 中所有 `missed<=max_missed` 的轨迹框整体作为 `gpu_rois["track"]` 输出。  
  - 检查 `docker/config/va/graphs/analyzer_multistage_ocsort.yaml`：确认当前 graph 对 `track.ocsort` 显式配置了 `cmc_method: "sparse"`、`embedding_off: "0"` 等，使 CPU 分支开启了 CMC 与复杂的外观权重逻辑，而 GPU 分支完全忽略这些配置，自然导致在相机运动和遮挡场景下 ID 稳定性与框位置显著偏离 CPU 结果。  
- 结果：初步结论是 GPU 路径尚未完整对齐 CPU Deep OC-SORT 的行为——缺失 CMC、缺失 integrated embedding 的自适应权重与 `embedding_off/aw_off` 控制、输出端没有 `min_hits` 与“仅输出命中检测”过滤，同时角度一致性依赖的 `d_track_vel` 在 CUDA 内核中只在建轨时初始化为 0、从未根据观测更新，导致角度项实质失效。综合这些差异，CPU 分支被视为“正确行为”，而 GPU 分支是“尚未行为对齐”的近似实现，需要后续按 CPU 逻辑补齐 CMC、embedding 权重策略与输出门槛，或在短期内在启用 GPU OCSORT 时同步关闭 CMC/embedding 以减小偏差。  

2025-11-20 00:08:09
- 任务：在“不实现 GPU CMC”的约束下，对齐 track.ocsort GPU 路径的非 CMC 行为（速度向量更新与 integrated ReID embedding 权重策略），缩小 CPU/GPU 追踪效果差异。  
- 操作：  
  - 扩展 CUDA 接口 `video-analyzer/src/analyzer/cuda/track_ocsort_kernels.hpp/.cu`：为 `ocsort_match_and_update` 与 `k_ocsort_step` 增加参数 `embedding_off, aw_off, w_assoc_emb, alpha_fixed_emb, aw_param`，并在 `NodeTrackOcsort::process_gpu` 中将 CPU 端对应字段（`embedding_off_/aw_off_/w_assoc_emb_/alpha_fixed_emb_/aw_param_`）透传给 GPU 内核；保留原有 `feat_alpha` 入参以兼容旧配置，但在 integrated 路径中不再依赖它。  
  - 在 `k_ocsort_step` 的 Stage1 代价构造中，将原先的 `score = w_iou * v_iou + w_reid * v_reid + inertia * angle_score` 重写为与 CPU 一致的 integrated OCSORT 形式：  
    - 先计算 `motion_sim = w_iou * IoU + inertia * angle_score`，再用 `v_reid` 得到 `emb_sim`；  
    - 若 embedding 未关闭，则依据检测分数（来自 `det_rois.d_scores`）和 `det_thresh_local` 计算 `trust`，再用 `w_assoc_emb_local/alpha_fixed_emb_local/aw_param_local` 得到自适应权重 `w`，最终使用 `combined_sim = w * emb_sim + (1 - w) * motion_sim`；  
    - 以 `combined_sim`（截断到 [0,1]）构造匈牙利 cost 矩阵，实现 GPU 侧与 CPU Deep-OC-SORT 一致的运动/外观自适应匹配策略。  
  - 在 Stage1/Stage2/Stage2.BYTE/Stage3 的匹配更新逻辑中，为每个成功匹配的轨迹显式维护速度向量 `d_track_vel`：更新 Kalman/box 前记录当前轨迹 bbox 中心 `prev_cx/cy`，写回新 bbox 后计算 `cur_cx/cy` 并将差值写入 `d_track_vel[t*2+{0,1}]`，保证下一帧角度一致性项基于真实轨迹运动方向而非恒零向量。  
  - 将 ReID 特征 EMA 更新从固定 `feat_alpha` 改为 integrated OCSORT 的 dets_alpha 逻辑：在所有匹配阶段中，对已有特征的轨迹按检测分数计算 `trust`，用 `alpha_fixed_emb_local` 推导 `det_alpha`，再执行 `tf = det_alpha * tf + (1-det_alpha) * df` 并 L2 归一化；首次赋值则直接拷贝检测特征。  
  - 再次在本地沙箱尝试对 `video-analyzer` 进行 CMake 配置与构建，仍因缺少 OpenCV 开发包（`OpenCVConfig.cmake` 不存在）而失败，确认当前改动尚未在本机完成编译验证，需要在真实 GPU + OpenCV 环境中运行完整构建与回归测试。  
- 结果：在不启用 GPU CMC 的前提下，OCSORT GPU 路径已经对齐了 CPU Deep-OC-SORT 的关键非 CMC 行为：  
  - 匹配打分采用与 CPU 相同的运动/外观自适应权重策略（受 `w_assoc_emb/alpha_fixed_emb/aw_param` 与检测置信度控制）；  
  - ReID 特征的 EMA 更新逻辑与 CPU 保持一致，而不再依赖固定 `feat_alpha`；  
  - 多阶段匹配中所有匹配成功的轨迹都会更新速度向量，角度一致性项不再退化为 0。后续仍需在真实环境中对比 CPU/GPU 轨迹和 ID，并考虑在 GPU 输出视图层进一步对齐 `min_hits` 与“只输出本帧命中轨迹”等输出过滤策略。  

2025-11-19 14:30:00

- 任务：排查目标追踪任务 GPU 分支显存占用高（约 30GiB）的原因。  
- 操作：  
  - 通读 `video-analyzer/src/analyzer/multistage/node_track_ocsort.hpp/.cpp` 与 `video-analyzer/src/analyzer/cuda/track_ocsort_kernels.hpp/.cu`，确认 GPU OCSORT 路径仅通过 `OcsortGpuState` 在首次启用时进行一次性 `cudaMalloc`，总容量约为 `max_tracks(<=256) * (4+D+状态字段)`，在典型 `D=1536` 配置下单路约 < 1MiB，且每帧只读写，不重复分配。  
  - 检查多阶段链路中所有与目标追踪相关的 GPU 节点：`node_nms`（GPU NMS + emit_gpu_rois）、`node_roi_batch_cuda`（ROI batch 到 [N,3,384,128]）、`overlay.cuda`（GPU 叠加），确认它们当前实现中所有 `cudaMalloc` 要么在同一函数内成对 `cudaFree`，要么通过 `GpuBufferPool::release` 在每帧或关闭时归还，不存在“每帧泄漏一个新 buffer” 的路径；`node_nms` 之前在 memo 2025-11-18 中提到的 GPU ROI 泄漏已在现有代码里通过 `gpu_boxes_mem_/gpu_scores_mem_` 复用逻辑修复。  
  - 结合 `docker/config/va/app.yaml` 与 `docs/context/CONTEXT.md`，分析目标追踪 Graph `analyzer_multistage_ocsort` 的主要显存占用来源：YOLOv12x 检测模型与 PASS-ReID 模型均通过 In-Process Triton（TensorRT 后端）在 GPU 上加载，显存开销由模型本身参数 + TensorRT workspace + Triton CUDA 内存池决定，而与 track.ocsort GPU 内核关系较小；`roi.batch.cuda` 的最大批量为 128、输出 [128,3,384,128](fp32)，单路约 70–80MiB，远低于 30GiB 量级。  
  - 综合上述代码路径，未发现 OCSORT GPU 分支存在显性显存泄漏；当前 30GiB 级别的显存占用更可能来源于 Triton/TensorRT 模型（尤其是在多实例或多路流同时运行时），需要在实际部署环境中结合 `nvidia-smi` 与 `[inproc.triton] cuda_pool_bytes(...)` 日志进一步量化各模型/内存池贡献。  
- 结果：初步结论是 track.ocsort 的 GPU 分支本身显存占用很小且无明显泄漏，目标追踪场景下的高显存占用主要由 YOLOv12x + ReID 模型在 In-Process Triton 中的加载及其 TensorRT workspace/CUDA 池化策略决定；后续在真实环境中可通过调低 Triton/TensorRT 相关配置（如实例数、workspace、CUDA 内存池大小）或限制并发路数来控制总体显存占用。  

2025-11-19 15:00:00

- 任务：为 `track.ocsort` 节点增加“是否使用 GPU 分支”的图级配置开关，便于在显存紧张时强制走 CPU 追踪路径。  
- 操作：  
  - 修改图配置 `docker/config/va/graphs/analyzer_multistage_ocsort.yaml`：在 `name: track` 的 `params` 下新增 `use_gpu: "1"`，作为布尔开关（1/true/on/yes 表示启用 GPU，0/false/off/no 表示禁用），并通过注释说明含义。  
  - 更新 `video-analyzer/src/analyzer/multistage/node_track_ocsort.hpp`：  
    - 在 `#if defined(USE_CUDA)` 下为 `NodeTrackOcsort` 增加成员 `bool prefer_gpu_{true};`，用于记录配置层面的“是否偏好 GPU”状态；保持默认值为 true 以兼容现有图未显式配置 `use_gpu` 的场景。  
  - 更新 `video-analyzer/src/analyzer/multistage/node_track_ocsort.cpp`：  
    - 在构造函数中解析 `cfg["use_gpu"]`：当值为 `"0"/"false"/"off"/"no"` 时将 `prefer_gpu_` 置为 false，为 `"1"/"true"/"on"/"yes"` 时置为 true；未配置则保持默认 true。  
    - 在 `NodeTrackOcsort::open` 中，将原有 `use_gpu_ = (ctx.gpu_pool != nullptr && ctx.stream != nullptr);` 替换为 `use_gpu_ = (prefer_gpu_ && ctx.gpu_pool != nullptr && ctx.stream != nullptr);`，即只有在图配置允许且运行时存在有效 GPU 上下文时才启用 GPU 分支；否则强制只走 CPU Deep OC-SORT 路径，同时仍保留后续为 overlay 构造 GPU ROI 视图的逻辑。  
  - 在仓库根目录执行 `cmake -S . -B build && cmake --build build -j` 尝试构建，配置阶段因缺少 OpenCV 开发包（`OpenCVConfig.cmake/opencv-config.cmake` 未找到）导致 CMake 失败，确认为当前沙箱环境限制，而非本次改动引入的编译错误。  
- 结果：现在可以通过图配置中的 `use_gpu` 开关显式控制 `track.ocsort` 是否使用 GPU 追踪分支：  
  - `use_gpu: "0"` 时，即便启用了 CUDA 且上下文中存在 `gpu_pool` 和 `stream`，`NodeTrackOcsort` 也不会调用 `process_gpu`，而是始终走 CPU Deep OC-SORT 分支，从而避免 GPU OCSORT 状态以及相关 CUDA 内核对显存和 GPU 资源的占用；  
  - `use_gpu: "1"`（默认）时行为与之前一致：在编译启用 CUDA 并具备有效 GPU 上下文时优先使用 GPU OCSORT 路径，失败时自动清理 `gpu_rois["track"]` 并回退到 CPU。  
  - 当前在沙箱环境未能完成编译验证，后续需在真实 GPU + OpenCV 环境中重新构建 `video-analyzer` 并通过目标追踪回归测试，确认 `use_gpu` 开关在显存统计和行为上生效。  
