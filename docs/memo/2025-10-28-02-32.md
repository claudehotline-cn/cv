# 2025-10-28 02:32 任务日志

- 按“GPU零拷贝问题修复.md”线路A实现：新增 `va::core::ensure_cuda_ready()`（`video-analyzer/src/core/cuda_tls.hpp:1`），并在以下路径调用：
  - 预处理：`video-analyzer/src/analyzer/preproc_letterbox_cuda.cpp:1`
  - 推理 IoBinding：`video-analyzer/src/analyzer/ort_session.cpp:1`
  - 叠加渲染：`video-analyzer/src/analyzer/renderer_overlay_cuda.cpp:1`
- 目的：确保每个工作线程在首次触达 GPU 路径时完成 cudaSetDevice + cudaFree(0) 的线程局部初始化，避免零拷贝在 ORT IoBinding / NVDEC 路径出现设备未初始化/上下文不匹配。
- 已完成 Windows 增量构建；受限于当前会话无法启动进程，请在本机启动 VA/VSM 并执行：
  `python video-analyzer/test/scripts/check_gpu_inference.py --require-gpu --require-iobinding --require-device-binding --url rtsp://127.0.0.1:8554/camera_01`

