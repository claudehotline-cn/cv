2025-11-01 20:06 启动与最小取证

- 启动状态：VSM/VA/CP 已运行（保留原有实例），端口监听正常：7070/7071、8082、50051、18080。
- CP 列表接口：
  - GET http://127.0.0.1:18080/api/models → data=5
  - GET http://127.0.0.1:18080/api/pipelines → data=4
  - GET http://127.0.0.1:18080/api/graphs → data=3
- 前端（5173）分析页经 CP 完整链路：
  - POST /api/subscriptions → 202 { data.id:"cp-1761998565478-1" }, Location:/api/subscriptions/cp-1761998565478-1
  - POST /whep?stream=camera_01:det_720p&variant=overlay → 201（SDP），随后 ICE/patch 正常
  - <video> 播放：readyState=4, paused=false, size=1280x720
- 证据：
  - 截图：logs/analysis-playing.png
  - 关键请求（经 DevTools MCP 抓取）：/api/system/info、/api/models|pipelines|graphs、/api/subscriptions、/whep

后续：
- 可按需验证切换到 variant=raw（暂停态），确认暂停策略与画面切换；补充长稳播放（≥30min）观察与日志。

2025-11-01 20:16 暂停/实时分析开关与播放连续性

- system/info：pause_policy=pass_through，whep_default_variant=overlay
- 在分析页定位到“暂停/实时分析”开关（元素序号 idx=5，文本包含“暂停实时分析”），执行点击触发：
  - 看到序列：DELETE /api/subscriptions/<旧id> → POST /api/subscriptions → 202 新 id → POST /whep?...&variant=overlay → 201
  - 未见 /api/control/drain（符合 pass_through 策略）
  - 未出现 variant=raw 的 WHEP（当前策略默认保持 overlay）
- 播放器状态：readyState=4，paused=false，currentTime 连续递增；10s 性能采样完成，页面稳定。
- 结论：在 pass_through 策略下，切换不触发 raw，链路保持 overlay；如需验证“停止计算”语义，请将 CP 配置 sfu.pause_policy=stop 后重测，应出现 /api/control/drain 与 raw 变体。

2025-11-01 20:26 页面初始三次 WHEP 协商（overlay→raw→overlay）修复

- 根因：SSE phase=ready 与初始化路径在 setAnalyzing(true) 前后多处调用 updateWhepUrl，且 update 依据 analyzing 切换 variant，导致 variant 抖动。
- 修复：
  - 在所有 phase=ready 分支中先 setAnalyzing(true)，再设置 whepUrl（优先后端 whep_url，否则调用 updateWhepUrl）。
  - 为 updateWhepUrl 增加同值短路（nextUrl 与现有 whepUrl 相同则不更新）。
- 结果：打开分析页仅一次 WHEP 201（overlay）；未再出现 overlay→raw→overlay 连发。

2025-11-01 20:34 启用 stop 策略并验证 raw 变体

- 修改：controlplane/config/app.yaml → sfu.pause_policy=stop，并重启 CP。
- system/info：pause_policy=stop（已生效）。
- 操作：在分析页点击“暂停/实时分析”开关触发暂停。
- 取证（最小）：
  - DELETE /api/subscriptions/<旧id> → 202
  - POST /api/control/drain → 202
  - POST /api/subscriptions → 202（新 id）
  - POST /whep?…&variant=raw → 201（仅一次）
- 截图：logs/analysis-paused.png
