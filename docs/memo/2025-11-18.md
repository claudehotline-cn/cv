2025-11-18 10:00:00

- 调整 VA gRPC 服务器消息大小上限：
  - 修改 `video-analyzer/src/controlplane/api/grpc_server.cpp`，将 `ServerBuilder` 的 `SetMaxReceiveMessageSize` / `SetMaxSendMessageSize` 从 `256 * 1024 * 1024` 提升到 `1024 * 1024 * 1024`（约 1024MB），以支持大体积 ONNX/plan 工件的上传与转换。
  - 控制平面 gRPC 客户端已使用 `SetMaxReceiveMessageSize(-1)` / `SetMaxSendMessageSize(-1)`，无需调整。
  - 后续通过 Docker 构建 VA 镜像后，再次测试模型页的 `convert_upload` 流程，确认不再出现 `Received message larger than max (...)` 报错。

2025-11-18 12:50:00

- 优化 RepoConvertUpload 的 TensorRT 转换逻辑（通用化动态 batch 推断）：
  - 修改 `video-analyzer/src/controlplane/api/grpc_server.cpp` 中 `RepoConvertUpload`：
    - 新增 `TrtexecShapeHint` 与 `infer_shape_from_config`，从当前 `triton_repo`（S3/MinIO，`s3://...` + `AWS_ENDPOINT_URL_S3` 等环境变量）下的 `<model>/config.pbtxt` 中解析 `max_batch_size` 与首个 `input` 的 `name`/`dims`，不再尝试本地目录；与 MinIO 模型仓库部署保持一致。
    - 若 `max_batch_size>1` 且成功解析到输入名与 dims，则自动生成：
      - `--minShapes=<input>:1x...`
      - `--optShapes=<input>:min(max_batch_size,32)x...`
      - `--maxShapes=<input>:<max_batch_size>x...`
      并同时追加到 trtexec 命令字符串与 `execvp` 参数列表。
    - 若解析失败或 `max_batch_size<=1`，保持原有行为（不指定 shapes，由 trtexec 使用默认 profile）。
  - 预期效果：对所有在 `config.pbtxt` 中正确声明了 `max_batch_size` 与输入维度的模型，`convert_upload` 自动生成支持对应 batch 上限的 TensorRT plan，而不再依赖针对单个模型的硬编码。

2025-11-18 13:15:00

- 目标追踪 ReID 改用动态 batch 推理：
  - 更新 `docker/config/va/graphs/analyzer_multistage_ocsort.yaml`：
    - 删除 `reid` 节点的 `roi_seq_batch: "1"` 配置，保留 `triton_gpu_output: "1"`，使 ReID 模型直接以 `[N,3,384,128]` 动态 batch 方式在 Triton 中推理并输出 GPU 特征 `[N,1536]`。
  - 调整 `video-analyzer/src/analyzer/multistage/node_model.cpp`：
    - 将“无 ROI 输入视为空集合并跳过节点”的逻辑从 `roi_seq_batch_ && in_key_=="tensor:roi_batch"` 放宽为仅判断 `in_key_=="tensor:roi_batch"`，以便在不启用顺序推理的情况下，当本帧无 ROI 时 ReID 节点仍视为成功且不报错。
  - 预期效果：目标追踪场景下 ReID 不再逐 ROI 循环推理，而是一次性处理最多 128 个 ROI；无检测框时流水线保持稳定，不会因为缺少 `tensor:roi_batch` 而导致节点失败。

2025-11-18 13:30:00

- 收紧 In-Process Triton DebugSeg 日志：
  - 修改 `video-analyzer/src/analyzer/triton_inproc_session.cpp`：
    - 将 `TritonInprocModelSession::run` 内所有 `[DebugSeg]` 相关日志改为使用 `VA_LOG_THROTTLED(log_level_for_tag("inproc.triton"), log_throttle_ms_for_tag("inproc.triton"))`，包括：
      - 入口 `TritonInproc::run enter`；
      - `InferenceRequest prepared`；
      - `ServerInferAsync dispatched`；
      - `OutputCount` 与逐个 `ResponseOutput[...]`。
    - 默认情况下，这些日志不再以 Info 级别每帧打印，而是按日志配置节流；需要详细排查时，可通过调整 `inproc.triton` 日志级别与节流窗口临时打开。
  - 预期：在开启目标追踪 + ReID 时，`inproc.triton` 不再刷满控制台，保留必要信息同时减少噪音与 IO 开销。

2025-11-18 15:00:00

- 任务：排查 OCSORT 目标追踪路径中追踪框不准确与 GPU 显存持续增长的问题，并给出初步修复。
- 操作：
  - 阅读 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md`，确认当前 OCSORT 多阶段 Graph（`analyzer_multistage_ocsort.yaml`）及 GPU 零拷贝设计的意图：`pre → det → nms → roi.batch.cuda → reid → track.ocsort → overlay.cuda`，其中 NMS 节点在开启 `emit_gpu_rois: "1"` 时应通过 `p.gpu_rois["det"]` 向下游 GPU 追踪与 overlay 传递检测框。
  - 检查 `video-analyzer/src/analyzer/multistage/node_nms.cpp/.hpp` 实现，发现 GPU ROI 输出路径中通过 `ctx.gpu_pool->acquire(...)` 为 `[N,4]` boxes 申请显存后，仅将裸指针写入 `GpuRoiBuffer`，但从未通过 `GpuBufferPool::release` 或其他方式归还，导致每帧新增一个未追踪的 device buffer，长时间运行时 GPU 显存单调上升。
  - 对比 `NodeRoiBatchCuda` 的实现，确认其通过 `staged_` + `GpuBufferPool::release` 在每帧处理前释放上一帧的 ROI batch 缓冲，是当前唯一正确实现了“帧级显存复用”的节点；据此为 NMS 节点补齐类似的生命周期管理。
  - 修改 `video-analyzer/src/analyzer/multistage/node_nms.hpp`：
    - 引入 `core/gpu_buffer_pool.hpp`，在 `NodeNmsYolo` 中新增成员 `va::core::GpuBufferPool* gpu_pool_{nullptr};` 与 `va::core::GpuBufferPool::Memory gpu_boxes_mem_;`，用于跟踪当前管线中为 `gpu_rois["det"]` 分配的 GPU boxes 缓冲。
    - 声明并实现 `close(NodeContext& ctx) override;`，在 Graph 关闭时归还或释放残留的 GPU 缓冲，避免管线销毁阶段遗留显存。
  - 修改 `video-analyzer/src/analyzer/multistage/node_nms.cpp`：
    - 在 `NodeNmsYolo::process` 开头，当 `gpu_boxes_mem_.ptr` 与 `gpu_pool_` 非空时调用 `gpu_pool_->release(std::move(gpu_boxes_mem_)); gpu_boxes_mem_ = {};`，保证每一帧开始前先归还上一帧用于 GPU ROI 的缓冲。
    - 在 `emit_gpu_rois_ && ctx.gpu_pool` 分支中，将原来的局部 `mem = ctx.gpu_pool->acquire(...)` 改为：
      - 记录 `gpu_pool_ = ctx.gpu_pool;`；
      - 若已有 `gpu_boxes_mem_` 但容量不足当前 `elems * sizeof(float)`，先通过 `gpu_pool_->release(std::move(gpu_boxes_mem_));` 归还旧块，再重新 `acquire` 更大的块；
      - 若当前没有缓存，则直接通过 `gpu_pool_->acquire(...)` 获得 `gpu_boxes_mem_`；
      - 使用 `gpu_boxes_mem_.ptr` 作为 `GpuRoiBuffer.d_boxes` 的来源，并通过 `cudaMemcpyAsync` 将 CPU `mo.boxes` 一次性拷贝到该缓冲中，写入 `p.gpu_rois["det"]`。
    - 在新增的 `NodeNmsYolo::close` 中，若仍持有 `gpu_boxes_mem_`：
      - 优先通过 `gpu_pool_->release(std::move(gpu_boxes_mem_));` 归还到池中；
      - 若 `gpu_pool_` 意外为空，则作为兜底直接调用 `cudaFree(gpu_boxes_mem_.ptr);` 释放显存，随后将指针与池指针清空。
  - 构建：尝试在当前容器中执行 `cmake -S . -B build && cmake --build build -j`，CMake 在 `video-analyzer/CMakeLists.txt` 的 `find_package(OpenCV)` 步骤因缺少本地 OpenCV 开发包（无 `OpenCVConfig.cmake` 或 `opencv-config.cmake`）而失败，无法在本环境完成完整构建与链接验证。
- 预期：
  - 在实际部署环境（已安装 OpenCV 与 CUDA）的 VA 进程中，开启 `emit_gpu_rois: "1"` 的 OCSORT 图长时间运行时，`nvidia-smi` 中显存使用不再随帧数单调增长，而是稳定在由 `GpuBufferPool` 管理的固定上限附近。
  - GPU 追踪路径（`track.ocsort` + `overlay.cuda(use_gpu_rois=1)`）的几何行为未改变：`gpu_rois["det"]` 仍然按 NMS 后的 boxes 顺序将 `[N,4]` 坐标传递给 OCSORT 内核，`GpuRoiBuffer.count` 不变；追踪框不准确的问题若仍存在，更可能与上游 YOLO 检测质量、ReID 动态 batch 配置或 OCSORT 参数（`iou_thresh`/`w_iou`/`w_reid` 等）有关，后续需要在有真实流的环境下结合日志与可视化进一步调参与定位。

2025-11-18 15:30:00

- 任务：针对 OCSORT GPU 路径下“目标离开画面后仍长时间保留轨迹框”的观感问题，减少画面上的“幽灵追踪框”数量。
- 操作：
  - 分析 `NodeTrackOcsort` 的 CPU/GPU 双路径行为：
    - CPU 路径（`process_cpu`）在 `video-analyzer/src/analyzer/multistage/node_track_ocsort.cpp:121` 起始处，当本帧缺少 `p.rois[in_rois_key_]` 时，会仅对内部 `tracks_` 做 `missed++` 与过期轨迹删除，并执行 `p.rois.erase(out_rois_key_);`，即“无检测帧不输出任何轨迹框”。
    - GPU 路径（`process_gpu`）则始终通过 `ocsort_match_and_update` 更新 `gpu_state_`，并在函数尾部无条件构造 `GpuRoiBuffer buf` 指向 `gpu_state_.d_track_boxes/d_track_ids`，写入 `p.gpu_rois[out_rois_key_]`。在 `gdet.count<=0` 时，这会导致即使当前帧没有任何检测框，仍然把上一帧乃至更早的轨迹框画到画面上，用户看到的效果类似“旧帧残留的追踪框”。
  - 修改 `video-analyzer/src/analyzer/multistage/node_track_ocsort.cpp` 中 `NodeTrackOcsort::process_gpu` 的“无检测”分支：
    - 当 `p.gpu_rois` 中存在输入 ROI 但 `gdet.d_boxes==nullptr` 或 `gdet.count<=0` 时：
      - 若 `gpu_state_ready_` 为假：仅执行 `p.gpu_rois.erase(out_rois_key_); p.rois.erase(out_rois_key_);` 并返回 true，确保本帧不输出任何 track ROI。
      - 若 `gpu_state_ready_` 为真：照旧调用 `ocsort_match_and_update` 更新内部 GPU 轨迹状态（`missed++` 与超时删除），但在成功后立即执行 `p.gpu_rois.erase(out_rois_key_); p.rois.erase(out_rois_key_);` 并返回 true，不进入后面的“构造输出 GpuRoiBuffer”逻辑。
    - 保持 `gdet.count>0` 的分支逻辑不变：仍然在 GPU 上完成匹配与轨迹更新，并构造 `p.gpu_rois[out_rois_key_]` 指向 `gpu_state_` 中的轨迹 boxes/ids，由 `overlay.cuda(use_gpu_rois=1)` 直接消费。
- 预期：
  - 当画面上暂时没有任何检测框（例如目标完全离开视野或 YOLO 在个别帧漏检）时，当前帧不再绘制“历史轨迹”的残留框，视觉上不会再出现大量“悬空在背景上的追踪框”。
  - 内部 OCSORT 轨迹状态仍然会通过 `ocsort_match_and_update` 正常维护（`missed` 计数与过期删除），只是不在“无检测帧”对外暴露，可在后续需要时通过更细粒度的 GPU 输出（例如增加 per-frame 可见标记）再迭代改进。

2025-11-18 15:45:00

- 任务：在保留 GPU 零拷贝 OCSORT 路径的前提下，修正 GPU 路径失败回退 CPU 时残留的 GPU 轨迹框问题。
- 操作：
  - 撤销临时关闭 GPU 路径的改动，还原 `NodeTrackOcsort::open` 逻辑为“仅在 `ctx.gpu_pool` 与 `ctx.stream` 均有效时启用 GPU 路径”：
    - 若存在有效 GPU 上下文，则 `use_gpu_ = true`，否则保持 `use_gpu_ = false`，便于在不支持 CUDA 的构建中自动回退到纯 CPU 行为。
  - 调整 `video-analyzer/src/analyzer/multistage/node_track_ocsort.cpp` 中 `NodeTrackOcsort::process` 的 GPU 回退路径：
    - 当 `use_gpu_` 为真且 `process_gpu(p, ctx)` 返回 false 时，之前仅打印一条 `gpu_path_failed -> fallback cpu` 日志并直接调用 `process_cpu`；
    - 现在在记录日志前先执行 `p.gpu_rois.erase(out_rois_key_);` 清理当前帧可能残留的 `gpu_rois["track"]` 视图，避免 overlay 在回退到 CPU 逻辑时仍优先使用已有的 GPU ROI，导致画面上绘制出“上一次 GPU 成功帧”的旧轨迹框。
  - 保持 `process_gpu` 成功路径的行为不变：正常更新 `gpu_state_`，从中构造 `p.gpu_rois[out_rois_key_]` 并清空 `p.rois[out_rois_key_]`，继续走 GPU 零拷贝叠加链路。
- 预期：
  - 在 GPU 路径偶发失败（如 `ocsort_match_and_update` 返回错误或 `cudaMemcpy` 失败）时，当前帧不会再用上一帧遗留的 GPU 轨迹数据进行叠加，画面上不会出现“卡住不动的旧轨迹框”；而是完全由 CPU 路径重算并输出新的 `rois["track"]`，由 overlay 正常绘制。
  - 在正常情况下（GPU 路径成功），仍通过 `gpu_rois["track"]` + `overlay.cuda(use_gpu_rois=1)` 完成零拷贝绘制；日志中的 `ms.track path=gpu` 代表本帧走的是 GPU 追踪。

2025-11-18 16:10:00

- 任务：让 `track.ocsort` 更接近 OCSORT 思路，实现简单的“速度预测 + 遮挡下重识别”行为，同时保持 GPU 零拷贝路径。
- 操作：
  - CPU 路径（`NodeTrackOcsort::process_cpu`）：
    - 扩展 `Track` 结构（`video-analyzer/src/analyzer/multistage/node_track_ocsort.hpp`）：
      - 新增字段：`age`（轨迹存活帧数）、`hit_streak`（连续命中次数）、`vx`/`vy`（简单速度估计，像素/帧）。
    - 在无检测帧分支中（`p.rois[in_rois_key_]` 不存在）：
      - 将所有轨迹标记为 `updated=false`，`hit_streak=0`，`missed++`，`age++`，并按 `missed>max_missed_` 删除，继续保持“无检测帧不输出任何轨迹框”的特性。
    - 在有检测帧分支中：
      - 预先为每个轨迹计算基于 `vx`/`vy` 的预测框 `pred_boxes[ti]`（常速模型：在 `cx,cy` 上加一次 `vx,vy`，宽高不变），用这个预测框与检测做 IoU gating 与匹配得分计算，而不再直接用上一次观测框：
        - 匹配得分仍为 `score = w_iou * IoU(pred_box, det) + w_reid * cosine(ReID)`，进一步提升在遮挡与中短期运动下的关联稳定性。
      - 在接受匹配时（贪心匹配阶段）：
        - 在更新 `tracks_[m.t].box = dets[m.d]` 之前，根据旧框与新检测框的中心差计算速度：  
          `vx = new_cx - prev_cx; vy = new_cy - prev_cy;`，写入 `Track.vx/vy`，同时重置 `missed=0`，`updated=true`，`hit_streak++`。
      - 为新建轨迹（未匹配检测）初始化：
        - `age=1`，`hit_streak=1`，`vx=vy=0`，其余逻辑（ReID 特征 EMA + L2 归一）保持不变。
      - 在匹配完成后的“未匹配轨迹”更新阶段：
        - 对 `!updated` 的轨迹执行 `missed++` 且 `hit_streak=0`；
        - 同时将其 `box` 推进到预测位置（基于当前 `vx,vy` 与上一次 box 几何）以便下一帧继续使用预测位置参与 IoU gating，实现“遮挡期间的轨迹预测”：
          - 用当前 `box` 的 `cx,cy,w,h` 加上 `vx,vy` 得到 `pcx,pcy`，再回写为新的 `[x1,y1,x2,y2]`。
      - 删除逻辑保持为 `missed>max_missed_` 时从 `tracks_` 中移除，从而形成“有限长度的 occlusion 窗口”，在窗口内可以依靠速度预测再识别（再关联）目标，超出窗口则自然丢弃。
  - GPU 路径（`track_ocsort_kernels`）：
    - 扩展 GPU 轨迹状态结构 `OcsortGpuState`（`video-analyzer/src/analyzer/cuda/track_ocsort_kernels.hpp/.cu`）：
      - 新增 `float* d_track_vel; // [T_max,2]` 用于存储每条轨迹的简单速度 `(vx,vy)`。
      - 在 `ocsort_alloc_state` 中为 `d_track_vel` 分配零初始化缓冲；在 `ocsort_free_state` 中释放。
    - 在 CUDA kernel `k_ocsort_step` 中：
      - 在计算每个 `(track, det)` 的匹配分数前，为当前轨迹 `t` 构造预测框 `pb`：
        - 从 `d_track_boxes` 读取当前 box 几何，计算 `cx,cy,w,h`，从 `d_track_vel[t]` 读取 `(vx,vy)`，得到预测中心 `pcx=cx+vx, pcy=cy+vy`，并构造预测框 `pb`；
        - 使用 `iou_yxyx(pb, db)` 作为 IoU 部分，结合 ReID 特征计算匹配得分。
      - 在接受某个 `(best_t,best_d)` 匹配时：
        - 在覆盖 `state.d_track_boxes[best_t]` 为检测框之前，根据旧 box 与新检测框的中心坐标差更新 `d_track_vel[best_t]`（`vx=new_cx-prev_cx`，`vy=new_cy-prev_cy`），与 CPU 路径保持一致。
      - 在为未匹配的检测创建新轨迹时，将 `d_track_vel[t]` 初始化为 `(0,0)`。
      - 在两个压缩阶段（无检测帧的 `N==0` 分支和最终删除 `missed>max_missed` 的压缩循环）中，除复制 `ids/missed/has_feat/feats/boxes` 外，同步复制 `d_track_vel`，保证压缩结果中的轨迹速度与其它状态一一对应。
- 预期：
  - 对于有明显运动的目标，轨迹在短时间遮挡或轻微漏检后更容易“接回原来的 ID”，而不是频繁换 ID 或在物体重现时立即开新轨迹；
  - GPU 与 CPU 路径在匹配与速度更新策略上保持一致，只是 CPU 在调试时更易观察和调整参数，GPU 路径负责在零拷贝链路中高效更新轨迹框并提供 `gpu_rois["track"]` 给 `overlay.cuda` 使用；
  - 在当前环境缺少 OpenCV 开发包的前提下，本地无法完成编译与单元测试，但从代码结构与 CPU/GPU 逻辑一致性来看，改动是局部且自洽的，后续可在实际构建环境中通过长时间压测验证行为与性能。

2025-11-18 16:20:00

- Deep OC-SORT + CMC 整合与编译修复：
  - 在 `video-analyzer/src/analyzer/multistage/node_track_ocsort.hpp/.cpp` 中补齐 Deep OC-SORT 对齐实现：
    - 引入 7 维 Kalman 滤波器（状态 `[cx,cy,s,r,vx,vy,vs]`，观测 `[cx,cy,s,r]`），`F/H/Q/R/P` 参数与 Python 项目中的 `KalmanFilterNew` 对齐，在每帧匹配前对所有轨迹进行预测，在关联成功后执行更新。
    - 完成三阶段匈牙利匹配流程：Stage1 使用高分检测 + Kalman 预测框 + IoU/角度 cost + ReID 余弦相似度，结合 trust/dets_alpha/w_association_emb/aw_param 融合代价；Stage2 采用 BYTE 风格对低分检测进行二次关联；Stage3 基于当前轨迹框与高分剩余检测做补充匹配。
    - 为轨迹添加观测 ring buffer，用于近似 Python 中的 `k_previous_obs` 逻辑，在角度代价计算时考虑历史观测与当前预测之间的方向一致性。
  - 集成 CMC（Camera Motion Compensation）稀疏光流校正：
    - 在 `node_track_ocsort.hpp` 中将 `CmcState` 定义为命名空间级结构体，包含上一帧灰度图与稀疏角点集合；在 `NodeTrackOcsort` 中以 `std::unique_ptr<CmcState>` 成员持有。
    - 在 `node_track_ocsort.cpp` 的匿名命名空间中通过 `using CmcState = va::analyzer::multistage::CmcState;` 引入类型别名，并实现 `cmc_compute_sparse`：使用 `goodFeaturesToTrack + calcOpticalFlowPyrLK + estimateAffinePartial2D(RANSAC)` 计算 2x3 仿射矩阵，将上一帧坐标系对齐到当前帧。
    - 在 `process_cpu` 开头根据 CMC 配置对所有在轨轨迹的 `box/last_obs/observations` 应用仿射变换，使后续匹配逻辑与 Python 项目的稀疏 CMC 模式保持一致。
  - 为保持 GPU 零拷贝叠加：
    - 在 CPU 完成 Deep OC-SORT 匹配并生成 `rois["track"]` 后，从 `NodeContext.gpu_pool` 中申请固定大小的 GPU 缓冲，将轨迹框坐标与 track_id 通过 `cudaMemcpyAsync` 写入 device，再以 `GpuRoiBuffer` 的形式填充 `p.gpu_rois["track"]`，供 `overlay.cuda` 直接在设备上的 NV12 帧上绘制，无需额外的 ROI H2D 拷贝。
  - 编译修复：
    - 修复 CMC 集成过程中引入的命名与可见性问题：将原本类内私有的 `CmcState` 移动到命名空间作用域，并在 `node_track_ocsort.cpp` 中通过类型别名使用，避免出现“`CmcState` is private within this context” 与 “`CmcState` was not declared in this scope” 等错误。
    - 在 CMC 实现中显式包含 `<opencv2/calib3d.hpp>`，解决 `cv::estimateAffinePartial2D` / `cv::RANSAC` 未声明导致的编译失败。
    - 经 Docker 环境下重新构建 GPU 版镜像（`Dockerfile.gpu`），确认 `NodeTrackOcsort` 相关编译错误已消失，VideoAnalyzer 目标顺利通过构建。
