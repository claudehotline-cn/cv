2025-11-01 21:30

- 修复：暂停开关无效仍显示叠加框（Analysis 页面）。
  - 定位到 VA 仅在 VA_DISABLE_HTTP_PUBLIC=ON 时才暴露 `POST /api/control/pipeline_mode`；开启公开 HTTP 时该路由缺失，导致 CP 代理 404，前端吞错后仍显示 overlay。
  - 变更：在公开 HTTP 分支同样注册 `POST/OPTIONS /api/control/pipeline_mode`（video-analyzer/src/server/rest_routes.cpp）。
  - 构建：使用 tools/kill_running.ps1 + tools/build_va_with_vcvars.cmd 编译通过。
  - 编码器：修正 FFMPEG 路径的 I 帧强制逻辑，去除对 AVFrame.key_frame 的访问（该字段不存在），保留 pict_type=I 提示；`requestKeyframe()` 通过私有标志实现。
  - 预期行为：前端切换暂停时仅调用 `setPipelineMode`，WHEP 不重建；VA 在 `analysis_enabled=false` 路径绕过分析/overlay，使用相同 track 发送原始帧。

- 下一步建议：
  - 前端 Network 观测确认切换时仅出现一次 `POST /api/control/pipeline_mode`（200），无新增 `POST /whep`；
  - VA 日志验证：暂停后 `ms.overlay drawn boxes=...` 停止刷新，仍有 `WebRTC enqueue frame`；恢复实时分析后首帧 IDR。

