2025-11-01 01:25

- 任务：确保 VA gRPC 监听、经 CP 订阅 + WHEP、清理前端直连并取证。

- 操作与结果：
  - 检查监听：50051/8082（VA）、18080（CP）均在监听。
  - 修复 CP 订阅路由 URL 解码：`/api/subscriptions` 解析 query 时新增 `percent-decode`，避免 VA 打开 `rtsp%3A...` 造成 RTSP 失败。
  - 统一返回体：`POST /api/subscriptions` 现返回 `{ code:"ACCEPTED", data:{ id:"<cp_id>" } }`，并设置 `Location: /api/subscriptions/<cp_id>`。
  - 构建与重启 CP：使用 `tools/build_controlplane_with_vcvars.cmd` 成功编译并启动。
  - 取证：
    - `GET /api/system/info` → 200 OK。
    - `GET /api/models|/api/pipelines|/api/graphs` → 200（len: 5/4/3）。
    - `POST /api/subscriptions?...source_uri=rtsp%3A...` → 202，`Location=/api/subscriptions/cp-...`，JSON `data.id=cp-...`。
    - `GET /api/subscriptions/{id}/events` → 200，`Content-Type: text/event-stream`。
  - 前端直连排查：在 Dashboard 仍见对 `http://127.0.0.1:8082/{metrics,api/events/recent}` 的请求。
    - 已修改前端 dev 基址策略：在 dev 强制使用相对路径，经 Vite 代理统一走 CP（改动文件：`src/components/observability/MetricsSummary.vue`、`MetricsDbPanel.vue`、`src/api/cp.ts`）。
    - 建议重启 `npm run dev` 以应用新 env/HMR，确认不再触达 8082。

- 影响与后续：
  - VA 端 RTSP 源应以解码后的 `rtsp://127.0.0.1:8554/camera_01` 打开，不再出现 `cv::VideoCapture open failed for rtsp%3A...`。
  - 如仍无画面，请在前端 Analysis 页发起订阅，观察 WHEP POST 201 + Location 与后续 ICE PATCH 204；若无协商日志，排查是否仍有直连路径。

 - 变更列表：
  - controlplane/src/server/main.cpp（订阅 query 解码、返回体包含 data.id）。
  - web-front/src/components/observability/MetricsSummary.vue（dev 基址=相对路径）。
  - web-front/src/components/observability/MetricsDbPanel.vue（dev 基址=相对路径）。
  - web-front/src/api/cp.ts（dev 基址=相对路径）。

- 阈值来源切换到图配置（multistage YAML）
  - 从 `config/graphs/analyzer_multistage_example.yaml` 的 post.yolo.nms 节点读取 `conf` 与 `iou`（例如 conf=0.65, iou=0.45）。
  - NodeNmsYolo 将其注入进程环境（`VA_CONF_THRESH`, `VA_NMS_THR`），后处理（CPU+CUDA）每次调用实时读取。
  - 代码：`video-analyzer/src/analyzer/postproc_yolo_det.cpp`（新增 getScoreThreshold/getNmsThreshold，移除静态缓存，GPU/CPU NMS 使用动态阈值）。
  - 取证：`logs/video-analyzer-release.log` 出现 `gpu_decode.pre ... thr=0.65`。
## 2025-11-01 现网验证（追加）
- 操作：重启 CP（应用 GET /api/subscriptions/{id} 兼容 include=timeline 修复），保持前端 dev 运行。
- DevTools 取证：
  - 订阅创建：POST /api/subscriptions → 202，返回 data.id 与 Location。
  - 资源查询：GET /api/subscriptions/{id}?include=timeline → 200，返回 { id, phase, created_at, pipeline_key, timeline }。
  - 未见 /whep 请求：前端仍未发起协商，当前页面还有直连 8082 的调用（metrics、events）。
- 建议：
  - 前端去除 8082 直连，统一经 CP；
  - 若前端以 SSE ‘ready’ 作为 WHEP 启动门槛，则需在 CP 实现 VA Watch → SSE 映射；或临时放宽前端门槛，使用资源 GET 的 phase=ready 触发。

## 2025-11-01 DB 列表接口修复（追加）
- 现象：分析页 models/pipelines/graphs 为空。
- 根因：CP 运行时配置缺少 `db` 段，`/_debug/db` 显示 cfg 为空（未配置 host/port/schema），CP 回退为空数组。
- 修复：补齐运行时配置 `controlplane/build/bin/config/app.yaml` 的 `db` 段（host=127.0.0.1, port=13306, user=root, password=123456, schema=cv_cp），重启 CP。
- 取证：
  - GET /api/models → 5 条；/api/pipelines → 4 条；/api/graphs → 3 条。
  - `/_debug/db` 返回 cfg 正确、errors=null。
- 建议：刷新前端（强刷新）后应看到非空数据；若仍为空，检查前端是否误用 mock 或缓存旧 base。
2025-11-01 04:35:42  前端管线编辑器：YAML 导出与竖向工具条
- 变更：重建 GraphEditorCanvas.vue（X6 画布、DnD、仅 out→in 连线、分组端口）；新增左侧悬浮竖向工具条（预处理/模型/后处理），移除 source/sink；Editor 导出 YAML 优先 yamlType，格式对齐 analyzer_multistage_example.yaml。
- 验证：vite build 通过；未改动后端接口。
- 后续：联调属性表单字段与新类型（preproc/model/post/overlay），补充 E2E 导出→CP apply 流程测试。
2025-11-01 04:40:06  前端编辑器贴边/抽屉默认收起
- 变更：Row gutter=0，去除列内边距；画布去圆角；工具条位置更贴边；抽屉默认关闭（保留FAB触发）。
- 验证：vite build 通过；布局横纵向均铺满。
2025-11-01 12:09:37  重建 CONTEXT/ROADMAP
- 变更：重写 docs/context/CONTEXT.md（体系结构、CP/WHEP、VA 框修复、前端编辑器、DB 依赖、测试与风险）；生成 ≤800 词 docs/context/ROADMAP.md（M0/M1/M2、阶段表、依赖矩阵、Top-5 风险）。
- 说明：替换原有乱码文本，统一中文 UTF-8；不改动代码逻辑。



# 2025-11-01 12:41:19 联调与测试
- 构建：已存在 VA/VSM/CP 可执行文件
- 启动：VSM/VA/CP 已启动 (PIDs 如上)
- 端口：7070/7071/8082/18080 (VA/REST/CP) 监听正常
- 订阅：POST /api/subscriptions 返回 202 + data.id，Location=/api/subscriptions/{id}
- VSM：/api/source/add OK（camera_01）
- WHEP：/whep OPTIONS 200，CORS 允许
- VA：/metrics 200；/api/system/info 410(Unknown) 需排查（但 CP 正常反代）
- 后续：补跑 run_rtsp_e2e.ps1（延长健康检查等待）与 cp_smoke（跳过本地构建）



# 2025-11-01 12:50:59 前端启动
- Vite 开发服务器：PID=43240 URL=http://127.0.0.1:5173
- 代理：/api,/metrics,/whep → 127.0.0.1:18080；/api/system/info 200
- 可访问：/analysis 页面，选择 source=camera_01，pipeline=det_720p 进行播放


# 2025-11-01 12:52:50 前端取证 (最小)\n\n{"pages":[{"url":"http://127.0.0.1:5173","status":200,"duration_ms":68}],"apis":[{"url":"/api/system/info","status":200,"duration_ms":19}],"whep":[{"url":"http://127.0.0.1:18080/whep","status":200,"acao":"*","methods":"GET,POST,DELETE,OPTIONS","duration_ms":5}],"subscription":{"status":202,"id":"cp-1761972760682-3","location":"/api/subscriptions/cp-1761972760682-3"},"artifacts":[{"kind":"screenshot","path":"D:\\Projects\\ai\\cv\\logs\\front_analysis.png","note":"Analysis page rendered (headless)"}] }\n



# 2025-11-01 15:07:43 Pipelines 编辑器交互修复
- 变更：将 Graph.panning 从 true 改为 { enabled: true, eventTypes: ['rightMouseDown'] }
- 目的：避免框选（rubberband）时画布跟随移动，左键仅用于选择/框选/节点操作，右键用于拖拽画布
- 影响：不改变缩放（Ctrl/⌘ + 滚轮）与吸附/对齐/拖拽等行为



# 2025-11-01 15:11:24 Pipelines 编辑器选中态增强
- 启用 Selection.showNodeSelectionBox 并自定义选中框样式
- 选中时高亮节点描边（#ffd166, width=2），取消选中恢复默认描边
- 框选样式更清晰（半透明蓝色）



# 2025-11-01 15:13:22 Pipeline 边选中态增强
- Edge: 选中高亮(#ffd166, 2.4px)，取消选中恢复默认(#6ea8fe, 1.6px)
- createEdge 与 fromJSON 为边写入基线样式到 data(_stroke/_strokeWidth) 以便还原
- CSS: :deep(svg g.x6-edge-selected path) 提升选中可见性；non-scaling-stroke 保持线宽稳定



# 2025-11-01 15:19:01 Pipeline 删除操作修复
- 增加全局键盘监听：Delete/Backspace 删除选中节点/边（输入框内不触发）
- 生命周期清理：onBeforeUnmount 移除监听，避免泄漏



# 2025-11-01 15:26:25 Pipelines 编辑器：导入 YAML
- Editor.vue 增加 ‘导入YAML’ 按钮与隐藏 <input type=file>，支持选择本地 .yaml/.yml
- 解析器：parseGraphYaml（nodes/edges 简单语法），映射 yamlType→kind（preproc/model/post/overlay）
- 行为：导入后更新 graphJson 并调用 canvas.fromJSON，随后运行校验



# 2025-11-01 15:40:02 编辑器：新增‘居中视图’按钮
- 位置：Editor.vue (editor-fabs-3)，点击调用 canvasRef.fitView()
- 样式：.editor-fabs-3 固定在右上（top:172px）
- 解析：导入 YAML 后已自动 autoArrange + fitView，按钮用于后续手动居中



# 2025-11-01 15:43:24 编辑器：新增‘自动排布’按钮
- 位置：Editor.vue (editor-fabs-4)，点击调用 canvasRef.autoArrange()+fitView()
- GraphEditorCanvas.vue：autoArrange() 基于拓扑层级横向布局 (dx=220, dy=96)



# 2025-11-01 15:51:15 编辑器 UI 调整
- 工具条改为左下角横排图标按钮（导入、导出、自动排布、居中、全屏、Apply）带悬停提示
- 去除右上角老按钮（CSS 隐藏），页面默认隐藏纵向滚动条，仅超出时滚动
- 双击节点打开属性抽屉，移除‘属性’按钮



# 2025-11-01 15:54:14 编辑器按钮位置/尺寸微调
- 工具条改为相对画布容器定位（canvas-shell, position: relative）
- 按钮改为绝对定位于左下角 (left:12px; bottom:12px)；尺寸 28px，间距 6px



# 2025-11-01 15:56:37 工具条紧凑化与半透明背景
- 尺寸：24px→22px；间距：6px→3px；位置：左/下 10→8px
- 背景：rgba(17,20,26,0.28) + blur(6px) + 1px 边框；按钮半透明 hover/active 渐变



# 2025-11-01 16:01:58 全屏与高度修复
- GraphEditorCanvas.vue：.ge 最小高度改为 100%（移除对 64px header 的依赖）
- Editor.vue：.page 高度统一使用 100%，fullscreen 同样用 100%；相对容器填充
- 仍保持‘进入编辑器隐藏 body y 滚动/退出恢复’的双保险策略

2025-11-01 18:46:08\n- 任务: 分析页重复下拉框排查与修复\n  - 操作: 删除遗留临时文件 AnalysisPanel.vue.tmp；检查 AnalysisPanel 仅保留表单式 4 个下拉；准备 DevTools 复核\n  - 结果: 已删除 .tmp；代码侧仅一组下拉；待 DevTools 取证\n- 任务: 前端启动与取证\n  - 操作: 后台启动 web-front dev；尝试连接 Chrome DevTools MCP\n  - 结果: DevTools MCP 未列出页面（No page selected）；待本地启用/授权后继续\n- 任务: 上下文学习\n  - 操作: 阅读 docs/context/CONTEXT.md 与 ROADMAP.md\n  - 结果: 已纳入对话上下文与实现参考\n
2025-11-01 18:49:09\n- 任务: 分析页下拉垂直居中\n  - 操作: 在 AnalysisPanel.vue 追加二次 <style scoped>，对 .toolbar-form 与 .el-form-item/label/content 应用 flex 垂直居中与去除 margin-bottom\n  - 结果: 样式已更新，刷新页面应居中显示\n
