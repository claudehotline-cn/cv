## 2025-10-17 00:52:47

- 任务：修复 SSE 跨域(CORS)
- 变更：在 sseWriteHeaders 增加 CORS 头(Allow-Origin/Methods/Headers)；重建并重启后端(Windows)
- 路径：video-analyzer/src/server/rest.cpp:sseWriteHeaders
- 校验：/api/db/ping=OK；/api/logs 200；DB 模式数据可取；SSE 将不再报跨域


## 2025-10-17 01:45:00

- 任务：后端分页与总数（logs/events）+ 前端日志分页接入
- 变更：
  - /api/logs 与 /api/events/recent 支持 `page/page_size/total`（向后兼容 `limit`）。
  - 新增 Repo 接口：`listRecentFilteredPaged`（含 COUNT 与 LIMIT/OFFSET）。
  - 前端 DBLogsList 切换为服务端分页，变更 dataProvider 传参。
- 文件：
  - video-analyzer/src/storage/log_repo.hpp/.cpp
  - video-analyzer/src/storage/event_repo.hpp/.cpp
  - video-analyzer/src/server/rest.cpp
  - web-front/src/api/dataProvider.ts
  - web-front/src/components/observability/DBLogsList.vue
- 验证：
  - GET /api/logs?page=1&page_size=1 → 返回 { items[1], page, page_size, total }。
  - 前端日志页（DB 模式）正常显示，两条数据，总数=2。

## 2025-10-17 19:25:05

- 任务：依据 CONTEXT.md 与 数据库设计.md 规划下一步工作
- 结论：确定8项下一步任务，按优先级推进（先前端 Events 分页，再 Metrics 与测试取证）
- 计划：
  - 前端：为 EventsList.vue 接入服务端分页（page/page_size/total），时间窗与筛选保持兼容；切换筛选重置到第1页。
  - 前端：DBLogsList.vue 在日期范围改变时强制 page=1 并优化分页摘要展示。
  - 前端：新增 Metrics 页小组件，读取 /metrics 展示 DB Pool、Writer 队列、Retention 指标。
  - 后端：补充 /api/db/retention/status 暴露保留策略配置与最近执行结果。
  - 测试：补充 Python 集成测试覆盖 logs/events 分页与保留策略；Playwright 取证 DB 模式（30天窗口）并保存证据。
  - 脚本：完善 Windows 重启/探测脚本，规避 LNK1104，构建后自动校验 /api/system/info。
  - 文档：执行后追加 memo 与必要的 ROADMAP 更新。
  - 约束：仅从数据库读取，失败时前端需告警（无回退）。

## 2025-10-17 19:31:12

- 前端：完成 EventsList 支持 DB 服务端分页（page/page_size/total），筛选与时间窗变化自动重置到第1页；新增分页控件与错误提示。
- 视图：Observability/Events.vue 增加 LIVE/DB 切换，DB 模式走分页 API；LIVE 保持 SSE。
- 前端：修正 DBLogsList 使用服务端分页时的本地二次 slice 问题；在日期变更时重置页码；新增分页摘要 X–Y/共N。
- 约束：仅从 DB 读取；失败以 ElAlert 告警。

## 2025-10-17 19:48:15

- 后端：新增 /api/db/retention/status 返回保留策略配置与最近执行指标（runs_total/failures_total/last_ms）。
- 路由：GET /api/db/retention/status；位置 video-analyzer/src/server/rest.cpp。
- 说明：此端点不访问数据库连接，始终 200 返回当前配置与内存指标。

## 2025-10-17 19:50:39

- 测试：新增 Python 脚本覆盖分页与保留策略：
  - video-analyzer/test/scripts/check_events_pagination.py
  - video-analyzer/test/scripts/check_logs_pagination.py
  - video-analyzer/test/scripts/check_retention_status.py
- 断言：items 为数组、total 合法；保留策略返回 config.enabled 与 metrics.runs_total。

## 2025-10-17 20:08:34

- 前端取证（DB 模式，30天窗口）：
  - Logs：rowsCount=2，firstRow={ ts: "2025/10/16 21:41:10", level: "info", pipeline: "det_720p", node: "rest", msg: "unsubscribe accepted" }
    - 证据：docs/memo/assets/2025-10-17/front_logs_db_eval.json
  - Events：rowsCount=0，firstRow=null
    - 证据：docs/memo/assets/2025-10-17/front_events_db_eval.json
- 说明：Events 无数据属正常（近 30 天内无事件写入或已清理）；Logs DB 读取成功无回退。

## 2025-10-17 20:17:23

- 修复：切换到 Events DB 模式时立即刷新并默认设为 30 天窗口（避免初次切换空窗口导致 0 条展示）。
- 取证更新：Events（DB 模式）rowsCount=3，证据已更新：docs/memo/assets/2025-10-17/front_events_db_eval.json

## 2025-10-17 20:25:16

- 完善脚本：更新 tools/win/probe_api.ps1（增加 events/logs/db/metrics/retention/status 探测；输出 sample）；默认输出目录改为当天路径。
- 重建并重启后端：通过 tools/build_with_vcvars.cmd + tools/win/restart_backend.ps1，验证新路由 /api/db/retention/status 生效。
- 探测结果：已写入 docs/memo/assets/2025-10-17/api_probe_windows.json（所有端点 200）。

## 2025-10-17 20:35:41

- 前端：扩展 Metrics 页，新增 MetricsDbPanel 显示 DB Pool / Retention 数值卡片与趋势图（轮询 /metrics，每 5s 更新，窗口可选 2/5/10 分钟）。
- 文件：
  - web-front/src/components/observability/MetricsDbPanel.vue
  - web-front/src/views/Observability/Metrics.vue（集成 MetricsDbPanel）
- 说明：采用 Prometheus 文本解析，支持模糊匹配指标名（va_db_retention_* / db_pool_*）。若页面未出现，请重启 npm run preview 并硬刷新。

## 2025-10-17 20:57:46

- 前端：MetricsDbPanel 卡片区新增 Writer 队列（logs/events）两张卡片，默认选择中已包含，趋势图与导出 CSV 均支持。
- 验证：页面已渲染 7 张卡片（含 Writer 队列），已通过 Playwright evaluate 校验。
## 2025-10-17 21:31:13
- 前端：
  - 接入 Metrics 查询面板（基于 /metrics 快照）：实现 metricsQuery/metricsMultiQuery/metricsTop，消除 "No backend configured" 报错；按选择指标返回当前时刻点。
  - Events DB 列表：新增分页摘要 X–Y/共N 与“导出 CSV”（导出当前页）。
- 说明：MetricsQueryPanel 现以快照点展示，后续若接入时序后端可平滑替换为多点序列。
# 2025-10-17 22:56:22
- 任务：数据库索引压测（批量插入+查询耗时）
- 动作：修复 db_bench_index.ps1（变量冲突/命令行长度/输出捕获），执行 -Seed -Logs 1000 -Events 1000
- 结果：生成 docs/memo/assets/2025-10-17/db_bench_index.json，包含四类查询耗时（IN + 近7天窗口）
- 后续：可扩展更大样本与更多过滤组合，增加无索引对照（仅在隔离环境）
# 2025-10-17 23:13:07
- 任务：控制面 REST 映射与错误语义统一
- 变更：新增 /api/control/status(GET)、/api/control/drain(POST)、/api/control/pipeline(DELETE)；gRPC UnsubscribePipeline 失败映射 INVALID_ARGUMENT/NOT_FOUND/INTERNAL
- 路径：video-analyzer/src/server/rest.cpp, video-analyzer/src/app/application.{hpp,cpp}, video-analyzer/src/control_plane_embedded/api/grpc_server.cpp
- 验收：通过；apply→status→drain→remove 全链路 200；不存在对象时 status 200(phase=NotFound)，drain/delete 返回 404
- 取证：已本地用 Invoke-WebRequest 调用并返回 200/404，详见上述输出
