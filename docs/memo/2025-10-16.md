2025-10-16 10:00

- 任务：阅读 CONTEXT 与数据库设计文档，产出后续工作路线图
  - 动作：通读 `docs/context/CONTEXT.md`、`docs/design/数据库设计.md`
 - 产出：`docs/context/ROADMAP.md`（里程碑、实施顺序、首批待办、风险）
 - 结果：已落地，待按路线图推进实现与验证

2025-10-16 10:20

- 任务：启动后端并验证数据库连通与基础查询
  - 动作：以配置目录 `video-analyzer/config` 启动 `VideoAnalyzer.exe`（包含 MySQL 配置）
  - 校验：`/api/system/info` 200；`/api/db/ping` 返回 ok=true；`/api/sessions` 可回读（basic_count>=0）
 - 结果：DB ping 通过；Sessions API 返回正常（存在历史样本）

2025-10-16 10:35

- 任务：长轮询与事件/日志读端点验证（M1 证据）
  - 动作：运行 `check_watch_longpoll.py --base http://127.0.0.1:8082 --url rtsp://127.0.0.1:8554/camera_01`
  - 产出：
    - `docs/memo/assets/2025-10-16/watch_longpoll_output.txt`
    - `docs/memo/assets/2025-10-16/logs_recent.json`
    - `docs/memo/assets/2025-10-16/events_recent.json`
 - 结果：sources rev 发生变化；logs/events watch 返回 keepalive（无新增项时 items=0），读端 `/api/logs`、`/api/events/recent` 正常返回

2025-10-16 10:50

- 任务：读端带过滤回归 + 手动保留清理（purge）验证
  - 动作：
    1) 重新启动后端，订阅/退订一次以写入 DB（stream=evidence_*，profile=det_720p）
    2) 读取最近 1 小时窗口内 `/api/logs?pipeline=...` 与 `/api/events/recent?pipeline=...` 保存 JSON
    3) 统计 purge 前行数（docker exec mysql 计数 events/logs/sessions）
    4) `POST /api/db/retention/purge {events_seconds:1,logs_seconds:1}` 并等待
    5) 再次统计行数并回读过滤结果
  - 产出：
    - `docs/memo/assets/2025-10-16/logs_recent_filtered.json`
    - `docs/memo/assets/2025-10-16/events_recent_filtered.json`
    - `docs/memo/assets/2025-10-16/db_counts_before.txt`
    - `docs/memo/assets/2025-10-16/retention_purge_response.json`
    - `docs/memo/assets/2025-10-16/db_counts_after.txt`
    - `docs/memo/assets/2025-10-16/logs_recent_filtered_after.json`
    - `docs/memo/assets/2025-10-16/events_recent_filtered_after.json`
  - 结果：
    - purge 前：events/logs/sessions 行数分别见 `db_counts_before.txt`
    - purge 执行：返回 `events_ok=true`、`logs_ok=true`
    - purge 后：`events`/`logs` 行数显著下降（见 `db_counts_after.txt`），读端正常

2025-10-16 11:05

- 任务：Sessions 列表与 Watch 回归（分页/时间窗）
  - 动作：
    1) `/api/sessions?limit=10` 基本列表
    2) `/api/sessions?page=1,2&page_size=20&from_ts=..&to_ts=..` 时间窗+分页
    3) `/api/sessions/watch` 初始快照与订阅后变更（since+subscribe/unsubscribe 触发）
  - 产出：
    - `docs/memo/assets/2025-10-16/sessions_list_basic.json`
    - `docs/memo/assets/2025-10-16/sessions_list_window_p1.json`
    - `docs/memo/assets/2025-10-16/sessions_list_window_p2.json`
    - `docs/memo/assets/2025-10-16/sessions_watch_initial.json`
    - `docs/memo/assets/2025-10-16/sessions_subscribe_response.json`
    - `docs/memo/assets/2025-10-16/sessions_watch_changed.json`
    - `docs/memo/assets/2025-10-16/sessions_unsubscribe_response.json`
  - 结果：watch 变更中出现 Running 项（订阅流），分页/时间窗返回正常

2025-10-16 11:20

- 任务：前端 Sessions/Logs/Events 页面取证（Playwright MCP）
  - 动作：
    1) 打开 `#/observability/sessions`，evaluate 返回 url/title/rowsCount/firstRow 关键字段
    2) 打开 `#/observability/logs` 与 `#/observability/events`，evaluate 返回 url/title/rowsCount
  - 产出：
    - `docs/memo/assets/2025-10-16/front_sessions_eval.json`
    - `docs/memo/assets/2025-10-16/front_logs_eval.json`
    - `docs/memo/assets/2025-10-16/front_events_eval.json`
 - 结果：Sessions 表格渲染正常（rowsCount=7，首行字段齐全）；Logs/Events 当前无新增项时 rowsCount=0（watch 页面处于 LIVE/空列表状态）

2025-10-16 11:35

- 任务：Sessions 导出 CSV 与自动刷新取证
  - 动作：
    1) Sessions 页点击“导出 CSV”获得 `sessions.csv`
    2) 保持“自动刷新”开启，点击“刷新”，采集会话表格行数
  - 产出：
    - `docs/memo/assets/2025-10-16/sessions.csv`
    - `docs/memo/assets/2025-10-16/front_sessions_eval_after_refresh.json`
 - 结果：CSV 导出成功；当前过滤窗口下前端显示 Total=0，建议选择更大的时间窗或清空流/管线筛选以查看历史记录（数据库中会话数据已验证存在）

2025-10-16 11:50

- 任务：REST 读端改为“仅 DB，失败即错误”
  - 代码：`video-analyzer/src/server/rest.cpp`
    - logs：`handleLogsRecent` 去掉合成回退；当 `logs_repo` 为空或查询失败时返回 503 + 错误
    - events：`handleEventsRecent` 同上（DB 专读，无回退）
    - sessions：`handleSessionsList` 当无 `sessions_repo` 或查询失败时返回 503；移除“空数组”回退
  - 构建：`tools/build_with_vcvars.cmd`，停止运行中的 `VideoAnalyzer.exe` 后重链通过
  - 校验：
    - `/api/sessions?limit=5` → 200，返回 DB 中 items 与 total
    - `/api/logs?limit=5`、`/api/events/recent?limit=5` → 200（DB 可用时），失败将返回 503/错误信息

2025-10-16 12:10

- 任务：前端 Logs/Events 支持 DB 时间窗视图与错误提示
  - 变更：
    - `web-front/src/api/dataProvider.ts`：logsRecent/eventsRecent 支持 from_ts/to_ts；HTTP 非 2xx 透传错误文本
    - `web-front/src/components/observability/LogsViewer.vue`：错误提示（ElAlert）已接；准备引入 DB/LIVE 切换与时间窗（后续继续）
    - `web-front/src/components/observability/EventsList.vue`：新增时间窗选择（datetimerange），刷新时带 from_ts/to_ts；错误提示条
  - 构建：`npm run build` 成功
  - 结果：Events 页可按时间窗从 DB 取数并显示错误，Logs 页错误提示已接（DB 时间窗切换后续继续完善）


## 2025-10-16 22:51:03
- 任务：前端自动化（Logs→DB 模式刷新、取证）
- 动作：Playwright 访问 5174 日志页，切换 DB 模式，设置 30 天窗口，抓取 rowsCount 与首行
- 结果：rowsCount=0，firstRow=null；证据：docs/memo/assets/2025-10-16/front_logs_db_eval.json
- 备注：页面显示 No Data；后端可能无可见数据或时间窗/编译版本差异，已重建并使用独立 preview(5174)。


## 2025-10-16 23:36:06
- 任务：前端自动化（Logs→DB 模式，4173 与 5174 双端）
- 动作：在 4173/5174 切换 DB、设置30天时间窗并采集rowsCount/firstRow；HTTP直查 /api/logs 复核
- 结果：两端 rowsCount 均为 0；/api/logs 直查当前不可达（8082未监听）；证据：
  - docs/memo/assets/2025-10-16/front_logs_db_eval_4173.json
  - docs/memo/assets/2025-10-16/front_logs_db_eval.json
  - docs/memo/assets/2025-10-16/api_logs_recent_now.json
- 建议：启动后端 VideoAnalyzer REST(8082) 后重试；或我可改用 mysqlsh 直接查库取证。


## 2025-10-16 23:51:46
- 任务：重启前后端 + 前端自动化复采（DB 模式）
- 动作：停止/启动 VideoAnalyzer，启动 preview(5176)，切换 DB、设置30天窗口并采集rowsCount/firstRow
- 结果：rowsCount=0；firstRow=null；URL=http://127.0.0.1:5176/#/observability/logs
- 证据：docs/memo/assets/2025-10-16/front_logs_db_eval_after_restart.json
