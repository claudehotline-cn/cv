2025-10-26 11:56:52

任务：将 /api/sources/watch_sse 纳入冒烟并校验指标增量，补充 Python 脚本；修正 SSE 首帧超时问题。

执行情况：
- 服务器：`controlplane/src/server/main.cpp` 的 SSE 分支在写入 headers 后先发送空的 `event: state`（items=[]），避免无流时 10s 首事件超时；`http_server.cpp` 支持将 sources/watch_sse 识别为 SSE。
- 脚本：
  - 新增 `video-analyzer/test/scripts/cp_sources_watch_sse.ps1`（SSE 首事件校验），已在 smoke 中执行；
  - 新增 `controlplane/test/scripts/check_cp_sources_watch_sse.py`（跨平台）；
  - `tools/run_cp_smoke.ps1`：加入 cp_sources_watch_sse 步骤；在 Metrics 校验中追加 `/api/sources/watch_sse|GET|200`。
- 构建与验证：
  - 单脚本运行 PASS；
  - run_cp_smoke.ps1 集成执行（含控制流测试与 watch_sse 校验）通过，指标增量检查通过。

备注：真实 Watch（subscriptions/:id/events）保留通过 CP_TEST_WATCH=1 控制，默认关闭；若需稳定重放，可在本机使用 tools/mediamtx 做 RTSP 上游。

