2025-11-13 00:06

- 任务：根据《cv_训练流水线（training_pipeline）详细设计_v_1》输出 WBS 与任务清单。
- 要点：分阶段（M0/M1/M2）拆解到组件（CP/Trainer/前端/MLflow/CI），列清依赖与验收。
- 产出：WBS 与任务清单已回传到会话，用于后续立项与拆 PR。

2025-11-13 00:06

- 任务：在 `docs/plans` 生成两份规划文档（WBS 与任务清单）。
- 变更：
  - 新增 `docs/plans/wbs-mlflow-training-pipeline.md`
  - 新增 `docs/plans/tasks-mlflow-training-pipeline.md`
- 要点：
  - WBS 按 M0/M1/M2 与组件分解，明确交付物/依赖/验收。
  - 任务清单按 PR 粒度（PR-1~PR-8）组织，含依赖与验收标准。

2025-11-13 00:16

- 任务：实现 PR-1（数据层：`train_jobs` 表 + CP CRUD 框架）。
- 变更：
  - `db/schema.sql`：新增 `train_jobs` 表（status/phase/cfg/metrics/artifacts 等 + 索引）。
  - `controlplane/include/controlplane/db.hpp`：声明 `train_job_{create,update,get_json}` 与 `list_train_jobs_json`。
  - `controlplane/src/server/db.cpp`：实现上述 CRUD（支持 MySQLX 与 JDBC 经典接口；无依赖时安全降级为 no-op/空结果）。
  - `controlplane/CMakeLists.txt`：增强 MySQL 连接器探测（mysql-concpp/unofficial + 回退搜索 mysqlcppconn8/mysqlcppconn 与头文件），自动定义 `HAVE_MYSQLX`/`HAVE_MYSQL_JDBC` 并链接。
  - `docker/cp/Dockerfile`：构建/运行期安装 `libmysqlcppconn-dev` 以提供运行时库与头文件（便于后续扩展）。
- 使用（Docker）：
  1) 迁移 DB：`mysql -h 192.168.50.78 -P 13306 -uroot -p123456 < db/schema.sql`
  2) 构建 CP：`docker compose -f docker/compose/docker-compose.yml build cp`
  3) 运行：`docker compose -f docker/compose/docker-compose.yml up -d cp`
  4) 配置：默认 `driver: mysql`（经典接口）。如使用 MySQL X 协议，可改为 `driver: mysqlx` 并设置 `mysqlx_uri`。
- 验收：当前仅落地数据层与 CRUD API；路由与 Runner 将在 PR-2 实现。

2025-11-13 00:22

- 任务：在 docker compose 中添加 MySQL 容器，并将 CP 指向该容器。
- 变更：
  - `docker/compose/docker-compose.yml`：新增 `mysql` 服务（MySQL 8.4，挂载 `db/schema.sql` 至 `docker-entrypoint-initdb.d`，首次启动自动建库建表；暴露 `13306:3306`；健康检查）。`cp` 增加依赖 `mysql` 健康。
  - `docker/config/cp/app.yaml`：将 DB 指向 `host: mysql, port: 3306`，保持 `driver: mysql` 与 `schema: cv_cp`。
- 使用：
  1) `docker compose -f docker/compose/docker-compose.yml up -d mysql`
  2) 首次启动会根据挂载的 `db/schema.sql` 初始化数据库与表。
  3) `docker compose -f docker/compose/docker-compose.yml up -d cp`（将等待 mysql 健康就绪）
- 备注：如需继续使用外部 DB，将 `docker/config/cp/app.yaml` 改回对应主机与端口即可。

2025-11-13 18:30

- 任务：实现 PR-2（训练编排骨架）。
- 变更：
  - `controlplane/src/server/main.cpp`：
    - 新增内存态与事件缓冲（TrainJobRec），生成 `t_<hex>` job id。
    - 新增路由：`POST /api/train/start`、`GET /api/train/status?id=...`、`GET /api/train/list`。
    - 新增 SSE：`GET /api/train/events?id=...`（内置最小模拟器产生 state/done 事件，便于前端接通）。
    - 通过 `db::train_job_create/update/get/list` 与数据库弱耦合（可无 DB 时退化）。
- 说明：当前未集成真实 Trainer 子进程，先用模拟器保障端到端事件语义与前端接入；后续 PR 将替换为子进程读取 stdout JSON Lines。
- 推送：因暂时网络限制未能推送；待网络可用时执行 `git push -u origin IOBinding`。

2025-11-13 18:40

- 任务：实现 PR-3（manifest 基础前置校验）。
- 变更：
  - 新增 `controlplane/include/controlplane/manifest.hpp` 与 `src/server/manifest.cpp`，基于 yaml-cpp 实现最小必要校验：`model.task`、`exports.onnx.opset`/`dtype∈{fp32,fp16,int8}`、`io.inputs[*].name/shape`、`io.outputs[*].name`。
  - `controlplane/src/server/main.cpp`：
    - `/api/repo/upload`：如上传 `model.yaml`，在写入 VA 前执行校验，失败返回 422（UNPROCESSABLE_ENTITY）。
    - `/api/repo/convert_upload`：调用 VA 前先通过 `RepoGetConfig` 拉取 `model.yaml` 并校验；缺失或不合法返回 422。
    - `/api/repo/load`：加载前同样拉取并校验 `model.yaml`，不通过返回 422。
  - `controlplane/CMakeLists.txt`：加入 `src/server/manifest.cpp` 构建。
- 说明：该前置校验为最小闭环，后续可扩展兼容矩阵（ORT/TRT 版本）、INT8 工件完整性与 trtexec dry-run。

2025-11-13 18:55

- 任务：实现 PR-4（Trainer 骨架：最小分类 + MLflow + ONNX + manifest + JSON Lines）。
- 变更：
  - 新增 `model-trainer/`：
    - `model_trainer/entry.py`：解析配置、训练/评估、MLflow 上报、导出 ONNX、生成 manifest、输出 JSON Lines（metrics/artifact/done）。
    - `model_trainer/data/datamodule.py`：ImageFolder 或随机数据 DataLoader（CPU 可运行）。
    - `model_trainer/tasks/classification.py`：ResNet18 训练与评估（torch/torchvision）。
    - `model_trainer/export/onnx_export.py`：最小 ONNX 导出（opset/dynamic_axes/input_size）。
    - `model_trainer/manifest.py`：生成满足最小校验的 `model.yaml`。
  - 配置与依赖：`model-trainer/configs/example-cls.yaml`、`model-trainer/requirements.txt`、`model-trainer/README.md`。
- 使用：
  - `python -m model_trainer.entry -c model-trainer/configs/example-cls.yaml`
  - 未设置 `MLFLOW_TRACKING_URI` 时默认写入 `logs/mlruns`（文件存储）。
- 说明：当前 Trainer 为最小闭环实现，后续 PR 将由 CP 子进程拉起并对接 SSE；再扩展门槛、注册表、对象存储等能力。

2025-11-13 19:05

- 任务：增强 PR-2（CP 子进程对接 Trainer）。
- 变更：
  - `controlplane/src/server/main.cpp`：新增 `launch_trainer_subprocess`，当设置 `CP_TRAINER_CMD` 时以子进程方式运行训练器，解析 stdout JSON Lines（metrics/artifact/done），更新内存态与 `train_jobs`，并通过 `/api/train/events` 推送。
  - 回退：未设置 `CP_TRAINER_CMD` 时使用原有模拟器，保证 SSE 语义不变。
- 用法：
  - 在 CP 容器/环境设置：`CP_TRAINER_CMD="python3 -m model_trainer.entry -c {config}"`
  - 启动训练：`POST /api/train/start`，通过返回的 `events` URL 订阅 SSE。
