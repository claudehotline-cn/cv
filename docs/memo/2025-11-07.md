# 2025-11-07 21:13:15

## 2025-11-07 21:14:48

- 任务：审阅 `docs/design/tensorrt_engine.md`，对照 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md` 评估后续工作。
- 结论：M1（原生 TensorRT + 异步预热）在上下文描述中已落地；M2 需推进：可观测（日志/Prom 预热与耗时）、回退链健壮化、端到端基准、动态 profile 与引擎序列化、/api/system/info。
- 建议：
  1) 实现 `engine_runtime` 状态面板与 REST `/api/system/info`；
  2) Prom 增加预热/构建/推理耗时直方图与显存水位；
  3) 完成回退链与失败指纹日志（deserialize 失败→ORT-TRT→CUDA）；
  4) TRT 引擎序列化与 plan 缓存；
  5) 动态 shape/profile（先静态→逐步引入）；
  6) 1080p/720p 基准与 P95 汇报；
  7) 可选：ORT NV TensorRT RTX EP 支持（provider: tensorrt-rtx）。

## 2025-11-07 21:28:00

- 任务：结合源码评估 `tensorrt_engine.md` 方案落地情况与差距。
- 发现：
  - 工厂与原生 TRT：`video-analyzer/src/analyzer/model_session_factory.cpp:1` 提供 Provider 归一与选路；`video-analyzer/src/analyzer/trt_session.cpp:143` 支持 `.engine/.plan` 反序列化与 ONNX 构建（TRT10 路径含 `enqueueV3`）。
  - 路径选择：`video-analyzer/src/analyzer/multistage/node_model.cpp:36` 基于 Provider 选择 `model_path_trt|_ort`，并回填运行态；`ms.node_model`/`ms.nms` 关键日志齐全。
  - 统一流与零拷贝：`video-analyzer/src/exec/stream_pool.hpp:1` 提供全局流；ORT/TRT 会话均支持 `user_stream` 和 device 视图/host stage。
  - 预热：`video-analyzer/src/core/pipeline_builder.cpp:85` 订阅后后台 `open_all`，并发与超时可配；ModelRegistry 侧有最小“预热/缓存”指标导出（`video-analyzer/src/server/rest_metrics.cpp:224`）。
  - NVENC 合规：`video-analyzer/src/media/encoder_h264_ffmpeg.cpp:1` 限定 `aq-strength ∈ [1,15]` 并映射 rc/preset。
  - Compose GPU：`docker/compose/docker-compose.gpu.override.yml:1` 已用 `gpus: all` 注入。
- 差距：
  1) 回退链未实现：TRT 反序列化/构建失败时未自动降级至 ORT-TRT→CUDA（当前 `loadModel` 失败即返回）。
  2) `/api/system/info` 缺实现：路由已注册（`video-analyzer/src/server/rest_routes.cpp:229`/`:236`），仅在 `video-analyzer/src/server/rest_impl.hpp:1090` 有声明，测试 `check_system_info_subs.py` 期望的字段尚未返回。
  3) 预热/构建耗时指标：有 ModelRegistry 直方图，但未覆盖 `open_all` 背景预热与会话构建时长（仅日志）。
  4) 原生 TRT 引擎序列化：运行时从 ONNX 构建未落盘 `.engine`（工具已提供 `trt_engine_exporter`）。
  5) RTX EP：仅别名归一为 `tensorrt`，未实现 NV TensorRT RTX EP 专属路径。
- 建议优先级：
  A) 最小可用观测：补齐 `/api/system/info`（包含 engine_runtime、订阅配额回显、ModelRegistry 概要）。
  B) 健壮性：在 NodeModel.open 失败时，按 native→ORT-TRT→CUDA 顺序尝试次优 Provider，并记录失败指纹；或在工厂内部提供带装载探测的 create+load 封装。
  C) 指标：为 `open_all` 与会话加载添加 Prom 直方图与失败计数；保留现有日志。
  D) 性能：将 TRT 构建成功时可选序列化 `.engine` 至 `/app/.trt_native_cache/engines`（可控开关）。
E) 后续：RTX EP（可选），动态 profile/批量（M2），基准报告。

## 2025-11-07 21:35:00

- 反馈与对齐：
  - `/api/system/info` 由 CP 提供，VA 不暴露该接口；评估与实现建议已切换到 `controlplane` 路径。
  - RTX EP（`provider: tensorrt-rtx`）暂不实现，维持别名归一（回退到 `tensorrt`）。
- CP 现状：`controlplane/src/server/main.cpp:160` 已实现 `/api/system/info`（聚合 VA QueryRuntime 与 VSM health），但缺少 `data.subscriptions` 段（前端与 e2e 用例期望）。
- 建议：在 CP `/api/system/info` 中补充 `subscriptions` 字段（heavy_slots, model_slots, rtsp_slots, max_queue, ttl_seconds + source），值来源：优先 env（如 `CP_SUBS_*`），否则默认值与来源标注 `defaults`；保持 2s 缓存。

## 2025-11-07 21:32:25

## 2025-11-07 21:40:00

## 2025-11-07 21:55:00

## 2025-11-07 22:05:00

## 2025-11-07 22:15:00

## 2025-11-07 22:01:10

## 2025-11-07 22:20:00

- 任务：将 Triton WBS 同步到计划总览并拆分任务板。
- 变更：
  - 更新 `docs/plans/M1_M2_tasks.md`，新增 “Triton 集成（M0–M2）” 小节与勾选项，链接至设计/WBS 文档。
  - 新增 `docs/plans/triton_tasks_board.md`，按阶段拆分任务，包含 Owner/开始/截止/状态（建议日程）。
- 结果：计划视图与任务分解完成，可直接用于跟踪与分派。

- 任务：根据参考方案在 `docs/design` 生成 Triton 整合设计文档。
- 变更：新增 `docs/design/triton_integration_design.md`，包含：目标、抽象与接入点、配置与 provider 映射、指标/日志、Compose 与推进路线（M0→M2）、风险与缓解、验收与基准。
- 结果：为后续落地 `TritonGrpcModelSession` 与工厂映射提供实施依据。

- 任务：补充基准与文档。
- 变更：
  - 文档：新增 `docs/examples/benchmark.md`（基准指南）与 `docs/plans/benchmark-report-template.md`（报告模板）。
  - 脚本：新增 `video-analyzer/test/scripts/benchmark_metrics.py`，从 `/metrics` 采样 `va_pipeline_fps` 与 `va_frame_latency_ms` 直方图，计算 FPS 与各阶段 P50/P95。
- 用法：`python3 video-analyzer/test/scripts/benchmark_metrics.py --metrics http://127.0.0.1:9090/metrics --label tensorrt-native --duration 30`
- 结果：产出 JSON，可直接粘贴进报告模板；支持多路时按 source 聚合平均。

- 任务：可选开启原生 TRT 引擎序列化。
- 变更：
  - `video-analyzer/src/analyzer/trt_session.hpp/.cpp`：在 ONNX→TRT 构建成功后，若开启序列化则生成 `.engine` 并保存至目录；命名 `<stem>_sm<major><minor>_<fp>.engine`。
  - 配置入口：
    - `engine.options.trt_serialize_on_build: true`（或 `serialize_on_build`）
    - 可选 `engine.options.trt_engine_dir: "/app/.trt_native_cache/engines"`（默认从 `VA_TRT_ENGINE_DIR`，否则 fallback 到 `VA_TRT_CACHE_DIR/engines`，再默认 `/app/.trt_native_cache/engines`）。
- 日志：`analyzer.trt serialize: saved engine '<path>' bytes=<n>`（失败忽略）。

- 任务：实现 VA 回退链与加载/预热指标，并输出到 /metrics。
- 变更：
  - 回退链：`video-analyzer/src/analyzer/multistage/node_model.cpp` 在 `open()` 中按 `tensorrt-native→tensorrt→cuda` 顺序尝试，会话加载失败自动降级；为每次尝试记录耗时（秒）与失败计数。
  - 指标聚合：新增 `video-analyzer/src/analyzer/load_metrics.hpp/.cpp` 提供全局直方图（边界：5ms..10s）与失败计数的快照接口：`model_session_load` 与 `graph_open_duration`。
  - 预热指标：`video-analyzer/src/core/pipeline_builder.cpp` 的后台 `open_all` 预热记录直方图与失败计数。
  - /metrics：`video-analyzer/src/server/rest_metrics.cpp` 输出两类直方图与失败计数：
    - `va_model_session_load_seconds{bucket,sum,count}`、`va_model_session_load_failed_total`
    - `va_graph_open_duration_seconds{bucket,sum,count}`、`va_graph_open_failed_total`
- 结果：回退链提升健壮性；Prom 可观测模型加载与预热耗时分布。

- 任务：修复 Controlplane Docker 构建错误（找不到 CMake 目标 `yaml-cpp::yaml-cpp`）。
- 变更：`controlplane/CMakeLists.txt` 在 `find_package(yaml-cpp)` 后增加目标名探测与回退（优先 `yaml-cpp::yaml-cpp`，否则 `yaml-cpp`，再者 `YAML::yaml-cpp`），确保在 Ubuntu 22.04 的 `libyaml-cpp-dev` 下正常链接。
- 结果：应消除 “Target links to yaml-cpp::yaml-cpp but not found” 的配置阶段错误；与现有 `docker/cp/Dockerfile`（已安装 libyaml-cpp-dev）匹配。

- 任务：在 Controlplane 实现 `/api/system/info` 的 `data.subscriptions` 段。
- 变更：`controlplane/src/server/main.cpp` 在构建 system info JSON 时新增 `subscriptions` 字段（heavy_slots、model_slots、rtsp_slots、max_queue、ttl_seconds），并包含 `source` 子对象（各键来源：env 或 defaults）。环境变量键：`CP_SUBS_HEAVY_SLOTS`、`CP_SUBS_MODEL_SLOTS`、`CP_SUBS_RTSP_SLOTS`、`CP_SUBS_MAX_QUEUE`、`CP_SUBS_TTL_SECONDS`。
- 结果：满足前端与 e2e 对 `data.subscriptions` 的期望结构，默认无 env 时返回默认值与 `defaults` 来源。


- 任务：阅读 `docs/context/CONTEXT.md` 与 `docs/context/ROADMAP.md`，并将内容加入当前对话上下文。
- 操作：定位文件、逐段读取（行数<250），将两份文档原文输出至对话。
- 结果：两文档内容已注入当前会话作为上下文；未修改任何代码或配置。
- 后续：如需对文档生成提要、术语表、待办清单或与实现差异对照，可继续指示。

## 2025-11-07 22:30:00

- 任务：按需将 Triton 版本从 24.08-py3 升级至 25.08-py3。
- 变更：`docker/compose/docker-compose.gpu.override.yml` 中 `triton` 服务镜像改为 `nvcr.io/nvidia/tritonserver:25.08-py3`。
- 注意：若使用 TensorRT plan（tensorrt_plan），需用同版本 TensorRT（25.08）重建 `model.plan`（可用 `nvcr.io/nvidia/tensorrt:25.08-py3` 容器内 `trtexec` 重建），否则可能出现反序列化版本不匹配错误（safeVersionRead）。

## 2025-11-07 22:45:00

- 修复：当 `provider=triton` 且未启用 Triton 客户端时，fallback 到 ORT/CUDA 不再使用 `__triton__` 占位路径（避免 ORT 加载失败）。
  - 修改 `video-analyzer/src/analyzer/multistage/node_model.cpp`：路径选择依据 `ProviderDecision.resolved`。
- 构建：`docker/va/Dockerfile.gpu` 引入 `tritonclient` 阶段（基于 `nvcr.io/nvidia/tritonserver:25.08-py3`），编译时开启 `-DUSE_TRITON_CLIENT=ON -DTRITON_CLIENT_ROOT=/opt/tritonclient`，运行镜像注入 `/opt/tritonclient/lib` 至 `LD_LIBRARY_PATH`。
- 结果：provider=triton 将使用 Triton gRPC 客户端；禁用客户端时自动回退 ORT/CUDA 且路径正确。

## 2025-11-08 02:55:41 Triton 回退诊断与构建修正
- 现象：provider=triton 但推理落在 ORT CUDA（ 日志）。
- 可能根因：构建未启用  或未链接 。
- 操作：
  - 更新 ，在 CMake 传入  与  并在构建后用  断言链接存在（缺失直接失败）。
  - 提供容器内排查命令（ldd/ls/strings/curl），指导验证 Triton 可达与模型 IO 名。
- 后续：用户按 compose 重建  并回贴 ldd/日志以进一步定位。

## ${NOW} Triton 回退诊断与构建修正（修正记录）
- 现象：provider=triton 但推理落在 ORT CUDA（`analyzer.ort` 日志）。
- 可能根因：构建未启用 `USE_TRITON_CLIENT` 或未链接 `libgrpcclient.so`。
- 操作：
  - 更新 `docker/va/Dockerfile.gpu`，在 CMake 传入 `-DTRITON_CLIENT_INCLUDE_DIR` 与 `-DTRITON_CLIENT_GRPC_LIB` 并在构建后用 `ldd` 断言链接存在（缺失直接失败）。
  - 提供容器内排查命令（ldd/ls/strings/curl），指导验证 Triton 可达与模型 IO 名。
- 后续：用户按 compose 重建 `va` 并回贴 ldd/日志以进一步定位。

## ${NOW} 修复 Triton Client 25.08 API 与 gRPC/absl 链接冲突
- 现象：构建阶段 `triton_session.cpp` 编译错误（Create/Infer 双指针签名不匹配）；链接阶段大量 `absl::lts_20230125::*` 与 `gpr_assertion_failed` 未解析。
- 根因：
  1) 25.08 SDK 的 `grpc_client.h` 使用 `InferInput**/InferResult**` 签名，与现有 unique_ptr 适配不一致。
  2) CMake 全局加入 `/opt/tritonclient/include` 导致所有源码使用 SDK 内的 `absl` 头（lts_20230125），但链接到系统 gRPC/absl（20220623），产生符号版本不匹配。
- 变更：
  - `video-analyzer/src/analyzer/triton_session.cpp` 重写 Create/Infer 调用，按双指针 API 传原始指针数组，局部持有对象生命周期。
  - `video-analyzer/CMakeLists.txt` 移除全局 `include_directories(TRITON_CLIENT_INCLUDE_DIR)`；改为仅对 `triton_session.cpp` 追加 `-I${TRITON_CLIENT_INCLUDE_DIR}`，避免污染全局 absl/grpc 头选择；保留链接 `libgrpcclient.so`。
  - `docker/va/Dockerfile.gpu` 采用 SDK 标准路径 `/workspace/install/{include,lib}` 拷贝到 `/opt/tritonclient`；构建后强校验 `ldd` 中命中 `grpcclient`。
- 预期：编译/链接通过；运行期日志可见 `analyzer.triton`，Prom 指标 `va_triton_rpc_seconds_*` 递增。
- 待验证：重建 `va` 镜像并重启容器，执行 ldd/logs/metrics 三项核验。

## ${NOW} 修复 Triton Host 路径 D2H 缺失 CUDA 头
- 现象：编译错误 `cudaMemcpyDeviceToHost` 未声明。
- 原因：`triton_session.cpp` 在 `USE_CUDA` 分支调用了 CUDA API，但未包含 `cuda_runtime.h`。
- 变更：条件包含 `cuda_runtime.h`（存在性检测 `__has_include`），定义 `VA_HAS_CUDA_RUNTIME`，与 `ort_session.cpp` 风格一致。
- 预期：GPU→Host 复制编译通过，T0 Host 路径可以从 GPU 预处理输出做 D2H staging 后送 Triton。

## ${NOW} 解决 Triton SDK 静态 gRPC 链接缺少 OpenSSL 依赖
- 现象：链接阶段 `libgrpc.a(ssl_session_openssl.cc.o)` 报 `d2i_SSL_SESSION@@OPENSSL_3.0.0` 未解析；`DSO missing from command line`。
- 根因：优先使用 SDK 的静态 gRPC 库时，需要显式链接 `-lssl -lcrypto`，否则静态对象中的 OpenSSL 符号无法解析。
- 变更：`video-analyzer/CMakeLists.txt` 在 `USE_TRITON_CLIENT` 分支中 `find_package(OpenSSL)`，若找到则链接 `OpenSSL::SSL OpenSSL::Crypto`；否则回退查找 `ssl`/`crypto` 并链接。
- 预期：链接通过，`VideoAnalyzer` 成功生成，随后运行期可看到 Triton gRPC 调用日志与指标。

## ${NOW} 元数据/SHM/指标与文档推进（阶段1）
- 失败原因指标：为 Triton RPC 增加按 reason 细分的计数，并在 REST /metrics 导出（总+按原因）。
- 日志节流：`analyzer.triton` 的 run 日志按模块节流（默认 1s），可由 env 覆盖。
- 配置透传：ConfigLoader 透传 triton_* 与 force_provider/providers，新增 triton_no_batch（默认保留 batch 维）。
- 文档：更新设计与快速上手，加入配置片段与常见问题。
- CUDA SHM：占位与降级日志（请求 SHM 时先回退到 host raw）；下一阶段实现 Register/SetSharedMemory。
