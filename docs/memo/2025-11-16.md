2025-11-16 任务日志

- 时间：2025-11-16
- 内容：根据当前调试结果确认 In-Process Triton（嵌入 VA 进程的 Triton/TensorRT 后端）在 CPU I/O 模式下调用 ServerInferAsync 仍然触发 Segmentation fault（即使 NVDEC→CUDA 预处理→Overlay 等 GPU 链路正常），为快速恢复到已验证的基线版本，使用 `git reset --hard f2efc511a207fa3e985a504c76d5e3214034c2c4` 将仓库回退到「引入OCSORT多阶段GPU追踪与ReID模型接入」提交，对应恢复 video-analyzer/analyzer 多阶段节点与 Triton 集成的稳定实现，并保留后续在此基础上再次迭代 In-Process/GPU 零拷贝方案的空间。回退操作只影响当前开发仓库，不包含对远程仓库的强制推送，后续如需保留本次回滚需通过新的提交推送至远程。 

- 时间：2025-11-16
- 内容：在保持当前稳定的 In-Process Triton + GPU I/O 实现前提下，为减少对固定输出名（如 dets/output0）的硬编码依赖，在 video-analyzer/src/analyzer/model_session_factory.cpp 中调整 In-Process 会话的输出名初始化逻辑：仅当配置中显式提供 triton_outputs 时才将解析后的列表写入 TritonInprocModelSession::Options::output_names，否则保持为空；随后在 video-analyzer/src/analyzer/triton_inproc_session.cpp 的 run() 中为 In-Process 路径增加 “首次推理自动发现输出名” 能力：当 output_names 为空时不调用 AddRequestedOutput，让 Triton 返回模型配置中的全部输出，并在解析 InferenceResponseOutput 时收集每个输出的 oname（若缺失则回退为 output<i>），推理完成后将收集到的真实输出名写回 opt_.output_names 并打印一条 autofill 日志。这样，在 YAML/engine 配置未显式设置 triton_outputs 时，In-Process 路径会在首帧推理后自动与模型 config.pbtxt 中的输出名对齐，既避免了对默认 dets/output0 的依赖，又不影响已有显式配置的行为。受限于本地环境缺少 OpenCVConfig.cmake，video-analyzer 的 CMake 配置与构建仍需在完整依赖环境中执行验证。 

- 时间：2025-11-16
- 内容：为配合 In-Process Triton 动态输出名填充，在 video-analyzer/src/analyzer/multistage/node_model.cpp 中放宽了 NodeModel::open 阶段对输出名的校验逻辑：在为每个 provider 加载会话后，仅当解析到的输出名列表为空且 provider 非 Triton 系列（triton/triton-inproc/triton-grpc）时才视为错误并中止该 provider；对于 Triton/Triton-InProc 会话，即便 loadModel 阶段 outputNames 为空也允许通过，后续由 triton_inproc_session.cpp 中的自动发现逻辑在首次推理后填充真实输出名。这样可以避免多阶段 Graph 在使用 In-Process Triton 但未显式配置 triton_outputs 时，因 open 阶段 outputNames 为空被错误判定为“model has zero declared outputs”，导致 Graph node open failed。 

- 时间：2025-11-16
- 内容：为避免多阶段 OCSORT 图中 ReID 节点在 In-Process Triton 下因输入名不匹配（reid_passvitb 模型输入名与全局 triton_input=images 不一致）导致 ServerInferAsync 报错 `unexpected inference input 'images' for model 'reid_passvitb'`，在 video-analyzer/src/analyzer/multistage/node_model.{hpp,cpp} 中为 NodeModel 增加了按节点覆盖 provider 的能力（force_provider_override_ 字段，并在 open() 中优先将 cfg.force_provider 写入 desc.options["force_provider"]），同时更新 docker/config/va/graphs/analyzer_multistage_ocsort.yaml：检测节点 det 显式指定 model_path_triton: \"yolov12x\" 保持使用 In-Process Triton + GPU I/O，ReID 节点 reid 增加 force_provider: cuda，使其通过 ORT/CUDA 路径加载 models/reid_passvitb.onnx 提供特征而不再走 Triton；这样既保留了检测链路的 In-Process GPU 零拷贝，又绕开了当前环境下 reid_passvitb 在 Triton 中的输入名兼容性问题，避免 Graph node det 因上游 Triton 错误持续返回 analyze() false。 

- 时间：2025-11-16
- 内容：按“让 ReID 也走 Triton 且在配置中不强制写 triton_input，由模型配置自动适配输入名”的需求，在 video-analyzer/src/analyzer/model_session_factory.cpp 中调整 Triton provider 的输入名初始化：构造 TritonGrpcModelSession::Options 时默认清空 input_name，仅当 engine.options 中显式存在 triton_input 时才将其写入并标记 has_triton_input=true；In-Process 分支仅在 has_triton_input==true 时才将 topt.input_name 传递给 TritonInprocModelSession::Options::input_name，否则保持为空。随后在 video-analyzer/src/analyzer/triton_inproc_session.cpp 的 loadModel 中增加基于 Triton C API 的 ModelMetadata 适配逻辑：当 opt_.input_name 为空时，通过 TRITONSERVER_ServerModelMetadata 获取模型元数据并序列化为 JSON，从中定位 inputs[0].name 并回填到 opt_.input_name，打印一条 autofill 日志；若 Metadata 调用或解析失败则保持现状，由后续 AddInput 失败日志辅助诊断。这样，在不配置 triton_input 时 In-Process Triton 会自动按 config.pbtxt 中的首个输入名进行推理，同时仍允许通过全局或节点级 triton_input 显式覆盖。 
  在调整过程中曾因 TRITONSERVER_ServerModelMetadata 调用参数顺序与头文件签名不符（误传为 server,&md,model_name,version）导致 Docker 内构建失败，随后修正为 server,model_name,version,&md 并重新编译，确认问题来自编译期类型不匹配而非运行时逻辑错误。 

- 时间：2025-11-16
- 内容：针对 In-Process Triton 在 yolov12x 推理阶段出现 `response error: output0: attempted a copy of ... Bytes to an uninitialized memory` 的问题，回溯发现该错误源自 tensorrt backend 与自定义 ResponseAllocator 交互时尝试向我们提供的 GPU 缓冲执行内部拷贝。为避免这类不透明内部状态导致的崩溃或错误，在 video-analyzer/src/analyzer/triton_inproc_session.cpp 中撤销自定义 ResponseAllocator 的使用，改为完全依赖 Triton 默认分配器返回输出缓冲，并在解析 InferenceResponse 时按 mtype/mid 判断：若输出位于当前 GPU 且 opt_.use_gpu_output=true，则使用 next_pow2 策略在会话持久化的 out_dev_bufs_ 中分配/复用 GPU 缓冲，通过 cudaMemcpyDeviceToDevice 将 Triton 的 GPU 输出拷贝进去并暴露为 on_gpu=true 的 TensorView；若输出在 GPU 且需要 CPU 视图，则执行 D2H 拷贝；纯 CPU 输出则直接复制到 host_out_bufs_。这样既保留了 In-Process GPU I/O（Triton→VA 之间 D2D 拷贝，无额外 Host 往返），又规避了自定义 allocator 带来的 “uninitialized memory” 报错。当前修改已在 Docker 构建日志中通过类型检查验证，但完整运行时回归仍需在具备 OpenCV 的环境中执行。 

- 时间：2025-11-16
- 内容：在确认默认响应分配器路径稳定后，应用户要求重新启用 In-Process 自定义 ResponseAllocator，但仅在已知输出名且启用 GPU 输出的情况下使用：在 video-analyzer/src/analyzer/triton_inproc_session.cpp 的 run() 中，当 opt_.use_gpu_output=true 且 opt_.output_names 非空时，为该会话创建 ResponseAllocator，并在 alloc 回调中按 tensor_name 查找输出索引、为匹配的输出在 out_dev_bufs_ 中分配/复用持久化 GPU 缓冲并返回 TRITONSERVER_MEMORY_GPU；若找不到匹配名称或 GPU 分配失败，则安全回退到 CPU 内存（malloc）并标记为 TRITONSERVER_MEMORY_CPU；release 回调仅释放 CPU malloc 的缓冲，GPU 缓冲由会话持久化管理。随后在 SetResponseCallback 中仅在 use_custom_allocator=true 时传入 allocator 与用户上下文，否则继续使用默认分配器。解析 InferenceResponse 时保持原有逻辑：对 GPU 输出在 use_gpu_output=true 时仍按 out_dev_bufs_ 返回 TensorView，对 CPU 输出保留 host_out_bufs_ 路径，从而同时满足“使用自定义 ResponseAllocator”与“保持 GPU I/O 路径”的需求。当前实现仍然受限于无法在本地 OpenCV 缺失环境中完成 CMake 配置与编译验证。 

- 时间：2025-11-16
- 内容：为修复 analyzer_multistage_ocsort 图在 Graph I/O 验证阶段出现的 `Graph input missing before node name='roi' key='det'` 与 `key='rois:track'` 等错误，在 video-analyzer/src/analyzer/multistage/node_roi_batch_cuda.hpp 与 node_track_ocsort.hpp 中对节点声明的 inputs/outputs 进行了对齐：`NodeNmsYolo::outputs()` 在 Graph 视图中使用 `\"rois:det\"` 表示 ROI 流，而 `NodeRoiBatchCuda` 与 `NodeTrackOcsort` 之前分别声明输入键为 `\"det\"` 和输出键为 `\"track\"`，导致 Graph.finalize 看不到从 NMS 到 ROI/Track 再到 overlay.cuda 的完整 ROI 链路。调整后，`NodeRoiBatchCuda::inputs()` 返回 `\"rois:\" + in_rois_key_`，`NodeTrackOcsort::inputs()` 返回 `\"rois:\" + in_rois_key_`，`NodeTrackOcsort::outputs()` 返回 `\"rois:\" + out_rois_key_`，而节点内部仍然在 Packet.rois/gpu_rois 中使用裸键 `in_rois_key_`/`out_rois_key_` 访问实际数据。这样既保持 Graph I/O 视图中 ROI 流的统一命名（rois:det → rois:track），又不改变运行时数据结构，避免了 Graph finalize 报 missing input 的问题，使 analyzer_multistage_ocsort YAML 能通过 I/O 校验并生效。 

- 时间：2025-11-16
- 内容：根据 gdb backtrace（SIGSEGV 落在 `triton::core::InferenceResponse::InferenceResponse(...)`）以及 Triton 日志中出现的 “attempted a copy of ... Bytes to an uninitialized memory” 等信息，最终确认 In-Process 自定义 ResponseAllocator 路径仍存在与 Triton/TensorRT 后端内部交互不稳定的问题。为避免在高并发实时分析场景下再次触发 VA 进程崩溃，今天重新回到“方案 A”：在 video-analyzer/src/analyzer/triton_inproc_session.cpp 的 run() 中彻底移除自定义 ResponseAllocator 相关逻辑，改为始终使用 Triton 默认 ResponseAllocator 获取输出 buffer，然后基于 InferenceResponseOutput 返回的 mtype/mid 对 GPU 输出执行一次 cudaMemcpyDeviceToDevice 到会话级持久化的 out_dev_bufs_ 中，再以 on_gpu=true 的 TensorView 暴露给后续 Node；CPU 输出继续通过 host_out_bufs_ 管理。保留此前的动态输入名（ModelMetadata 自动填充）与首帧动态输出名发现逻辑，并在关键路径增加 DebugSeg 日志。由于本地环境仍缺少 OpenCVConfig.cmake，video-analyzer 的 CMake 配置在当前沙箱内无法完成，完整编译与运行回归需在宿主机 Docker GPU 镜像中执行。 
