2025-10-26 05:02:10

任务：启动 VSM 与 VA，执行 /api/control 端到端成功路径验证与指标采样。

执行情况：
- 启动：
  - VSM：video-source-manager/build/bin/VideoSourceManager.exe（已运行）。
  - VA：video-analyzer/build-ninja/bin/VideoAnalyzer.exe bin/config（已运行）。
- 健康：GET /api/system/info → 200，runtime.provider=cuda，vsm.streams=0。
- 成功路径：
  - POST /api/control/apply_pipeline { pipeline_name:p_demo, spec.yaml_path:<abs yaml> } → 202；
  - GET /api/control/status?pipeline_name=p_demo → 200（phase=OK，metrics.phase=Ready）；
  - POST /api/control/drain { pipeline_name:p_demo, timeout_sec:2 } → 202（drained=true）；
  - DELETE /api/control/pipeline?pipeline_name=p_demo → 202。
- 指标：/metrics 出现计数：apply_pipeline(202)=1、status(200)=1、drain(202)=1、pipeline(DELETE 202)=1。

备注：使用绝对 YAML 路径（analyzer_multistage_example.yaml）确保解析稳定；hotswap 成功路径因缺少有效模型暂未验证，后续补齐。

