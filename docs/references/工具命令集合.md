# 依赖下载

```bash
docker run --rm \
    -v /home/chaisen/projects/cv/.repository:/root/.m2/repository \
    -v /home/chaisen/projects/cv/controlplane-spring:/app \
    -w /app \
    maven:3.9.9-eclipse-temurin-21 \
    mvn -DskipTests dependency:go-offline
```

# 推流命令

```bash
ffmpeg -re -stream_loop -1 -i D:\Projects\ai\cv\video-analyzer\data\01.mp4 -c:v libx264 -profile:v baseline -tune zerolatency -preset veryfast -x264-params "keyint=60:min-keyint=60:scenecut=0" -bf 0 -c:a aac -ar 44100 -ac 2 -b:a 128k -f rtsp rtsp://127.0.0.1:8554/camera_01 

ffmpeg -re -stream_loop -1 -i D:\Projects\ai\cv\video-analyzer\data\01.mp4 -r 30 -vsync cfr -fflags +genpts -c:v libx264 -profile:v baseline -tune zerolatency -preset veryfast -x264-params "keyint=30:min-keyint=30:scenecut=0" -g 30 -bf 0 -pix_fmt yuv420p -b:v 3500k -maxrate 3500k -bufsize 3500k -rtsp_transport tcp -c:a aac -ar 44100 -ac 2 -b:a 128k -f rtsp rtsp://127.0.0.1:8554/camera_01  

ffmpeg -re -stream_loop -1 -i D:\Projects\ai\cv\video-analyzer\data\01.mp4 -vf scale=1280:720:force_original_aspect_ratio=decrease -pix_fmt yuv420p -c:v libx264 -profile:v baseline -tune zerolatency -preset veryfast -g 30 -x264-params "keyint=30:min-keyint=30:scenecut=0:repeat-headers=1" -bf 0 -c:a aac -ar 44100 -ac 2 -b:a 128k -f rtsp -rtsp_transport tcp rtsp://127.0.0.1:8554/camera_01
```

# Onnxruntime 源码构建

```bash
.\build.bat --use_cuda --use_vcpkg --cuda_home "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.9" --cudnn_home "C:\Program Files\NVIDIA\CUDNN\v9.13" --config Release --build_shared_lib --parallel --cmake_extra_defines "CMAKE_CUDA_ARCHITECTURES=120" --cmake_extra_defines "FETCHCONTENT_TRY_FIND_PACKAGE_MODE=NEVER"

.\build.bat --use_cuda --use_vcpkg --cuda_home "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.9" --cudnn_home "C:\Program Files\NVIDIA\CUDNN\v9.13" --config Release --build_shared_lib --parallel --cmake_extra_defines CMAKE_CUDA_ARCHITECTURES=120 --cmake_extra_defines CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDLL
```

# Mysql docker 容器启动命令

```bash
docker run -p 13306:3306 --name mysql -v D:\Projects\java\data\mysql\log:/var/log/mysql -v D:\Projects\java\data\mysql\data:/var/lib/mysql -v D:\Projects\java\data\mysql\conf:/etc/mysql/conf.d -e MYSQL_ROOT_PASSWORD=123456 -d mysql
```

# 终端工具提示词

```
测试前先试用GET /terminals?userId=assistant-1查看有没有终端，有的话全关闭，测试还是使用终端api开启两个终端，一个用于运行video-analyzer项目，一个用于执行测试命令或自动化测试脚本，测试完毕后关闭两个终端，关闭video-analyzer项目，关闭还没有结束的测试脚本（如果有的话）。项目构建前，先检▌ 查项目是否在运行，如果在运行先关闭，再构建，项目构建不使用终端api。

追加不需要在询问我，以后就按代码修改->测试->如果测试通过，追加文档->提交代码到远程仓库。
```

# FFmpeg 转 annexb

```bash
ffmpeg -y -i "D:\Projects\ai\cv\tools\webrtc_file_test\build\01.mp4" -c:v copy -bsf:v h264_mp4toannexb "D:\Projects\ai\cv\tools\webrtc_file_test\build\01.h264"

ffmpeg -i input.mp4 -c:v h264_nvenc -preset p1 -tune ll -bf 0 -g 50 -rc cbr -b:v 6M -maxrate 6M -bufsize 6M -an -r 25 -f h264 out.h26
```

