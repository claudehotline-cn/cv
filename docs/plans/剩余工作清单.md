# 剩余工作清单（按计划与现状对比）

- 控制面接口完善
  - 增补 REST 映射
    - GET/DELETE 管理面 Pipeline：列表/删除/状态/排空（映射 gRPC 的 ListPipelines/RemovePipeline/GetStatus/Drain）
    - 文件：video-analyzer/src/server/rest.cpp:610
  - gRPC 错误码语义补齐
    - UnsubscribePipeline 失败路径按 INVALID_ARGUMENT/NOT_FOUND/INTERNAL 返回
    - 文件：video-analyzer/src/control_plane_embedded/api/grpc_server.cpp:147
- 校验与错误语义
  - REST HTTP 状态精细化（400/404/409/503 与内部错误码一致）
  - 覆写键校验与警告（未知的 node./type:/engine.options.* 输出告警并透传）
  - 未消费输出/重复输出等图级校验已有，补文档说明
- 可观测性与指标
  - /metrics 增加错误标签与明细
    - 控制面操作计数按 code 维度拆分（va_cp_*_total{code=...}）
    - VSM 侧为 REST/gRPC 增加错误按 code 统计
  - 运行态查询增强
    - 新增“管理面 Pipeline 一览”REST（含 project/tags/revision/graph/template/overrides 摘要）
    - 文件：video-analyzer/src/control_plane_embedded/controllers/pipeline_controller.hpp:10
- VSM 稳定性与解析
  - JSON 解析健壮性（数组/嵌套/转义/大体积体）与错误提示
    - 文件：video-source-manager/src/app/rest/rest_server.cc:112
  - SSE 并发/资源回收边界复核，心跳/超时配置项落盘与文档
    - 文件：video-source-manager/src/app/source_agent.cc:1
  - /metrics 健壮性：并发抓取、优雅关停；Windows 仅在 WIN32 下链接 ws2_32
    - 文件：video-source-manager/src/app/metrics/metrics_exporter.cc:1、video-source-manager/CMakeLists.txt:1
- 模板与覆写深化（可选增强）
  - 模板参数化：支持 graph 内占位符 ${param} 与 overrides.params.* 映射
  - overrides 白名单/类型化（bool/int/float 自动解析），避免字符串歧义
  - 引擎覆写作用域隔离（当前为全局 EngineManager；如需按 Pipeline 隔离需更大改造）
- 端到端回归与脚本
  - 快速验收脚本：Apply→Subscribe→推流→渲染→Update(model/overrides)→Remove
  - 多路并发/失败退避/重试覆盖（VSM 重启、网络抖动、deadline/backoff 行为）
  - curl/grpcurl 样例已加，补一键批跑脚本
- 文档与索引
  - 将新增接口/示例加入 docs 索引与 README 入口
    - 已新增：docs/examples/overrides_examples.json、docs/examples/grpcurl_apply_overrides.md、docs/examples/
            rest_apply_overrides.md
  - 说明“引擎覆写为全局生效”的限制与规避建议

  建议的下一步（可立即执行）

- 补 REST 管理面映射（List/Remove/Status/Drain）与 REST 错误状态精细化。
- /metrics 增加控制面错误按 code 的计数标签。
- VSM JSON 解析增强与 SSE 并发清理再回归一轮。
- 为 overrides 增加键校验与类型化解析，输出未知键警告。
