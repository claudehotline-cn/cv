name: cv-trainer

entry_points:
  main:
    parameters:
      config: {type: string}
    command: "python -m model_trainer.entry -c {config}"

# 使用 mlflow run 时建议：
#   mlflow run . -P config=/path/to/train.yaml --env-manager local
# 需保证当前环境已安装 requirements（或提供 conda.yaml/requirements 文件并选择合适 env-manager）。

