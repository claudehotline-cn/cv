from typing import Dict, Any

def get_config() -> Dict[str, Any]:
    return {
        "name": "test-agent",
        "key": "test_agent",
        "description": "test-agent created via Agent CLI"
    }