from typing import Dict, Any

def get_config() -> Dict[str, Any]:
    return {
        "name": "demo-agent",
        "key": "demo_agent",
        "description": "demo-agent created via Agent CLI"
    }