{
  "annotations": { "list": [] },
  "title": "CV Connectivity Overview",
  "uid": "cv-connect-01",
  "panels": [
    {
      "type": "stat",
      "title": "Targets UP",
      "gridPos": { "x": 0, "y": 0, "w": 8, "h": 6 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [ { "expr": "sum(up)", "legendFormat": "" } ]
    },
    {
      "type": "table",
      "title": "Targets Health",
      "gridPos": { "x": 8, "y": 0, "w": 16, "h": 8 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [ { "expr": "up", "legendFormat": "{{job}} {{instance}}" } ]
    },
    {
      "type": "timeseries",
      "title": "VA Throughput (bytes/s)",
      "gridPos": { "x": 0, "y": 8, "w": 24, "h": 8 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [ { "expr": "rate(va_encoder_bytes_total[1m])", "legendFormat": "{{source_id}} {{path}}" } ]
    },
    {
      "type": "timeseries",
      "title": "Scrape Duration (s)",
      "gridPos": { "x": 0, "y": 16, "w": 24, "h": 8 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [ { "expr": "scrape_duration_seconds", "legendFormat": "{{job}} {{instance}}" } ]
    }
  ],
  "schemaVersion": 38,
  "version": 1
}

